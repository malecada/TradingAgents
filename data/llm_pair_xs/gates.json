{
 "llm_c3p_pair_xs": {
  "registered_utc": "2026-08-17",
  "spec": "docs/superpowers/specs/2026-08-17-llm-c3p-pair-xs-charter.md",
  "parent": "llm_c3_rank_xs (dead P0, THESIS §64) — pairwise revival per Aug-14 leads note",
  "hypothesis": "Weekly Bradley-Terry scores from LLM pairwise duels on anonymized numeric PIT cards have residual IC (after vol/momentum/size neutralization) vs 5d forward returns AND beat a GBDT twin on identical inputs",
  "universe": "cards.parquet verbatim from C3 (top-200 by 30d median dollar volume, monthly PIT, 799-sym store)",
  "construction": "k=10 seeded permutation-pairing rounds/week; EVERY pair issued in both orders (A,B)+(B,A) in disjoint prompts; 20 duels/prompt; gpt-5.4-mini temp 0 JSON; BT via MM, 200 iters, alpha=0.5 virtual-opponent prior, log-strength score",
  "windows": {
   "dev": ["2021-01-01", "2025-03-31"],
   "holdout": ["2025-04-01", "2026-07-01 — SEALED, virgin for LLM-XS family, one-shot only if P3 passes"]
  },
  "probes": {
   "P0b_order_swap": "base run, 8 seeded weeks (seed 20260817): pooled swap-consistency >=0.60 AND per-week >=0.55 in >=6/8 AND slot-1 pick rate in [0.35,0.65]; STOP else",
   "P0a_determinism": "3-week seeded subset cache-bypassed rerun: instance agreement >=0.90 AND weekly BT-score Spearman >=0.90; STOP else",
   "P0c_slot_invariance": "3-week disjoint seeded subset, instances re-shuffled+re-chunked: instance agreement >=0.80 AND weekly BT-score Spearman >=0.80; STOP else",
   "P0d_cost_checkpoint": "infra only: extrapolate P1+P2 spend from P0 tokens; if projected > cap surface to user before P2; registered cost-only de-scope = biweekly Fridays, blind to results",
   "P1_anonymization": "26-week seeded subset named vs anon, identical pairs/orders; named weekly IC > 1.5x anon IC => memorization STOP; primary eval anonymous only",
   "P2_incremental": "full anon dev run; weekly residualization on {vol rank, 4w momentum, size rank}; mean residual IC>0 with NW-t>=2.0 AND LLM residual IC >= LightGBM twin (walk-forward, identical features/targets); STOP on either",
   "P3_decile_ls": "s43 harness, 5bp+funding: net SR>=0.8, dual-family placebo p<0.05, DSR>0.5 at declared n, >=3/4 subperiods nonneg"
  },
  "multiplicity": "confirmatory n_trials=1 for the P2 primary gate (single frozen pre-registered hypothesis, rail 9); family <=10 ledgered configs and cumulative ledger reported as declared sensitivities; ledger data/llm_pair_xs/trial_ledger.jsonl append-only",
  "llm_spend_cap_usd": 150,
  "stop_rule": "any STOP = dead this cycle; no prompt/k/batch/alpha/field/frequency changes after P0 runs; unresolved-instance rate <0.95/week = infra flag not a tuning license; holdout untouched until registered one-shot"
 }
}

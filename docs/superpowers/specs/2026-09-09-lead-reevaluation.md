# September 9 lead reevaluation charter

The user has authorized reevaluating leads where the repaired system changes the evidential basis. This cycle is limited to fixed historical constructions affected by a concrete defect. The previous repair and its S2/S3/ENet correction remain complete; they are not repeated. No new strategy, threshold, forecast-model fit, venue, or holdout is introduced.

## Fixed slate and interpretation

- Momentum: all12 original L7/14/28 × skip0/1 × K10/20 configurations, corrected weekly held-unit accounting and same full-universe benchmark.
- Carry: all6 original L1/7/30 × leg0.1/0.2 configurations, signed realized funding and original4.5% full-capital charge. This is a lower-priority completeness check because omitted maintenance costs generally flattered the old book.
- Liquidation fade: all6 original threshold2.5/3.5 × horizon6/24/48 hourly configurations, original universe/triggers/exposures, corrected hourly compounding and executable holdings. Funding remains excluded as originally specified and is a limitation. Original probe requirements remain binding; an unavailable measurement does not become a negative signal verdict.
- PRX pairs: original50 formation months January2021–February2025, corrected Engle–Granger selection, original random comparator and fixed-beta persistence measurement. Every month and failed pair count is retained. Original Wilcoxon is reported with a predeclared monthly dependence sensitivity; no P1 runs even if the historical P0 comparator passes.
- NLST4: original3981 entered pools (1205 prior,2776 new-set), existing cached data only. Correct feature availability, prior-only scaling, completed wallet-outcome timing and intradayFX. Original global top-quintile economics remain retrospective cohort diagnostics because the threshold uses the full evaluated sample. A causal online trading filter would be a new design and is outside this cycle. No P1 runs.

The full machine-readable specification, cells, original thresholds, seeds and stop rules are in `data/predlab/gates.json[audit_reevaluation_2026_09_09]`. They are committed before empirical outcomes. All original gates remain unchanged.

## Windows, funding and immutable data

Daily and hourly strategy decisions use January1,2021–March31,2025, with prior data for causal warmup. Liq-fade includes March31's last hourly bar. PRX retains its original50 month loop, ending February2025. NLST4 consumes only original development-created pool windows; their already observed settlement cashflows may extend past the creation boundary, and ETH quotes are used only for those cached settlements. No post-March2025 created pool or unspent holdout series is evaluated. Combo-C1 already spent the momentum/carry/value/liq-fade holdout; this status is not reset.

Raw stores are read-only. All sources are hashed before use and rechecked at completion. Missing held prices/funding cause explicit failure; no silent zero filling, cohort truncation or replacement data is permitted. DEX legacy ownership without verified availability is missing, not negative; raw2.b24 must precede entry; wallet outcomes become known only at the later of nominal7days and actual selected exit header. No API or RPC is called.

## Staged inference and denominators

All26 primary cells are retained. For24 accounting configurations, compute original/corrected summaries and original-denominator DSR (momentum74, carry87, liq-fade100). Also report current cumulative accounting DSR at150, fixed from126 existing unique rebuild strategy identities plus24 registered configurations; forecast trials are not mixed into that accounting namespace. Failure of either the original SR floor or original-denominator DSR establishes failure of the original conjunction, so expensive original bootstrap/placebo tests may be skipped with explicit `not_run_primary_gate_failed`. They remain unknown and every cell stays in the denominator. A survivor receives its original remaining gates; no winner is selected and no holdout follows.

Zero-fee, double-cost and deliberately incorrect log-return PnL replays are predeclared forensic diagnostics under the same signal/book. Simple returns remain the only valid accounting convention. Diagnostic configurations are not candidates. Missing or invalid inputs remain an accounting/data limitation, not proof of no edge.

## Execution, review and stop

New wrappers call audited library functions, not legacy script main functions or stale local fast engines. Tests first use synthetic examples for window fences, held units, unavailable observations, unchanged-input verification, missing-month denominators and causal feature/FX/history timing. All executable source and registration are committed before any run. Each family has an exclusive start marker and immutable output; append ledger rows and correction records after outcomes. No criteria are changed after seeing results. Report every evaluated and blocked cell, effect uncertainty where computed, the original and current policy comparison, and further data requirements without a profitability claim.

Pre-result metadata clarification: NLST4 pins original pair/quarter/block membership; creation timestamps were interpolated and cannot be relabelled exact. Some original entries occur as late as April8; original helper settlements end April15 at09:21:35UTC. ETH quote reads are capped at open timestamps strictly before April15 at09:20UTC and used only for those existing pools. No April-created sample is admitted.

Pre-result source clarification: liquidation-fade keeps the original shared RNG48, including consuming identical draws for skipped portfolio tests. Its P2 cumulative event return is compounded; the old additive sum is a historical diagnostic, not the corrected25bp probe. Right-censored development-end outcomes are counted and excluded; internal missing prices are unavailable. This is an arithmetic correction, with no signal or threshold change.

PRX formation regressions are recomputed as the original rolling statistical selector requires. The no-fit restriction applies to forecast-model retraining; no new model class or formation setting is introduced.

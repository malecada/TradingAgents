# Research preparation result — September 10, 2026

**The bounded preparation is complete; no strategy-search experiment was run.**
Zero validated strategies and the historical verdicts remain unchanged. The
next phase can use the [updated launch prompt](../PROMPT_STRATEGY_RESEARCH_PHASE_2026-09-10.md)
and [current state](../research/STATE.md).

## Changes and acceptance

| Preparation item | Delivered and checked |
|---|---|
| Safe default verification | A reviewed, fail-closed offline test inventory, explicit empirical/network/account opt-ins, importlib collection for duplicate verifier names, network/write guards and a pinned minimal CI workflow. The historical V2 replay copies inputs to temporary storage when separately admitted. |
| Portable instructions | Tracked concise AGENTS, a CLAUDE pointer, truthful README/start page, one current-state file, three validated domain skills, and evidence/script/engine/data indexes. Root instructions and skill links point to the tracked sources; originals are preserved in a hashed local backup. |
| Locked runtime | Checkout-local CPython 3.13.13, 234 installed packages from the unchanged lock, correct package import origin, and an offline `uv sync --check` completeness check. No dependency upgrade or engine migration. |
| New-program admission | Committed charter/source checks, explicit family/parent and canonical sample identities, cumulative budgets, exposed/spent-window guards, and frozen prospective designs with later committed input bindings. |
| Run lifecycle | Exclusive claims, declared hash-checked inputs, immutable outputs, complete cell denominators, serialized once-only terminal records and failure receipts. Independent verification reconstructs claims from committed evidence before they can affect future admission. |
| Evidence integrity | 42 declared repository anchors, 38 carry capture members and 284 external input files match their expected hashes. Historical source, gates and financial evidence remain preserved. |
| Codex setup | Astra High workspace defaults, Astra Medium workers, three role files, a selectable research profile, and four overlapping skills disabled. No connector or entire plugin was installed/removed. Activation details and global skill scope are explicit in the setup guide. |

The independent review found claim-editing and path/reference validation gaps in
the new helper. Those were corrected with synthetic regression coverage before
the final integrated run. Published claims are pinned during execution; prior
claims are reconstructed from committed registrations/bindings; secret aliases
and malformed revision arguments are rejected before access/subprocess work.

## Verification evidence

- [Offline suite](offline-tests.txt): **742 tests and 20 subtests passed** in
  61.03 seconds. This is the reviewed engineering/checker profile, not a claim
  that every historical test was run. It includes **65 synthetic lifecycle tests**.
- [Runtime receipt](runtime.json): Python 3.13.13, local editable source, installed
  version inventory, lock SHA and successful offline environment-completeness check.
- [Artifact check](artifact-check.json): all declared anchors/members/external
  hashes match. Local Git blobs are checked for tracked anchors. This does not
  verify remote backups of the external stores or completeness of the full corpus.
- [Agent comparison](agent-evaluation/REVIEW.md): two fresh-context Astra Medium
  and two Astra High answers to the same synthetic packet; **12/12 functional
  checks per answer**. Both efforts had minor instruction-detail omissions.
  No effort superiority/equivalence, timing improvement or usage savings was
  established. These implementations are evaluation artifacts, not production code.
- Three domain skills passed `quick_validate.py`; local documentation links,
  TOML/YAML parsing and Git whitespace checks passed. The two lifecycle examples
  run in disposable synthetic Git repositories and independently verify their
  own structural receipts.

The financial ledger remains **820 rows**, SHA-256
`4459ddc70d41d9a99db06ab53d9ad4c8f42b6f4d282abd93b4857b4474041927`.
The dated-carry capture manifest and its 432-row measurement ledger remain
unchanged. Of 8,823 pre-existing tracked data/docs/findings/lock files checked
before preparation, only the intentionally updated future launch prompt differs;
all 8,822 other files remain byte-identical. The former detailed handoff and
readiness review are retained as historical checkpoints.

## Operational limits and next session

Read [Codex setup](../research/CODEX_SETUP.md) before choosing the next task's
effort. The workspace defaults were verified, but this worktree's project layer
is ignored until its main checkout is trusted. A named research profile provides
the requested model/worker defaults when entering the worktree directly. Trust
and permission settings were not changed, and the current task retains its
runtime selection. Four personal skill exclusions apply across projects;
all 20 configured plugin entries remain enabled.

The Python test guards and run helper are cooperative controls, not OS sandboxes.
Hashes and Git dates do not prove economic validity, honest data publication or
complete exposure history. Each empirical runner and independent reviewer must
still establish those properties. Heavyweight dependency separation, broad
engine consolidation, historical runner migration and universal data relocation
were deliberately unnecessary for this bounded preparation.

Hosted CI passed for implementation commit `a24670c4f8718304e8ec37b1b3866d4563aa5fae`:
[GitHub Actions run](https://github.com/malecada/TradingAgents/actions/runs/34511369570).
The [delivery receipt](delivery.json) records the checked revision and branch;
the subsequent documentation checkpoint does not change executable source. The next
research phase must create its actual program/charter/gates before outcomes,
retain adaptive ancestry, and continue through evidence-backed decisions. The
22 settlement-blocked cases remain deferred. No provider contact, paid purchase,
account operation, quote refresh, strategy replay, paper start, deployment or
scheduler was performed.

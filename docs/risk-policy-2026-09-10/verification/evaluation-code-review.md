# Independent evaluation and runner review

The root reviewer read the evaluator, runner and synthetic integration tests before any empirical execution. No material blocker was found. The final source commit identifies the reviewed implementation; any subsequent source change requires renewed review before execution.

The fixed gate is checked against implemented policy/cost/reporting constants. Every saved input is pinned and reconciled with the prior immutable result. Arrow date filtering precedes payload materialization and complete target/control calendars are then required. Saved target directions are never reconstructed from new account outcomes.

All A00 controls execute before alternatives, each across four independently simulated cost paths and both sleeves. Every original trace column and complete return frame must match; a control failure prevents all alternative execution. Each new sleeve/cost call constructs a fresh controller, so blocks, stops and permanent halts cannot leak between books. Equity and trace returns reconcile before evaluation.

The evaluator retains initial NAV and complete cash clocks, including the fixed periods. Zero-variance Sharpe is null within a completed measurement. Policy metadata, causal sigma, turnover, staged accounting, nominal risk and block conformance are checked. Original-target sizing age remains explicitly distinguished from daily policy resizing. Dollar summaries and contrasts retain sleeve identities and units.

Failure handling preserves a successful sibling sleeve without manufacturing an index. Global failures preserve all72 identities in start/failure/prepared-ledger receipts; input/control failures can append unavailable attempts only when source and the exact ledger prefix are intact. A source/ledger mismatch prevents central mutation. The normal path serializes all ledger rows and the complete result before one locked append and verifies exact final bytes. Exclusive output creation prevents blind reruns. A disk/interruption failure after append requires recovery from saved receipts, not re-execution.

All54 direct and54 factorial effects are deterministic descriptive calculations. No bootstrap, p-value, winner selection or adoption gate is present. Invalid-log shadows keep the same primary exposures/stops/costs and are labeled invalid; nullable sign/order diagnostics cannot establish a strategy. All costs, stops and exposure changes remain visible.

The separate result checker uses direct algebra instead of the runner/controller to verify saved marks, charges, policy decisions, exits, clocks, summaries, contrasts and hashes. Its initial review gaps and corrections are retained in `checker-code-review.md`. The formatter's unavailable and stale-receipt issues are likewise retained in `report-code-review.md` and were corrected before source freeze. Synthetic failure probes and the final related test suite provide the execution evidence; no real input values were used for this review.

Final reviewed identities: runner `5a541de99ec8a22e343d838a24d74f8550cf20894c0a34ca3ec757ab37c60bcd`; evaluator `9aa66c099a6f1b5c616c3e6dd020ddeb28e8f8d0db446dc16e8ca5eecf379a18`. The runner's final addition explicitly rejects a prior result whose execution commit differs from the registered source identity; its synthetic failure case performs no engine calls and preserves the failure denominator.

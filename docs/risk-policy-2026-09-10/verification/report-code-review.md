# Independent report formatter review

Initial verdict: corrections required before source freeze. Scope: `verification/render_report.py`, the committed charter/gate, and the runner/evaluation result schema. No empirical observations, strategy simulation or network request was used. The only execution was a fabricated all-unavailable 72-cell result in a temporary directory, built with the runner's `unavailable_cell` helper and the registered metadata.

1. **Incomplete fixed-period denominator and misleading unavailable diagnostics.** The original loop at lines87–90 iterated only saved `periods`; an unavailable index has no list, so its three registered intervals disappeared. The synthetic all-unavailable fixture produced zero period rows instead of216, while retaining72 primary,54 contrast,288 cost,144 sleeve and72 shadow rows. At line83, absent halt diagnostics became `none` in all144 unavailable sleeve rows. Arm-level sums likewise became zero without showing the available-sleeve denominator. Required correction: generate the three registered periods for every cell; preserve status/reason for absent results; distinguish unknown halt state from an observed absence of halts; disclose which sleeve observations contribute to sums.
2. **Independent verification receipt was not tied to the rendered result.** Lines34–35 checked only `status == 'pass'`. The same synthetic fixture supplied `result_sha256='wrong-result-hash'`; all five reports were still written and the narrative claimed independent verification. Required correction: compare the review's result SHA-256 with the exact result bytes before rendering, and reject a mismatch before writing any report.
3. **Undefined Sharpe was omitted from convention calculations but counted as eligible.** Lines95–114 counted complete shadow books/direct comparisons, then skipped null Sharpe values without a separate valid Sharpe denominator. All-cash complete books can have undefined Sharpe. Required correction: preserve distinct finite comparison counts for Sharpe and compound return, and retain the null reason rather than interpreting an undefined comparison as an unchanged sign/order.
4. **Artifact availability and output publication require qualification.** Line131 stated that all576 trace/daily/stop sets exist even when sleeve evaluations are unavailable. Retained identities and actual available artifact sets should be distinguished. All five destination paths should be checked before the first write; the original sequential exclusive writes could leave a partial report set when a later destination already existed.

The fixture output was: five reports;72 primary rows;54 contrasts;288 cost rows;144 sleeve rows;zero period rows;zero complete shadows/direct comparisons;144 unavailable first-halt entries incorrectly shown as `none`; mismatched verification SHA accepted. All temporary fixture files were removed automatically.

The core interpretation is appropriate: fixed full-calendar comparisons are descriptive, the original holdout remains spent, separate sleeve indices are not executable pooled portfolios, lower exposure or indefinite waiting can reduce losses without proving alpha, and invalid-log arithmetic cannot select a policy. The fixed18×4 design, all cost identities and all primary sleeve identities were retained in input order. No selection or financial replay was introduced by the formatter.

A related schema observation was sent to the parent: the independent checker's `check_periods` should verify exactly the three registered ordered periods before iterating, so an empty list on a purported complete index cannot pass through that loop. Source changes remain owned by the parent. Final disposition will be appended after the bounded corrections are rechecked.

## Follow-up after corrections

Final verdict: **PASS; no remaining material formatter issue found in the bounded review.** Reviewed formatter SHA-256: `07aae294503e9f5f8256d598adb7e39e61d3196d7f847c5880a189db51acdbba`.

Four independent synthetic probes completed successfully using temporary directories and manually fabricated values, without reading empirical outcomes or replaying any policy:

- **All72 identities unavailable:** all72 primary,54 contrast,288 cost,144 sleeve,216 period and72 shadow rows survived. Unknown halt dates were unavailable rather than `none`; empty measured-sleeve groups produced unavailable activity/stop/risk sums. Pipe and newline characters in synthetic error reasons were escaped, preserving a constant Markdown column count in every table.
- **All72 complete, all-cash books with undefined Sharpe:** all registered table denominators survived. The72 complete shadows had zero eligible Sharpe comparisons and72 eligible compound-return comparisons; the54 complete direct contrasts had zero eligible Sharpe comparisons and54 eligible compound-return comparisons. All216 period rows retained `zero_variance`. Observed absence of a halt remained `none` on all144 complete sleeve rows.
- **Wrong review result hash:** rejected before any Markdown file was written.
- **A pre-existing later destination (`PERIODS.md`):** rejected before any earlier report was written; the original destination stayed unchanged.

The revised narrative distinguishes registered identities from available artifact sets, displays measured and finite/paired denominators, and retains the exposure-suppression qualification without implying strategy validation. Source inspection also confirmed that `check_periods` now requires the three exact ordered intervals and each complete period calendar. The original findings above are preserved as the review history. This follow-up checks report safety and synthetic schema behavior only; final empirical artifact arithmetic and provenance remain separate checks.

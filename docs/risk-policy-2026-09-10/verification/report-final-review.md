# Final saved-result and interpretation review

Verdict: **PASS after one prose rounding correction.** The rendered reports were checked against the immutable saved result, not by rerunning a strategy or return series. Result SHA-256: `245ad6a30022a4ab7f115b1acb52d33dc5537ce43d8c0b6850e5ec25391d1006`; execution source: `2dfb047d5c2f2ff821706736eb9f5508a14e7304`.

All850 displayed data rows matched the saved scalar metrics or their stated point differences:4 overview,72 primary,54 direct contrast,288 cost,144 primary sleeve,216 period and72 shadow rows. Identity lists were also checked against the exact registered order, preventing a duplicate row from replacing a missing configuration, arm, cost, sleeve or period. All rows are complete and no registered identity is omitted.

The independent numerical review receipt matches the same result hash and reports13,780,216 passed assertions,576 traces,144 original control traces,288 return frames,72 original control return frames and72 shadows, with no unavailable evaluations. Its numerical checks were not rerun by this review.

The interpretation's substantive scalar claims match the result: original nominal-risk breaches4458 versus zero in both daily-sizing arms;7/18 higher means for daily sizing and14/18 higher means and compound returns for either waiting arm; positive-return configurations7/18 versus10/18 with waiting; the four named long/short sign-flippers positive under both waiting arms across all four cost cases; and cross-asset momentum changing from positive to negative. In either waiting arm, all seven improved long-only indices remain negative. Their14 sleeves retain118 active,16671 waiting and571 other flat dates, sum17360, and every sleeve is gross and net negative. Four of the22 remaining sleeves avoid the halt; the explanation of exposure suppression is consistent with these counts.

Convention counts also match: one changed primary Sharpe positive/nonpositive label, one changed compound-return label, two changed direct Sharpe-difference labels and four changed compound-return-difference labels. These remain invalid-accounting diagnostics without a policy-selection claim.

The only identified report mismatch was the new interpretation's MA10/50 A00 Sharpe prose: the saved value is0.8585071974190749, which rounds to0.859 rather than0.858. The parent corrected this in the new `INTERPRETATION.md` and current-cycle findings suffix only; the rendered table and all empirical artifacts were already correct. The corrected prose was rechecked.

Qualifications are retained: spent development history, zero validated strategies, no winner/adoption decision, daily price proxies, assumed funding, threshold stop fills, separate-sleeve indices and possible indefinite cash from the waiting rule. A potentially useful historical mechanism is explicitly separated from a separately registered prospective validation requirement. No new data, model fit, financial replay, holdout access, deployment or paid settlement recovery occurred.

The new document-complete preservation receipt `preservation-final-docs.json` passed at15:28:08UTC with SHA-256 `88249c57e16b3ae30d872c0feeb124c92650562d0e945ebb81a1ebb128f99a6b`. It records the unchanged old files/gates/270 external inputs and exact820-row ledger. Findings and corrections retained their baseline prefixes, with4406 and1155 appended bytes respectively. That receipt preceded the same-length rounding correction within the new findings suffix; the subsequent Git-retention check records the final staged bytes and rechecks the original prefix. Earlier preservation receipts remain untouched.

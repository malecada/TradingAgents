# Operational integration after deferring missing settlement evidence

The September 10 user instruction ends the Binance support attempt and authorizes continuation without the missing historical records. The request text was not sent; the supplied contact address was entered in a visitor form before security verification. No support follow-up is scheduled. Missing historical settlement prices, fees and funding remain unknown; the 22 affected accounting cases remain unavailable. No proxy substitution, cohort trimming, gate change, strategy search or empirical replay is part of this stage.

Engineering baseline: `fb40355a31193639b6301361e293f608f5e769cb`. Current main was fetched and verified at `ed5e22f63d9e2d8ab4bf163103f40b0029eb0e7b`; it was merged into `fix/operational-integration-2026-09-10`, preserving both histories and the existing dirty monitoring worktree. A direct source-tree replacement would have removed newer monitoring functionality and was not used.

## Bounded compatibility repair

The v2 paper writer and legacy monitor/backup currently disagree on filenames and economic meaning. Update the monitor to prefer v2 without falling back to legacy when a v2 file is present but incomplete. Corrected book performance uses base net returns; corrected overlay performance uses the already-net overlay return. Legacy rows remain identifiable diagnostics, never relabeled as net. Missing or corrupt rows, duplicate dates, accounting-version changes and calendar gaps cannot silently become flat returns or a shortened complete series. The first initial-state row and genuine overlay warm-up must be distinguished from missing active measurements.

The API adds explicit measurement/version/reconciliation status while preserving the existing top-level routes. Frontend cards, charts and operations/gate views must show that status and tolerate unavailable numbers. Withdrawn development references and the derived 0.946 threshold must not be shown as active corrected gates; original registration files are not edited. The journal backup must retain the v2 files alongside existing legacy evidence, detect stale/missing v2 data, and report failed synchronization. Synthetic local Git remotes validate backup behavior; no real backup push is invoked during testing.

## Operational inspection and rollout boundary

Read-only SSH inspection may collect documented checkout hashes, service states, journal schema/date/completeness metadata and funding-store freshness. It must not read credentials, environment files, authentication tokens, full cron bodies or private account contents, and must not call an exchange account API. No service edits, remote source changes, orders, live/testnet runs or deployment occur. Preparation includes a concrete manual rollout/rollback sequence based on observed paths. Funding capture and a complete v2 measurement warm-up remain prerequisites to any later execution readiness claim.

The engineering verification consists of monitor/API/frontend tests, backup fixtures, the previously relevant accounting/lifecycle suite, source/package inspection and immutable financial-evidence hashes. New code is reviewed before commit and backup push of this integration branch. Original worktrees, stores, gates, results and financial ledger remain preserved. This work produces an integration candidate and operational findings, not a profitable-strategy or deployed-state claim.

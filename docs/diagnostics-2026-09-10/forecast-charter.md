# Saved forecast inference — registered diagnostic, September 10, 2026

The user authorized completion of saved-forecast inference and factor risk diagnostics. This charter governs only inference on the existing sixteen corrected Elastic Net (ENet) development forecast vectors. The separate factor charter governs trace diagnostics. The correction already completed on September 9 is not repeated: saved effective predictions, availability reconstruction and fallback decisions are retained. No fitting, new forecast, trading rule, monetary backtest, model selection, holdout access, network acquisition or paid data is part of this cycle.

## Inputs and complete scope

Key: `audit_saved_forecast_inference_2026_09_10`. Inputs are the sixteen corrected Parquets and their immutable result at `data/predlab/audit_correction_2026_09_09/`, sixteen original declared-baseline Parquets in the preserved predlab worktree, and the original P1/P2 gate objects. Exact hashes and cell order are committed in the new gate. Prior input receipts must agree with current hashes. Only timestamps through March 31, 2025 at 00:00 UTC may be materialized. BTC starts January 1, 2021; ETH starts December 1, 2021. Expected full clocks contain 37,201/1,551 BTC hourly/daily and 29,185/1,217 ETH hourly/daily origins.

The fixed grid is BTC/ETH × 1h/24h × return/direction/realized-variance/log-volume. Baselines are respectively rw_zero, base_rate, har_levels and seasonal_naive_m24/m7. No competitor is selected or newly tested. ENet was one of two original nonbaseline contenders for returns and three for the other targets; this diagnostic does not adjust away the entire earlier research search.

Both hourly variance files retain an unscoreable origin at October 28, 2024 20:00 UTC; both hourly volume files omit that same origin. These four files are reindexed onto their full physical clocks with an explicit unavailable mask. No target or prediction is invented. Any other missing timestamp, duplicate, target disagreement, inconsistent mask or unavailable required baseline makes the affected cell unavailable. Every one of the sixteen cells remains recorded. The existing scoreability mask governs inclusion; valid baseline-fallback observations remain included and have zero paired loss differential. The fallback policy is not reconstructed or changed.

## Frozen statistical policy

Define loss differential as baseline loss minus effective ENet loss. Return uses squared error; direction uses Brier loss on y_true>0; variance uses the existing normalized QLIKE; volume uses absolute error on the saved log-volume target. The positive training MASE scale is common to model and baseline and cancels from relative improvements and paired inference. Previously recorded MASE means are retained, not re-estimated from training data. Non-volume means and the volume relative-loss ratio must reconcile with the prior correction within relative 1e-9 / absolute 1e-12 tolerance.

The single dependence diagnostic is a circular stationary bootstrap over the full origin clock, jointly resampling masked model/base loss numerators and eligibility counts. Geometric mean block length is 21 calendar days (504 hourly or 21 daily origins), B=2,000, seed=20260910 independently reset for each cell. Paired quantities share exactly the same draws. Report observed mean differential, relative loss improvement and percentile 95% intervals. The one-sided null-centered differential p-value is (1 + exceedances)/(B + 1), with exceedance defined as bootstrap differential minus observed differential >= observed differential. Every draw must contain eligible observations and a valid positive baseline mean for relative inference; otherwise the relevant inference remains unavailable. No block-length or method selection follows results. Constant/degenerate paired differences do not establish a formal significant result.

Return, direction and variance comparisons have unresolved nesting/test eligibility: ENet restrictions overlap their baselines, and penalization, training-tail alpha selection, expanding estimation, clipping and fallback do not establish applicability of ordinary nested OLS tests. Their bootstrap outputs are conditional uncertainty diagnostics only; formal primary p-values are null and family-adjustment inputs are 1. Volume versus the fixed seasonal-naive forecast is eligible for this conditional nonnested diagnostic. Holm adjustment includes exactly sixteen slots, with every unavailable/ineligible comparison entering as 1. Report raw diagnostic and eligible family-adjusted p-values separately. No DM/GW/Clark-West substitution, original-gate reversal, model-class significance or fresh validation claim follows.

Fixed stability periods are 2021–2022, 2023–2024 and 2025Q1. Report each paired count and mean-differential sign; the historical stability descriptor requires positive differential in at least two periods and at least thirty scoreable origins in every period. Report the original effect descriptors: return OOS-R² 0.002 hourly/0.005 daily; variance relative improvement 2%; volume relative improvement 5%; direction accuracy edge versus base-rate prediction and point AUC. The required direction AUC interval is not supplied, so its original conjunctive floor is not declared passed. These descriptors do not change original criteria or promote a model.

## Execution and preservation

Commit this charter, gate and exact input hashes before implementation and empirical outcomes. Test full-clock missing masks, fallback equality, paired resampling, seed determinism, zero denominators, sign/scale behavior, nesting exclusion, sixteen-slot Holm, alignment rejection and immutable output guards with synthetic inputs first. Reviewed executable source must be committed before the one empirical run. Reuse registry preflight, exclusively create `data/diagnostics/2026-09-10/forecast/`, preserve start failures, record source/runtime/input hashes, and recheck inputs on completion. No runner may refit, fetch, regenerate availability, invoke old experiment mains or overwrite an existing output.

Write sixteen cell records to a separate forensic ledger in the new output directory; the existing 748-row financial ledger remains byte-identical. Preserve every prior gate object, report, result and cache. Results, denominator checks, statistical qualifications and independent reviews are retained in new artifacts; completion may append the correction register and findings. The P1/P2/P5 holdouts remain spent. The twenty-two settlement-blocked strategy configurations remain deferred.

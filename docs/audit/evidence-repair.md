# Evidence and inference corrections — September 9, 2026

The original gates are unchanged; only `audit_correction_2026_09_09` was added. Its accounting clarification was committed before any correction outcome. The 11 originally uncommitted final ledger rows were copied verbatim into the integrated branch after checking that its 686 committed rows were an exact prefix of the 697-row source snapshot. This preserves history without pretending those rows were committed at their original evaluation time.

`evidence.py` provides append-only corrections with explicit supersession; `registry.resolved_evidence` exposes the original registration and its current qualification together. Original PASS/FAIL rows remain readable. Historical results and true vintage data cannot be reconstructed merely by changing software. The correction register therefore marks affected claims, stale holdout flags, the prior OFLOW sign correction, and registration-order exceptions. Every historical gate criterion remains intact.

`registry.preflight` pins the full code commit and gate hash and rejects uncommitted executable inputs, uncommitted gates, invalid development windows, and known audited experiments requiring new registrations. The generic runner calls it before fitting. Ledger writers use one SHA256 identity over experiment, cell, model, configuration and window. RVIV and XFAM no longer emit incompatible config-only SHA1 identities. Existing identity counts are normalized on read without rewriting legacy rows. Legacy scripts remain historical research artifacts; they must not be used to reopen closed cycles. The new bounded correction command is the authorized empirical entry point for this repair.

Prospective statistics use Bartlett HAC with lag `max(h−1, floor(4(T/100)^(2/9)))` and asymptotic normal tails. An explicit legacy DM option preserves the old HLN arithmetic for reference; its use is not permission to change archived criteria. Exact independent comparisons are in the tests. Champion p-values include Bonferroni selection over every contender, including unscoreable ones. Missing family tests count as non-rejections instead of shrinking the denominator. BH remains available for its original assumptions; `by_fdr` is available for prospectively registered arbitrary-dependence control. These changes are not retrospective rejections of previously registered hypotheses.

PRX formation now uses the Engle–Granger null for estimated residuals. OFLOW's reversal-admissible gate checks the absolute t-statistic. A fixed future-volume weighting experiment no longer claims to bound every feasible weighting rule. SMW standard-error reporting uses the HAC decision statistic and retains the iid quantity only as a labelled diagnostic.

Method references: [statsmodels HAC](https://www.statsmodels.org/v0.14.3/generated/statsmodels.stats.sandwich_covariance.cov_hac.html), [Engle–Granger](https://www.statsmodels.org/stable/generated/statsmodels.tsa.stattools.coint.html), [multiple testing](https://www.statsmodels.org/stable/_modules/statsmodels/stats/multitest.html).

Verification: eight original and two additional counterexamples were recorded failing before correction. The focused evidence/statistics/registry suite passed 28 tests. Bounded-run reader/availability tests use synthetic data, including a Parquet row-group date filter, a real zero forecast and an unavailable fit held until refit. No historical numerical result was used to set these software expectations.

## Independent-review corrections

The execution/code review identified and repaired additional authorization and clock gaps before any empirical correction ran. The correction-policy JSONL participates in dirty-input rejection and its hash is recorded. S2/S3 require their exact original 1,551-day / 37,201-hour clocks and finite observations; the saved ENet reconstruction reproduces the original inclusive March31 midnight endpoint. A missing source produces explicit unavailable records for every dependent forecast cell. Return archives use the tested serialization boundary and retain the accounting version.

The actual HARQ battery calls now declare their nesting relationship to the HAR-levels baseline. A Clark–West primary test requires registered squared-error comparison. Nested QLIKE has no valid matching-loss test supplied by this harness and is explicitly ineligible for a significance-based promotion; DM/GW/CW numbers remain diagnostics. Roll-up preserves the raw primary value, raw DM value and selection-adjusted value separately. An unavailable inference is not labelled evidence of no skill.

Bybit data-only fetching also received a synthetic-tested repair: existing files must cover the requested end before being skipped; prior symbol metadata survives limited/resumed enumeration; new data merge with existing history; old Parquet/manifest versions are preserved by hash. Actual historical stores were not refreshed. Current instrument funding intervals are disclosed cache checks, not historical schedule vintages. OI per-column last-observation sampling remains causal but asynchronous, with incomplete field coverage; this is a qualified sampling contract rather than a proved look-ahead defect.

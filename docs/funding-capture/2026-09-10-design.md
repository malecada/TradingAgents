# Current public funding capture and paper admission

The user authorized continuing the operational funding-data priority after the September 10 integration. Baseline: `78c89363a402edad752732396a47770b3d13fb1b`; branch `fix/prospective-funding-capture-2026-09-10`. This is data infrastructure and synthetic engineering verification, not a strategy experiment. No research gate, financial result, trial ledger, sealed window or original raw store is changed. Historical BZRX/LUNA/BNX support remains abandoned and their settlement inputs unavailable.

## Scope and sources

Add a public-only collector using explicit bounded requests to Binance USD-M `/fapi/v1/time`, `/fapi/v1/exchangeInfo`, `/fapi/v1/fundingInfo`, `/fapi/v1/premiumIndex` and per-symbol `/fapi/v1/fundingRate`. No account client, credentials, order endpoint or provider message is used. The initial operational capture covers the most recent nine UTC calendar days through a fixed exchange-server-time cutoff. Symbols are selected from current USDT-margined USDT-quoted perpetual metadata, with explicit requested symbols retained in the denominator even when unavailable. Current eligibility does not reconstruct historical universe membership. Original contract identifiers/onboard times remain recorded; no successor mapping is performed.

Each run uses a new directory, preserves every response body and request outcome with UTC retrieval times and hashes, then writes normalized per-symbol Parquet plus a final manifest. Exact duplicate events may be deduplicated; conflicting values or ambiguous event types cannot be silently resolved. Raw data and previous snapshots are never rewritten. Pagination is explicitly bounded, ascending and checked for lack of progress. HTTP failures, rate limits, exhausted retry budgets, malformed or incomplete responses are recorded and cannot become a successful empty history. Conservative request pacing stays below the documented shared funding endpoint quota; a rate-limit response stops collection rather than circumventing it.

The manifest distinguishes query exhaustion from economic completeness. Funding history is published event evidence, not actual account debits. Current interval metadata and announced next funding times are captured with availability timestamps but are not backprojected as historical schedules. The consumer initially retains its disclosed regular-cadence inference, including rejection of unsupported or ambiguous transitions. Unknown faster events remain a limitation. No inferred value or estimated lastFundingRate is substituted for a realized history row.

## Consumer boundary

An explicit optional funding-snapshot input selects the immutable snapshot; configured missing or invalid snapshots never fall back to legacy storage. Verify normalized file hashes, request success/coverage and source metadata. Preserve the snapshot identity/hash and acquisition time in journal coverage. The existing UTC daily sum and funding approximation are unchanged: sum [D00,D+1 00), require the following boundary for the existing cadence check, positive rates paid by longs. This is not event-marked exchange cashflow accounting.

With the operational snapshot configured, preflight the relevant settlement day and all previously held names, plus complete closing prices/current quotes, before writing either paper journal. The actual intended target and its net settlement use the existing writer; no strategy rule is changed. A delayed capture produces WAIT without a financial row, allowing same-day retry. Existing broken or gapped journals are not reset or repaired by truncation. Initial flat state and genuine overlay warmup remain valid. No real paper journal is started in this stage; synthetic fixtures exercise the path.

## Acceptance and stop condition

Synthetic tests must demonstrate bounded public pagination, preserved failures/raw hashes, conflict quarantine, immutable retry outputs, snapshot tamper/missing-file rejection, no fallback, delayed-data retry without a poisoned journal, and unchanged accounting/legacy behavior when no snapshot is configured. A bounded real public capture validates the path, with full symbol/request denominators and limitations. No financial replay, strategy score, account operation, deployment or service modification is performed. Package the reviewed source and manual scheduling instructions; runtime scheduling and a future paper start require the stated operational and research prerequisites.

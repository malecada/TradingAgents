# Fixed historical-input and precision readiness

Experiment allocation-input-readiness-20260915; admission6/6, parent terminal
allocation-spot-engine-20260915. Before opening prices, refine the existing
precision/selection question into fixed-input integrity, causal valuation/clock
compatibility and whether the required estimator/power evidence exists. This
uses the final admission slot, not an extra source or financial trial. Retain
family import179/cap189; after this fifth actual claim effective184/189 including
the failed unclaimed custody launch. Four recipe claims and one confirmation
remain. Acquisition usage is22APIrequests before this run. Two new official-document
open operations are conservatively charged separately to the public-resource
budget; four actual API requests would make26APIrequests and28charged operations
of100. Earlier design orientation remains its separately recorded design work.
No return, empirical variance, fitted model, strategy/benchmark profit,
performance ranking or power estimate is calculated in this admission.

## Exact inputs and requests

One fixed cohort: [2025-09-01T00:00:00Z,2026-09-01T00:00:00Z), 365 days.
200-day warmup starts2025-02-13T00:00:00Z. Request566daily open timestamps through
the terminal2026-09-01open. No alternate window, pair, shortened warmup, provider,
retry, extra capital or fallback after failure. Exact four GET URLs and order
are in readiness-spec.json: Binance BTCUSDC daily bars; ETHUSDC daily bars;
Kraken USDC/USD identity metadata; Kraken USDC/USD daily bars. The exact expected Kraken internal pair key is USDCUSD, baseUSDC,
quoteZUSD, altnameUSDCUSD and wsnameUSDC/USD. Binance query identities are fixed
and inherit public pair metadata from the retained spot-source audit.

Kraken prices are cross-venue USD valuation proxies, not account redemption,
transfer or Binance execution evidence. Only the single final bar opening on the receipt UTC day may be an incomplete
current bar; it is retained raw and excluded from usable rows. Any future-day
open is invalid and makes the source cell unavailable. All returned bytes are
exposed; no response remainder may later be called fresh confirmation. Actual
request/receipt times and response coverage record this exposure, including
post-cohort fields. The terminal day's open may be used; its later OHLC fields
are never normalized for financial use. Existing old inspected samples and
concurrent options exposure remain spent.

## Fixed source checks

Parse strict JSON: no duplicate keys/NaN/nonfinite values. Exact source shape,
integer daily UTC opens, strict ordering/uniqueness, positive finite decimal
OHLC and low<=open,close<=high; nonnegative volumes and integer trade counts.
Binance12field kline rows require close_time=open+86400000-1, integral timestamp
and exact query bound. Kraken8field rows require metadata identity first; error
array must be empty, result keys exactlyUSDCUSD/last, integer last marker. Reject
wrong identities, malformed rows or duplicate timestamps. Require every one of
the566fixed opens with positive reported volume/trades (missing/zero activity
makes the panel unavailable; not a zero-cost fill). No forward fill/interpolate.
No price-level/outcome threshold or outlier deletion is introduced after seeing
prices. Historical publication and actual executions remain unverified.

Raw prefixes survive transport errors; at most256KiB per response,10second hard
request alarm, four attempts, no redirect/retry/proxy-variable/auth.403/418/429/
451 stops that host, preserving remaining unattempted cells. Kraken bars require
valid pair metadata. Different host's independent request may proceed. Capture
cooperative70seconds; existing v2guard120seconds/512MiB/twoCPU. Total JSON outputs
at most2MiB. Registration includes exact transport/source/dependency hashes.

## Output and remaining inference requirements

Seven cells: four source requests, joined-panel coverage, declared-estimator
requirements, and confirmation-power evidence. Six outputs: four raw receipts,
panel.json and readiness.json. Coverage contains exact dates, observed row counts,
missing dates and reasons; every unavailable cell remains. The panel retains
separate normalized source series and a joined panel only on complete coverage.
Its columns are daily open/close in native quote units; terminal row has open
only. This is a historical bar/proxy dataset, never executable fill admission.

Freeze downstream clock convention before prices: monthly decision first-day
00:05UTC using closes through that day's00:00. Conditional execution at the
second-day00:00 open (86100second delay), shared by strategies and controls;
terminal liquidation at2026-09-01T00:00, never September2. Initial, daily-open,
action and terminal NAV use same-timestamp Kraken USDC/USD open proxy. Daily
close marks and their next-midnight availability remain separate. No current
USD mark is projected backward. A daily open is the first reported trade of
that interval, whose exact execution time is unobserved. Synchronous00:00 marks
and fills are explicitly hypothetical bar conventions; they are not witnessed
quotes or proof that both venues supplied executable prices at that instant.
The source check does not run that book.

The downstream estimator must report net cash P and benchmark differences D
on all committed capital; one fixed annual cohort is one annual observation,
not365annual replications. Existing88contrast alpha allocation remains. No
empirical variability is computed here; any such computation requires a
registered development measurement. A coherent joint model for full-gate80%
power at the frozen20%absolute/4%incremental planning alternative is absent.
Record confirmation-power evidence as unavailable. Synthetic mechanics or a
marginal Normal illustration cannot certify that joint gate.

The separately reviewed conditional route may use10bp commission+5bp spread+
5bp impact per side, zero USDC accrual and an authored$10terminal route charge;
these are proposed scenario inputs, not verified costs or an upper bound.
A separate financial contract must freeze them, fee currencies, lot rules,
causal policy details, full grid, stress and diagnostic outputs before outcomes.
B1actual and implementation/promotion remain unavailable until their evidence
exists. No financial result or permission to revise the gate follows from this
source readiness. A failed source panel must not trigger four empty financial
claims merely to consume unused slots.

Independent review must reconstruct raw bytes/identity/calendar and verify
all7cells, hashes, absence of return calculations and cumulative history.
If complete, use this exact source panel for the four registered conditional
recipes; otherwise record the specific input blocker and perform finite coverage
review. No new DeFi financial panel or options allowance is created.

Primary documentary orientation reviewed before registration:
- https://docs.kraken.com/api-reference/market-data/get-ohlc-data
  documents720recent entries including a current partial bar, and daily interval.
- https://developers.binance.com/en/docs/catalog/core-trading-spot-trading/api/rest-api/market
  documents the fixed public kline schema. Documentation is not market capture.

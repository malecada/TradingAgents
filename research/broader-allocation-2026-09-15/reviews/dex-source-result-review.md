# Independent DEX raw-evidence closure review

PASS with two explicit source limitations. Independent reconstruction from
base64 receipts confirms18actual RPC requests,18cells,19hashed outputs,
16complete and2unavailable. All HTTP200; the two unavailable results are explicit
JSON-RPC historical-state errors. Request IDs, chain identities, ABI calldata/
addresses, canonical-hash dependencies, chronology and summary fields reconcile.
All raw/terminal hashes match;129,209raw response bytes are retained.

Ethereum's requested old factory state is unavailable; Arbitrum reports missing
trie state; Base returns24,535code bytes matching its reported SHA256. These are
exact-provider/request findings, not comprehensive archive, liquidity, execution
or chain-ranking results. The result keeps implementation_admitted=false; no
profitability or benchmark value has been measured.

Guard exited0 with no limit reason after26.47seconds at approximately66MiB peak
sampled aggregate RSS. One actual allocation lifecycle claim plus179imported
administrative attempts gives180/189; admission2/6. The failed custody launch
remains charged without a fabricated claim. No closure blocker found. The
separately reviewed spot-source child may proceed through committed-source
admission. This review made no additional market requests or reruns.

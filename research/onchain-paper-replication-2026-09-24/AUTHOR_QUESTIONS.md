# Prepared questions — not sent

1. Can the exact code revision/environment and complete training configurations be
   supplied, including dictionary K/sample count, neighborhoods, s1/s2/alpha and
   matching schedule? Which Algorithm1/prose convention was executed?
2. Which APIs/raw dataset versions and event filters were used for each chain?
   What attributes/units, directedness, aggregation and BTC input/output mapping?
3. How were node embeddings pooled into a graph vector? Were MLP/GAT/LSTM trained
   jointly, and how were graph embeddings aligned across snapshots?
4. Which price field, daily timestamp/horizon, weekly anchor and availability join?
   Exact fold membership resolving2016–2024 tests after two years of training?
5. Which baseline implementations/settings, especially HLSTM and WatchYourStep?
6. Were precision/recall/F1 binary, macro, weighted or annual averages? Why do
   Table1 MAE/RMSE inequalities and Table2 implied class prevalences differ?
7. What is the exact65-entity fund address list, snapshot date and inclusion rule?
   What dates were used for Tables5–6 and their changed price-only baselines?
8. How was weekly whale volume defined, and what domain determined its maximum?
9. Does52GB refer to host RAM, and what was device memory/peak workload/runtime?

Sending these questions requires separate user authorization. Source silence is
handled by declared assumptions where possible; absent historical cohort identities
remain an external blocker, not an invented dataset.

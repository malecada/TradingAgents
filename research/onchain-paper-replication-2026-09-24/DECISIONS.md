# Decision register

Every configuration is an independent assumption unless the source audit explicitly
supports its mathematical form. Numeric source silence is not a verified match.

- D01: Rendered Algorithm1 controls schedule and single normalization pass; symbol
  names follow argument types. Its half coefficient is retained, not called the
  Eq2 derivative. Cost if wrong: alternative solver results differ.
- D02: Directed native successful positive transfers, ordered-pair aggregation,
  incident count/value attributes, log1p native units; ETH float precision retained.
  Cost: source graph differs from an unavailable original graph.
- D03: Empty-edge term0, rectangular literal normalization, hard injectivity,
  row-major ties, no hidden slack. Cost: unmatched/isolated behavior is reconstructed.
- D04: Weak1-hop,512 samples,32 medoids, average linkage, declared partitioning and
  overlap halves. Cost: dictionary differs from unknown original settings.
- D05: Mean graph pooling and joint downstream gradients; widths/optimizer/epochs
  frozen in JSON. Cost: unreported original pooling/training might differ.
- D06: Monday UTC, previous available complete week with one-day lag, Yahoo Close,
  next-day target,2018–2024 folds and explicit boundary exclusions. Cost: numerical
  comparison cannot establish exact reproduction; retrospective vintage remains.
- D07: HLSTM4×7 temporal hierarchy from Hou's two-level feature hierarchy;
  comparator alignment/settings explicit. Cost: adapted baseline, not recovered code.
- D08: Binary-up annual means primary, pooled/macro alternatives separate. Reported
  inconsistent metrics remain untouched. Cost: unknown original aggregation differs.
- D09: Whale weekly incoming+outgoing value, strict90%max; fund induced filter
  provisional until actual cohort admission. Cost: treatment inclusion differs.
- D10: CPU implementation first, tensor accelerator later; no available CUDA and
  no implicit remote compute. Cost: C06 GPU and full-scale feasibility remain pending.
- D11:34-stage claim proposal/cumulative51 keeps all17 historical claims;1,420fits
  maximum and no second lane/retry allowance. Exact releases must independently
  bind sources/budget before execution. Cost: blocked attempts require amendment.

Source review: a separate read-only reviewer confirmed D01/D03 mathematical
ambiguities, original source/cohort omissions, true two-level HLSTM, and table
inconsistencies. It did not certify code, empirical admission or outcomes.

# Retained ETH backup continuation 02

The first attempt remains terminal FAILED and immutable. Its guard stopped
when host available memory fell below the3GiB reserve during concurrent synthetic
testing. Cleanup was independently verified. Its149-file local tar and manifest
remain; the504,233,984-byte remote partial is recorded by size and remote SHA256,
without claiming successful retrieval. No original transaction file was removed.

This distinct continuation preserves the same59,083-file inventory. It uses
256MiB maximum/192MiB high memory, zero swap,3GiB reserve and3.25GiB startup
headroom. No concurrent root synthetic test is launched during transfer. The
first517MB bundle is again a finite byte-preservation pilot under a new remote
identity; this is not an empirical rerun or relaunch of the failed identity.

A successful independently reviewed pilot must produce an exact BULK_RELEASE.json
before the single all-bulk controller can start. The remaining194 bundles are
partitioned into13 disjoint phases of at most16bundles each. Each phase is guarded
for at most8hours, with a48hour cumulative controller ceiling and245GiB total
reserved payload budget including the pilot. The controller stops on its first
failure. All reservations are exclusive; never rerun a completed/failed controller
or phase. Any successor must reconcile completed batch receipts and partial files.

Downloads are streamed through bounded SSH dd and compared to expected archive
and member hashes before completion. Only verified generated temporary tar copies
are released. The frozen inventory excludes graph/MCM arrays, pilot SQLite scratch
and other historical stores; completion cannot claim those separately scoped bytes.
Every original, failed attempt and inspected research sample remains preserved.

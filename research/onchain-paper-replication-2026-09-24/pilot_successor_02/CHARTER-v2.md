# Registered whole-graph resource successor02 (release review required)

Objective: measure the frozen transaction-graph replication pipeline with retained
ETH sources and synthetic training labels. No predictive outcome, tuning, price
capture, old-run replay or original source mutation is permitted. The previous17
claims remain consumed. This new engineering attempt remains one of the proposed
34 additive allowances; the separate metadata claim has since completed (cumulative ceiling51); any interrupted retry needs a new
reviewed allowance/identity. This pilot is not a financial strategy test.

The eight fixed weeks and largest complete training week by declared transaction
count are bound by source-index.json. Exact source bodies are verified during the
new transform, not refetched. January1,2024 uses its explicitly mapped compressed
context ranges and original base64 footer receipt. Metadata mapping-v1's failed
special-plan comparison remains preserved; v2 is active. Source vintage and
canonical-chain completeness remain qualified by prior evidence; this pilot does
not repeat the closed global transaction-identity job.

Resource limits: protocol-freeze-v4,3GiB memory/2.5GiB high/zero swap, the same two
CPUs for all threads,3GiB additional host reserve,20GiB disk floor, at most8hours
whole job and1hour per safe phase. No GPU is available. Each graph is durably
checkpointed. Long partial component work is checkpointed at most600seconds apart
where operations permit. Timeout/capacity outcomes retain attempted input identity,
last progress and exact unmet scope; dependent phases remain unavailable, while
independent graph/resource phases continue. No neighborhood, dictionary or graph
scope is reduced to manufacture a successful full-size result.

Nine graph-build/decode phases use every transaction in their declared weeks.
Neighborhood sampling uses512 centers/seed11. A resource-only32-motif dictionary
is fitted from the first training week (2022-01-03), solely for component timing;
it is never an admitted prediction dictionary. Matching uses the first32 sampled
neighborhoods paired with the next32 as a fixed timing batch; this is a component
probe, not a replacement for full MCM. Full MCM attempts every node against all32
motifs and retains any incomplete prefix distinctly. GAT/full-model resource
probes use the actual complete weekly topology and synthetic MCM inputs, prices
and labels; their results establish tensor cost only, not matching coverage.
Synthetic training uses one batch of16x28 daily steps referring to the same weekly
graph, one optimizer step, seed11. A real training batch spans more distinct weeks;
capacity forecasts explicitly multiply the per-graph incremental peak by the
observed/calendar-required unique-week bound and apply1.5margin. That estimate is
not a demonstrated multiweek batch. Checkpoint I/O writes the actual probe model
and optimizer. All source, decode/build, neighborhood, matching, dictionary, MCM,
neural and checkpoint cells—including unavailable dependencies—remain in ledger.

Outputs: source/phase cell ledger, immutable artifact index, measured time/memory
receipts and capacity forecast. Original raw plus new graphs/features/checkpoints,
temporary peak, per-fold/seed repetition and backup requirements are accounted for.
At insufficient capacity, remaining financial fits stay pending and independently
executable software/source work continues. Any external paid capacity or provider
contact requires separate authorization. This charter alone cannot admit a run.

Implementation-specific resource qualifications: dictionary distances currently use
the independently verified scalar reference matcher in both directions. Its
timeout is evidence about this implementation, not a lower bound for an optimized
implementation of the paper. A conservative preallocation estimate rejects neural
intermediates above1GiB, leaving2GiB for graph/model/runtime under the3GiB aggregate
cap. This is an explicit safety screen, not a measured out-of-memory observation.
Phase order is each week's graph, neighborhoods, matching, neural probe, then the
first week's dictionary and each week's MCM. The total-job deadline reserves120s
for durable closure; unstarted cells retain the total-budget reason.

Prospective cap amendment: host MemAvailable remains approximately6.2GiB, below
the original9GiB startup condition. The effective kernel cap is reduced to3GiB
and high threshold2.5GiB;3GiB additional reserve and cap+reserve startup6GiB are
unchanged in meaning. This is not the rejected4GiB startup under a6GiB cap. All
109cells, whole graphs, source membership and algorithm dimensions remain fixed.
The1GiB preallocation ceiling leaves2GiB for graph/model/runtime. A blocked
component remains unavailable at this cap; it does not establish failure at6GiB
or intrinsically prevent a separately admitted optimized/higher-capacity run.
The first pilot had not been launched when cap-v4 was reviewed; its subsequent
failed terminal is preserved and bound by this successor registration.
Cap-v4 alone added no attempt, fit, price source change or financial release.
The separately registered correction below adds one attempt within the unchanged
cumulative ceiling through a prospective source-batch allocation amendment.

Successor02 correction: parent eth-paper-resource-pilot-20260924 terminated
failed with9decode-phase failures and100unavailable dependent/source cells. The
serialized input-binding paths were strings passed to a Path-only hash helper;
all9failed at this check before transaction-body decoding. Source e446e1f948a8e0116bc08c212e5c0bed29ae72f1
and all failure/guard/observer evidence are preserved. This new experiment and
pilot-02 artifact/owner/guard identities never relaunch the terminal parent.
The sole behavior correction converts each serialized binding path to Path
before hashing. Unchanged sources,9wholeweeks/109cells, bounds and synthetic
labels are retained. There is no tuning or financial fitting.

The prospective cumulative allocation amendment keeps the51claim ceiling and
1420fit denominator. It allocates two pilot claims instead of one by grouping
the still-unclaimed BTC2016–2017body capture into one two-year source claim
(same dates/fields and no coverage omitted). Missing-body claims become14;
source claims become17including completed metadata and two price batches.
No paid capacity, author contact, trading or deployment is admitted.

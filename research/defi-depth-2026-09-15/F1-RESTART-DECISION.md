# F1 manual zero-output crash continuation

The user reported a PC restart and explicitly requested continuation. The original
F1 process was absent. Independent inspection found only the original claim and
an entirely empty outputs directory: no result, pending temporary file, intent,
receipt or terminal. The unchanged frozen main requires history.json before
capture, and the source writer durably publishes an intent before every send.
Under that intact-root/source assumption, no F1 network request or financial
calculation occurred. The original claim and its exposure/budget remain spent.

This is a prospective operational recovery decision after an external crash,
not a retroactive change to F1's economic, source, sample, metric or cost rules.
No new scientific trial or shared measurement-repair allowance is created. The
original family remains prior6 plus its one F1claim, effective7/cap7. The phase
remains8/11claims, financial2/4 and sharedrepair1/1spent. No source is remeasured.

## Exact permitted attachment

Only the F1claim started2026-09-16T09:36:19.909003UTC, SHA256
2ad4dc79f812cafa80486b64a28cea1a45170c99380bb3eff9aaac187a1c6730,
at original sourceb2a1659e2645c6fa6cba03bba6b73d0c74b30bb8 is eligible.
Its root, source, registry, claim time/hash, inputs and empty-state requirement
are fixed in f1-restart-recovery.json. The original root stays at that HEAD.
Original source files, gate, claim bytes/time and output denominator never change.
Ordinary ResearchRun.start is not called again for a new claim.

The separate committed adapter validates its own frozen source/decision, checks
for an existing original process and acquires a session-long exclusive recovery
lock shared by all adapter checkout locations targeting the original root.
The original run lock protects the atomic empty-state/admission recheck. There
must be no terminal, output, pending file or extra member in the claim directory.
Revalidate the original committed claim and its own-claim admission, all original
input hashes and all original source/runtime/contract checks before attachment.

Restore only the original ResearchRun ownership bookkeeping for that existing
claim and an empty published-output map. The unchanged original main receives
an exact-once start factory returning that object. Its original read_input,
write_json, finish, failure handling and per-operation source/input checks remain.
The adapter adds its own source-integrity check; it does not replace or cache the
original checks. The original financial/source function calls remain unchanged.
No prior financial output or partial source attempt is replayed by this adapter.

## Additional provenance and failure handling

The adapter executes from a separately fixed detached checkout, whose HEAD/source
also remain fixed through completion. Immutable started/completed/failed recovery
receipts live under research_recoveries/defi-depth-f1-restart-20260916 there. They
link the original claim/source and the additional adapter source/config. They
are operational evidence, outside the unchanged scientific output denominator.
The hidden shared lock and an exclusive, durable attachment marker in the
original research_runs root are operational metadata. The marker is created
under the shared lock before attachment and survives a change of adapter checkout
or power loss. It blocks a second automatic attachment even when the original
outputs remain empty. External full recovery receipts remain separately retained.

The original source commit alone is therefore incomplete execution provenance.
Every eventual F1 report/closure and F3 source handoff must retain both the
original scientific source and this adapter's recovery source/receipts. No claim
that the entire execution used only the old source is permitted.

The one recovery event cannot be overwritten or automatically repeated. If a
future interruption occurs, inspect its actual state; any nonempty original
output state is ineligible for this narrow adapter. Do not infer failure from
time taken, erase an incomplete claim, invent a fresh trial identity or apply a
new retry allowance. There is no experiment duration or CPU-time kill.
Independent exact adapter review and synthetic rejection/completion tests must
pass before this attachment. No orders, paid resources, account/wallet operation,
provider/VPN change, production change or implicit recurring job is authorized.

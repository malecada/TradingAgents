# Successor research authorization and preservation

September 15, 2026. The user explicitly requested continuing the remaining
strategies, deeper investigation, and as much time as a justified experiment
needs so it is not killed untested. This supersedes the prior initial-phase
stopping point and prospective aggregate/per-run elapsed-time ceilings for this
successor work. It does not alter a completed result, relax economic/risk gates,
or authorize paid resources, account actions, orders or production changes.

The initial allocation phase remains closed at 188/189 cumulative administrative
attempts, six admission slots and four financial recipes consumed, with its
original confirmation slot unused and inadmissible. Its RESULTS.md, all gates,
raw inputs, failed attempts and 81 protected anchors remain historical evidence.
The old shared options study and operational runtime have separate ownership.

A finite count of distinct questions and financial variants remains required.
The user's willingness to wait removes elapsed-time truncation as an economic
stopping rule; it does not license outcome-driven parameter searches, duplicate
failed tests, unlimited API retries, provider-denial circumvention or uncounted
sample inspection. Exact numerical slot/request/data allowances and ancestry
are subject to independent review before empirical execution.

New acquisition and calculations run without an elapsed-time kill. Use finite
registered cell/request inventories, per-request network timeouts and immediate
immutable receipt publication. Finite network failure is a source limitation,
not economic failure. Processing should stream/chunk inputs and publish useful
progress; a genuine resource/measurement failure retains partial evidence and
needs explicit reviewed recovery. Never modify an old worker/guard or silently
rerun a previously completed book under the new runtime preference.

Existing interpretation stays fixed: $10,000 baseline, larger capital only for
a demonstrated constraint, 10% annual net profit target, 30% maximum drawdown,
50% market/stablecoin stress; catastrophic custody/contract/token failures
separately at affected-position total loss, with actual losses always in NAV.
USD, Czech Binance and wallet/DEX alternatives are accepted. 365-day horizon,
pre-personal-tax treatment and 2%-of-capital incremental hurdle remain disclosed
research assumptions. Missing actual fees, cash terms and access stay unknown.

Next: independently review a narrow cumulative registration route and a finite
map for the deferred protocol mechanisms, then commit the first source charter,
full request denominator, input/source hashes and exact gate before capture.

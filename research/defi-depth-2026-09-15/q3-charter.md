# Q3: staking conversion, redemption and wrapped-token source observability

This is one authorized source question in PHASE.md, under the accepted $10,000
baseline, 10% annual net objective, 30% drawdown and 50% market/stablecoin stress
limit. Catastrophic affected-position losses remain separately assessed and
included in actual NAV. The 365-day horizon, pre-personal-tax accounting and
2%-capital incremental hurdle are explicit research assumptions. This source
experiment computes no financial outcomes and grants no implementation status.

## Mechanism, inheritance and finite question

Staking may add ETH-denominated quantity. ETH price changes, wrapper discounts,
slashing, delays, gas and exit costs can dominate that addition. Native wrapper
conversion, a bridged wrapper oracle and redemption queue aggregates are distinct
objects. The fixed question is whether their source identities and coverage can
be established through the reviewed public routes. No observed price level or
return selects an instrument, window, cohort or route.

One conservative family imports six direct predecessors: allocation-dex-source-
20260915, defi-depth-q1-20260915, failed defi-depth-q2-20260915, failed
defi-depth-r1-20260915,
wbeth-inputs-20260911 and wbeth-book-20260911. Their exact claims and terminals
are input-bound. Family prior6/cap7 permits this source claim only. Parent-null
reflects the ordinary helper's cross-mechanism restriction, not fresh history.
Original188/189 allocation closure, 44 related dependent DeFi records, spent
windows and full ancestry remain; imports overlap them and are not new trials.
The narrow protocol family and shared R1 repair remain exhausted. This question
neither recaptures Aave/LP state nor fills the failed R1 output.

## Exact observations and chronology

q3-design-v2.json freezes all contract addresses, selectors, types, calendar,
source policy and bounds. Ethereum: one chain ID and15 native wstETH/aggregate
queue calls at Q1's exact retained qualified finalized block. No new latest
anchor or retry of Ethereum's unavailable historical state. The Q1 raw receipt,
request, parsed result and historical calibration must agree before transport.

Base: chain ID; two daily wstETH oracle fields on the unchanged1096-day calendar,
2023-09-02 through2026-09-01, encompassing three fixed365-day cohorts. R1's88
header-pair receipts are reused, including the failed October22 pair and raw
November28 pair without a parsed R1 vector. Exact176 attempted method/parameter
keys exclude all prior headers, including failures, from new acquisition. Only
1008 previously unattempted dates after November28 permit2016 new header calls.
Adjacent left/right headers must have matching ancestry,2-second spacing and
bracket00:00UTC. State calls use the canonical left block hash. No height search,
replacement day, value fill or retry is permitted.

All four boundary dates are checked first. Wrapper decimals18/nonempty code,
positive oracle price, nonzero oracle source/nonempty code, USD zero base and
unit1e8 establish source observability only. The first boundary's unit/base
observations are imported from exact R1 raw receipts, even if unavailable. The
other three boundaries permit exactly six new unit/base calls. A dependent source
code call uses only the same-block returned address, without fallback.

Interior acquisition requires both frozen boundary checks for its fixed cohort.
Positive price magnitudes never affect eligibility. Every day/field is retained
exactly once, including shared boundaries and skipped/unavailable interiors.
Four code snapshots and daily source-address observations do not establish
uninterrupted proxy/oracle semantics. Native historical conversion, investor
queue wait/claim cash, executable wrapper prices, full gas/bridge/funding/exit
terms remain unavailable; these are explicit cells, not assumed zero costs.

## Transport, preservation and accounting of attempts

At most4243 new single RPC/HTTP requests,10-second per-request deadline,
256KiB response cap and1,112,276,992 worst-case raw bytes. Five seconds elapse
after the preceding response on the same endpoint before another request. This
is authored pacing, not a claim of a safe provider rate. No aggregate elapsed
or CPU-time cutoff. No JSON batches, retry, credential/account access, redirect,
provider/VPN change, paid resource, order or production mutation.

First HTTP403/418/429/451 or recognized RPC throttling stops that endpoint,
including throttling returned under HTTP200. Exact codes/message fragments are
frozen in the design. Other ordinary missing/revert responses remain unavailable.
Retain every intent before transport and exact raw receipt immediately afterward;
suppressed attempts retain their reason. Interrupted partial claims remain spent;
no automatic restart. A denial is source unavailability, not economic failure.

Prior charged RPC1793 plus4243 leaves18964 of25000. Prior known raw637835 plus
262144 conservative Q2 unknown and the Q3 bound remains below2GiB. Documentary
49/60 is unchanged. This consumes source question3/6 and successor claim4/11;
financial0/4 and repair1/1 remain. No new confirmation allowance.

## Decision and independent verification

Primary source decision: all required calendar/ABI/identity fields observed, or
exact missing denominator. Same-block conversion identities and queue-ID/count
consistency are checked without annualization or a cashflow forecast. Independent
review must reconstruct raw envelopes, block alignment, source/pacing/denial
rules, counts and ancestry; structural lifecycle verification alone is inadequate.
Source output can admit later preparation only. No P/D, alpha, Sharpe, parameter
search, confirmation or strategy verdict is computed. Any later financial test
needs a separately frozen full-capital book, all applicable cash/crypto/allocation
comparators, executable costs, stress/risk gates, original selection history and
independent accounting review. Observed samples remain exploratory/spent.

# Q1 — public protocol accounting-state observability

The user authorized deeper continuation and enough time per experiment. This
source-only question occupies Q1 in the immutable phase-grant.json. It grants no
financial test or confirmation and does not change the economic/risk contract.

Question: do the already selected Ethereum/Base/Arbitrum endpoints expose the
Aave V3 native-USDC and Uniswap v3 WETH/native-USDC accounting states needed for
subsequent reconstruction, at one exact retained old block and one finalized
block per chain? The shared source need justifies a joint protocol probe. Current
protocols are selected by documented cashflow mechanisms, never a yield ranking.

## Inheritance and budget

Ordinary ResearchRun uses new source mechanism
`evm-protocol-accounting-state-observability`, prior_attempts1/attempt_budget3.
One is the known direct DEX lifecycle predecessor, already inside old188; it is
not another global historical attempt or a complete count of old on-chain calls.
Exactly two new source claims are reserved in this family, Q1 and Q2, both charged
to the six-source successor grant. Old allocation family remains188/189;
related44DeFi records and unknown multiplicity remain. This source family cannot
sponsor lending/LP alpha trials or reset old NLST/SMW/carry allowances.

The first structural parent is null because the ordinary helper requires a
same-mechanism parent. The exact original DEX claim, full source result, core and
DeFi crosswalks and original closure are declared inputs. This is explicit
cross-family provenance, not a claim of no ancestry. The source dataset is
exploratory/exposed; current source states become exposed on receipt. No source
fields are fresh confirmation or economic validation.

## Complete frozen request matrix

q1-spec.json fixes three endpoints, chain IDs, public protocol contract addresses,
call data, ABI word types, identities and full-range tick boundaries. Aave Pool
and aToken candidates come from the three retained official address-book document
snapshots. Arbitrum uses native-USDC's USDCn aToken, never bridged USDC.e.
The previously mapped0.3%WETH/native-USDC pool is retained on every chain.

Per chain: chain ID, historical header and finalized header (3 calls), then
20 ABI calls at each of the two epochs (40 calls): USDC/WETH decimals, aToken
POOL/underlying identity, Aave normalized income/configuration/public-contract
USDC balance/scaled supply/total supply, Uniswap factory mapping/token0/token1/
fee/tick spacing/slot0/active liquidity/global fee growth0/global fee growth1/
minimum usable tick/maximum usable tick. Total **129 request cells**, plus
12 source-coherence cells (Aave supply and LP identity at sixchain/epoch nodes)
= **141 cells**. No chain/protocol/epoch disappears when unavailable.

Old heights are the retained original source probes: Ethereum21,000,000,
Base20,000,000 and Arbitrum280,000,000. They have different dates in2024 and are
source-capability probes, not matched financial windows. Header number/hash/time
must exactly match the retained predecessor. The new finalized header must be
valid and after the frozen history even if its new reread fails; finality is a provider assertion, not independent proof.
All state calls use canonical blockHash with requireCanonical=true. No fallback
to latest, block-number substitution or replacement endpoint is permitted.

A source field is complete only after HTTP/body/envelope/ABI/expected-identity
checks. Strict signed extension matters for negative ticks; bools and addresses
must have canonical encoding. Aave scaled supply times normalized index uses
ray-half-up rounding and must agree with total supply within one base unit.
This is a source/accounting identity; zero/invalid index cannot qualify.
The LP coherence cell checks exact factory/token/fee/spacing identities, all
required states, initialized sqrt-price/tick bounds and valid oracle cardinalities/index. Zero active liquidity is
retained explicitly rather than assumed tradeable. A returned configuration
bitmap is raw state; current bit semantics are not silently applied to every
historical implementation. Protocol version and upgrades need later admission.

The only balanceOf call targets USDC held by a named public aToken protocol
contract. No personal wallet/account address is queried. This balance is not a
guaranteed user withdrawal amount: pause/cap/liability/reserve-deficit/access
rules, execution impact, rewards and transaction gas remain separate questions.
Global fee-growth counters alone are not LP returns; boundary ticks, inventory,
liquidity path, price/cost and dilution assumptions must be admitted before that
calculation. No accrual ratio, annualized yield, net profit, risk ranking or
chain winner is computed in Q1.

## Runtime and retained evidence

There is **no total elapsed-time kill, cooperative elapsed deadline or cumulative
CPU-duration cutoff**. Source execution is synchronous, with no subprocess,
background worker or concurrency. Reuse the frozen read-only dex transport,
which bounds each individual request to10seconds and256KiB, retains received
prefix bytes and forbids redirects, credentials, proxies and alternate endpoints.
Per-request timeouts are unavailable source evidence; they do not kill the
experiment or imply an economic failure. Exactly one attempt per request.
HTTP403/418/429/451 stops subsequent calls at that endpoint; other chains proceed.
Missing chain/header blocks dependent calls, retaining them as unattempted.
JSON-RPC state errors remain individual unavailable cells and do not silently
remove the rest of the registered matrix. No access-denial circumvention.

Worst-case raw reservation129*262144 =33,816,576bytes (within phase2GiB). There
is no elapsed-based partial matrix. Every request publishes its immutable intent
before transport, raw-byte receipt immediately after transport and parsed cell
result before the next request. The12coherence results and summary are also
published separately: **400 outputs**. Request/receipt UTC, elapsed time, URL,
payload, status, body completeness, bytes, base64 and SHA256 are retained.
A process/storage failure retains all published intents/receipts/results and
consumes its attempt; incomplete trailing cells are not claimed evaluated.
No automatic restart; the explicit shared repair grant requires separate review.

## Verification, decision and next question

Commit exact charter/spec/grant/addresses/ancestry/source/gate before any RPC
call. Run metadata admission, focused invented-input checks and a disposable
no-network lifecycle preflight. Independent review must examine the literal ABI
request matrix, source/time boundaries, imported history and raw-byte retention.
After completion, standard receipt verification and independent raw decoding
reconcile all141cells/400outputs and every unavailable/request-denial case.

Qualified fields admit only those particular source observations. An old-state
failure at one endpoint does not prove the chain lacks archives. Current-only
success does not admit historical performance, user eligibility, withdrawal,
capacity or a profitable route. Q2 is justified only by an explicit remaining
reconstruction question and a complete reviewed historical/cost/mark request
contract; it may not replace providers or select a chain on inspected returns.
Staking, small-token and inherited carry/info questions remain independently
ranked under the same finite grant. No further go-ahead is needed for admitted
work; no financial inference is made in this source claim.

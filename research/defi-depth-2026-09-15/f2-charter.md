# F2: one indivisible wrapped-staking/cash financial attempt

Pre-execution contract. The design, exact implementation, source inventory and
this charter must be committed and independently reviewed before any acquisition
or calculation. No time/CPU cutoff. This uses the unusedF2 financial slot and
never restarts Q3/R1. Failure of indispensable source prerequisites consumes F2
as unavailable, preserving the failure and all planned cells.

## Estimand and inherited search

One fixed Base wstETH/cash allocation tests conditional full-account net dollar
profit P and its wrapper-price return increment over an ETH/cash control. ETH
appreciation alone is a competing explanation, as are oracle construction,
conversion caps, bridge/market discounts and hypothetical fills. No native
staking rewards, conversion accrual or redemption cash is added to the wrapper
oracle-price return. It cannot identify pure staking yield or realized wrapper
market returns. The published Aave source supplies a model, not a verified
historical deployment/upgrade or freshness attestation.

Seven direct predecessors are inherited: old DEX source; Q1/Q2/R1/Q3; WBETH
inputs/book. Their failures and spent samples remain. Narrow financial family
prior7/cap8 admits this first unhedged recipe; no old hedge/source attempt is
refreshed. Original188/189 allocation and44relatedDeFi records overlap these
imports and are not summed as independent tests. Full crosswalks, exact claims,
terminals, original panel and baseline output are pinned. Q4/Q6 remain closed
with their source limitations, not supporting alpha. Shared repair1/1 is spent.

## Capital, clocks and fixed recipe

$10000 total initial committed value. The accepted10%annual net target,
30%drawdown and50%market/stablecoin stress limits remain.365days, pre-personal-tax
reporting and$200incremental hurdle are disclosed research assumptions.
Primary window2025-09-01 00:00UTC through2026-09-01 00:00UTC; entry September2
00:00UTC, no intermediate trading. Earlier2024/2025annual financial cells remain
unavailable for missing complete retained prerequisites; no new older acquisition.
There is no outcome-based cohort, range, weight, chain or token selection.

Conditional starting basis:0.005nativeETH gas prefunded on Base; nativeUSDC is
floor((10000−gasUSD)/USDCUSD*1e6) atoms. Funding rounding is explicitly charged.
All gas and residual USDC remain committed capital. Inception wallet funding,
CEX withdrawal and bridging routes are not witnessed: the starting-wallet basis
is a disclosed condition, not a proved dollar-deployment cost. No outside top-up.

At entry spend exactly floor(initialUSDCatoms/4) into BasewstETH. This is25% of
initial investable USDC after gas reserve, not25% of total capital or of later
NAV. Hold tokens unchanged to terminal. The matched ETH25control spends the same
USDC budget on nativeETH. Wallet cash leaves USDC unchanged. No staking accrual
is separately added. Same-day oracle/WETH mark is assumed for nativeETH; bridge,
wrapped/native basis and execution remain unverified.

## Fixed acquisition and semantics

Only Base mainnet public read-only RPC; exact oracle and tokens in f2_source.py.
Same-block getAssetsPrices(address[]) returns [WST,ETH,USDC] daily; at the two
retained boundaries acquire only [ETH,USDC] and reuse exact raw WST price.
The official AaveOracle source loops over getAssetPrice for this view; dynamicABI
must be exactly offset32,countN,Npositive uint256 prices. The USD unit1e8 and
zero base currency must match both retained Q3 boundary qualifications.

366daily marks. Reuse four exact Q3 header receipts at2025/2026boundaries.
Never-attempted days use original Q2calibration, adjacent blocks and canonical
hash-tagged calls; no height search. Exclude all182R1/Q3historical header keys
and every other previously attempted Base key retained in the metadata inventory.
The paired two-second bracket straddles each00:00UTC target. The left observation
can be one second before the nominal mark. This source/bar timing and oracle
basis differ from the original CEX benchmarks; cross-venue differences are
conditional, not clean mechanism attribution. No substitute source on failure.

Order is inception, entry, terminal, then remaining days chronologically. No
prices determine order. The first missing indispensable bracket or price stops
later acquisition; every remaining planned request and financial cell is retained
as unavailable. Each new key has one owner. Later financial recipes may reuse
these receipts, including failures; they cannot retry an owned failed key.
Each actual RPC retains its intent before sending and exact body/hash/UTC receipt.
Five seconds after the previous response; one synchronous call; no retry/burst,
provider change or VPN manipulation. HTTP403/418/429/451 and first recognized RPC
throttle stop the endpoint. Per-request transport10seconds/262144bytes stays.
No overall duration stop. Unknown/unreceipted intents reserve their whole possible
request and byte charge. No financial book is calculated without all366marks.

1094newRPCmax;286785536rawbytesreserved. Prior1841RPC leaves22065after maximum
reservation. Prior known raw754936, Q2unknown262144 and existing metadata reads
1140942 are conservatively charged separately. Total remains below2GiB.55/60
source documentary operations have been used. The additional operation reviewed
[AaveOracle](https://raw.githubusercontent.com/aave/aave-v3-core/master/contracts/misc/AaveOracle.sol).
No paid resources or external account actions.

## Complete conditional costs and cash accounting

Three fixed scenarios: primary, doubled modeled costs, frictionless diagnostic.
Primary gas0.0001ETH/transaction, doubled0.0002, diagnostic0. These are combined
Base transaction-cost assumptions, not an observed or proved upper bound.
Entry: USDCapproval + atomic swap, two transactions for F2 and ETH25.
F2terminal: wrapperapproval + atomic sale, two transactions, then one nativeETH
sale of all remaining gas minus that sale's own gas. ETH25terminal and walletcash
terminal use one nativeETHsale. Total F2five,ETH25three,cashone modeled transactions.
No extra gas is sourced from a sale's proceeds. Insufficient prefunded gas excludes
the entire affected financial cell; positive sales rounding to zero are unavailable.

Hypothetical F2route is USDC→ETH→wstETH and reverse, two hops; ETHcontrol uses one.
No actual pool/route is admitted for wstETH. Each primary hop has0.3%fee and10bp
adverse execution. Entry retained-value factor perhop=(1−0.003)/(1+0.001);
exit=(1−0.003)*(1−0.001). Doubled modeled cost adds another0.3%friction perhop and
20bp adverse; it does not claim the actual pool fee tier changed. Frictionless
sets both zero. Integer output atoms round down at the aggregate hypothetical
route output. Internal per-hop execution/rounding is unverified, another reason
this is conditional. Fee/impact/rounding are embedded exactly once in the signed
conversion; gas is an additional nativeETH debit. No added native conversion.

Final USDC marks to USD, less the old$10route charge ($20doubled,$10diagnostic).
This is a hypothetical USD cash route, not proof of redemption or spending access.
Funding formation residual, swaps, gas, all balances and terminal route enter P.
Exact rational cash identity: P=held-asset market PnL−marked swap/gas/rounding
losses−funding formation loss−terminal route. Atom deltas independently reconcile
all balances. Unknown actual source/exit costs are not asserted zero; they block
implementability. The conditional cost model is the screen's explicit hypothesis.

## Risks, controls and primary decision

Daily plus after-event liquidation NAV on full capital gives discrete drawdown;
this cannot bound intraday losses. Worst rolling30day loss is descriptive.
Primary deterministic shocks at every retained state: ETH/WST−50%;−80%;−90%;
WSTdiscount30%alone;ETH−50%plusWSTadditional30%discount;USDC−20%/30daylock;
7daywalletoutage withETH−50%,WST−65% and5×adverse; combined withUSDC−20%/30daylock.
Double-then−60%crypto path retains peak drawdown separately, without rebalancing.
No rewards accrue during the synthetic path. Delayed liquidation is marked after
an authored lock; actual dollars at day365 are unavailable when lock crosses it.
It cannot establish passing the full stress/implementability rule.

Total wallet loss and total wrapper-position loss are separate affected-position
tails, never estimated probabilities. Actual losses would still reduce NAV.
Market/stablecoin loss is not excluded as a tail. All gas/reserves get own marks.

For each scenario retain P and all D against B0–B9 plus walletETH25 and walletcash.
B1 remains unavailable, not replaced by B0. Original comparator costs/risks stay
unchanged; risk-ineligible benchmarks remain displayed. The primary scenario must
have P≥$1000, discrete drawdown≤30%, numerical stressloss≤50% and D≥$200 versus
EVERY fixed comparator with known passing numerical risk. Unknown eligibility
or missing B1 never counts as a pass. Doubled/frictionless results cannot rescue
primary failure. D is separate from absolute cash profit. No Sharpe/metric/window
substitution. WalletETH25 supplies source-compatible increment; original CEX
controls retain source/route basis limitations.

Passing these numerical conditions is descriptive only. Full decision remains
unavailable without B1, implementable execution/cash/stress and untouched or
prospective confirmation. No strategy selected or promoted from this exposed
historical screen.27book cells (threecohorts×threepolicies×threescenarios) and
36D cells all remain; no significance inference, parameter tuning or fresh alpha
allocation. A log-return convention diagnostic deliberately computes the wrong
cash adjustment separately; actual PnL never uses it.

Synthetic flat/gain/loss/rounding/gas/depeg tests and independent accounting,
source/history and exact preregistration review precede execution. Post-run review
reconstructs raw source, every cell and signed balances independently. Missing
data, economic failure and measurement failure remain distinct. Preserve this
complete attempt; no repair allowance remains. Run only in a detached fixedHEAD
execution root with all predecessor evidence verified; no commits there until
terminal. No production/orders/account queries/background automation authorized.

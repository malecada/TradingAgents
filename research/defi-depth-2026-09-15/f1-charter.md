# F1: unlevered native-USDC lending and cash

Pre-execution contract under the unused F1 financial slot. No new source or
financial calculation is authorized until exact source ownership, inputs, code,
resource reservations and this charter are committed and independently reviewed.
One indivisible financial claim includes its necessary source acquisition. No
experiment duration or cumulative CPU-time kill; at most two computation threads.

## Objective, fixed recipe and inherited search

Total committed initial capital is$10000. The accepted objective is at least10%
annual net return, with30% observed drawdown and50% authored market/stablecoin
stress limits. USD,365days, before personal tax and a$200/2% incremental benchmark
hurdle are explicit research assumptions. Catastrophic exchange, wallet, contract
and token-position failures remain separate affected-position total-loss tails;
actual losses always reduce NAV. No orders, wallet actions, account requests,
paid resources, provider contacts or production changes are authorized.

One70% allocation of initial investable nativeUSDC after prefunding0.005nativeETH
for gas is supplied to the fixed Base Aave V3 native-USDC reserve. The other30%
of USDC and all remaining gas stay committed. Entry is September2,2025; terminal
liquidation September1,2026; inception September1,2025. No rebalancing, leverage,
reward farming, protocol substitution or interest credited twice. Hold exact
scaled aToken units and redeem their modeled underlying entitlement once.

The earlier100% proposal is preserved. Its pre-outcome combined50% credit haircut
and20% USDC depeg algebra loses60% before costs;70% was chosen before F1 outcomes
with a margin below the resulting75% boundary. There is no weight sweep. Interest
can raise the lending share and cause this fixed allocation to fail stress.

Six direct source predecessors are imported: original allocation DEX source,
Q1,Q2,R1,Q3 and F2. Family prior6/cap7 admits this first lending recipe. Original
allocation188/189 and44 related DeFi records remain overlapping cumulative
history, not independent observations or reset budgets. Q4/Q6 source negatives,
Q5 inherited carry closure, frozen options and all spent samples remain intact.
No new confirmation allowance or alpha claim arises from this exposed development
window. The F2 missing canonical bracket and all unavailable books are preserved;
F1 never fills or recalculates them.

## Cash accounting and fixed financial denominator

Fund USDC atoms by flooring the exact residual after the native gas reserve at
inception USD marks. Charge formation dust. Supply floor(initialUSDCatoms*7/10)
and model two transactions, approval and supply. Terminal withdrawal costs one
transaction and residual nativeETH sale costs one, funded from the same reserve.
No external cash or gas top-up. Primary gas is0.0001ETH per transaction;
doubled0.0002ETH; frictionless zero. Native sale retains the one-hop authored
0.3% fee and10bp adverse execution; doubled uses0.6% and20bp; frictionless uses
zero for both. Doubled friction does not claim a protocol fee change.
Terminal USD routing is$10/$20/$10 across those scenarios.
Starting wallet funding and actual dollar withdrawal routes remain unverified.

A single cap-assumed numerical book per cost scenario measures the conditional
interest mechanism. An enabled unproved supply cap is explicitly unproved and
never assigned a successful feasibility value. A separate strict entry-feasibility
cell uses the same amount/units/flags and the proved sufficient cap bound if its
inputs exist; the disabled-cap branch needs no invented treasury/debt. No source
observation here qualifies the missing full reserve/debt ABI. Ordinary lending
flags and observed withdrawal cash remain required for the numerical book.

An independent opportunity ceiling uses only fixed inception prices, entry income
and terminal income/prices, with necessary model identities. It assumes entry and
withdrawal, omits nonnegative modeled costs and uses ceil(deposit*RAY/entryIndex)
then ceil(scaled*terminalIndex/RAY), which dominates the two half-up steps. Add all
idleUSDC and initial nativegas at terminal marks. Below$1000, only this fixed
source-model opportunity misses the absolute hurdle even under generous
assumptions. Above is inconclusive. This ceiling cannot replace net P, risk,
benchmark comparisons or implementation. Missing daily fields remain missing.

All three annual cohorts ending2024/2025/2026 remain visible; only the fixedlatest
cohort has new source acquisition. Earlier financial cases are unavailable without
complete qualified retained inputs. The exact financial denominator is27books
(3cohorts×F1/walletcash/walletETH25×3costs),6auxiliary strict-entry/ceiling cells,
36D comparisons and4boundary/decision/implementation/confirmation cells:73cells.
Every book and auxiliary attempt has a durable intent before financial arithmetic.
A cell-local feasibility failure leaves other fixed cells available; a measurement
defect suppresses later financial attempts and retains partial literal ledgers,
current source state, prior stress and explicitly incomplete attribution.

Wallet controls reuse the reviewed funded engine restricted to its two policies.
The engine's unused WST price is an explicit internal padding value for identically
zero WST units, never market data. Assert zero balances/deltas through every state,
then remove that asset from reported source marks. No missing held price is filled.
The controls' cash/gas/routes remain fully funded and comparable.

## Primary decision rule and interpretation

Report absolute exact net cash P and each D separately. The primary authored cost
scenario must satisfy P≥$1000, observed discrete drawdown≤30%, numerical market
stress≤50%, and D≥$200 against every fixed known numerically risk-eligible
comparator. Preserve B0–B9 plus walletcash and walletETH25; unavailable comparison
and risk cells are not passing evidence. The CEX controls retain their original
bar/oracle/route differences, which are not clean mechanism attribution.
Doubled-cost and frictionless cases cannot rescue a primary failure. No Sharpe,
alternate year, favorable benchmark or ceiling can replace the fixed decision.

Interest attribution uses changes in integer underlying entitlement once. Price
PnL includes idle cash, the existing claim and gas; the price/index cross term is
counted exactly. Signed conversion losses, formation rounding and route costs
reconstruct P. The erroneous-log-return diagnostic is isolated and never cash.
No confidence or validation claim is made from one exposed year and inherited
selection. Unknown actual B1 cash terms, exact personal routes, counterfactual
protocol behavior and genuinely fresh confirmation keep adoption unavailable.

At each funded state test ETH−50/80/90%, ordinary credit impairment50%,USDC−20%
with30daylock,7daywithdrawaloutage with ETH−50% and fivefold adverse execution,
and combined ETH−90%/credit−50%/USDC−20%/30daydelay with fivefold adverse
execution. Keep a double-then−60% ETH path drawdown
separately. Lock stresses are post-delay liquidation marks, not day365 usable
cash. They are authored scenarios, not maximum possible losses or probabilities.

## Source model, actual clocks and first ownership

Reuse the exact prior canonical left block when available. New headers use the
original fixed Q2 calibrated height, with recorded timestamp in[target−1second,
target] for that date's00:00UTC. Getter calls use its canonical block hash and
that actual clock. This contract does not claim nearest-block or adjacent-bracket
qualification. There is no height search or alternative timing chosen by returns.

After F2 terminal reconciliation, F1 may acquire only genuinely unsent new keys.
All actual/uncertain prior keys remain excluded, including the failed right header.
Expand prior scalar/vector oracle requests into canonical block+asset exclusions;
changing an oracle vector cannot revive a failed overlapping price field. Retained
header and price raw bodies are reparsed and bound to exact actions/blocks before
use. The complete ownership map and preserved suppression proofs are inputs.
Unresolved old price tags cannot prove new ownership.

Acquire the existing five scalar getters daily: normalized income, configuration,
contract USDC cash, scaled aToken supply and total supply. Half-up scaled/index
supply must agree within one atom. Entry/terminal witnesses are nativeUSDC
6decimals, aToken Pool/underlying identities and nonempty USDC/Pool/aToken code.
Different endpoint code witnesses make the conditional model unavailable; equal
proxy code does not prove unchanged implementation or semantics. No unverified
reserve tuple, assumed treasury/debt, Multicall route or daily proxy-slot sweep.

Reuse successful same-block oracle ETH/USDC marks. For provably unsent slots,
getAssetsPrices([WETH,USDC]) supplies positive USD units1e8 under the retained
oracle model. NativeETH is marked at WETH parity as an explicit assumption.
The original oracle boundary unit witnesses remain inherited; continuous oracle
construction, freshness, basis and upgrades are unproved. Added lending capital
can change utilization, rates and borrower behavior; historical index replication
is a conditional calculation, not executable return assurance.

## Bounded requests, persistence and stop rules

Only the fixed public Base endpoint, single synchronous read-only calls, no
credentials, redirects, provider changes or VPN changes. Collect inception,
entry and terminal first, then remaining dates chronologically. Continue the
fixed daily inventory despite isolated missing fields to preserve independent
endpoint and source findings; no value-dependent source order or selection.

Each newly owned logical request has exactly three declared physical slots.
Retry only registered transientHTTP502/503/504 or specified transport failures,
with15seconds then60seconds after the preceding response and a minimum5second
response-to-request pause. A complete RPC/model/ABI failure is not retryable.
Only the exact−32011 no-healthy-backend RPC error is compatible with transient
HTTP retry; other RPC errors take precedence. Prior failures cannot use this
new within-claim retry policy. Every sent and unused physical slot has an immutable
intent/receipt, and every actual physical send consumes the global request budget.

HTTP401/403/418/429/451, recognized quota/throttle text/code and access/TLS/
authentication failures stop all later requests to the endpoint. Mandatory rules
cannot be weakened by configuration. Retain Retry-After; honor a valid delay/date
up to300seconds, in short sleep chunks, including before the next logical call.
A longer or malformed provider delay stops the endpoint. Never bypass its limit.
Per-request wall deadline is30seconds. Retained response-prefix caps are16KiB for
scalar/vector getters,64KiB for bytecode,256KiB for headers; an extra detection
byte may be received, so these are not total wire-byte caps. No whole-run deadline.

The exact builder must reserve all three physical slots per potential new request
and their full retained-byte caps inside25,000requests/2GiB. Prior actual usage,
Q2 unknown-response reservation and all spent claims must be reconciled before
admission. Documentary60/60 is exhausted; no new lookup allowance. F1 consumes
one of four financial recipes and one of eleven total phase claims irrespective
of result. Shared repair1/1 is already spent. Exact design.json provides the
numerical inventory; unused retries are not actual sends. Independent raw,
accounting, denominator and risk review is required at closure. Preserve economic
failure, source absence, measurement failure and inadequate evidence separately.

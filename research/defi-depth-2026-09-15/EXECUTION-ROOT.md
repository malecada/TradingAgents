# Authoritative R1 execution root

The sole authorized R1 execution root is the detached research worktree
`/home/malecada/master_thesis/TradingAgents-defi-depth`, created at the exact
committed reviewed R1 packet. This isolates the long source capture from unrelated
HEAD changes in the active options checkout. No scientific or source field change
is introduced by this location mapping.

Use the pinned Python at
`/home/malecada/master_thesis/TradingAgents-audit-fixes/.venv/bin/python`
with PYTHONPATH set to the isolated worktree, which owns all relative source,
input and output resolution. Check imported runtime locations and exact source,
input, predecessor receipt and gate bindings before the sole launch. Record the
actual execution commit and tool session in STATE.md after starting.

One owner, one R1 launch. Per-checkout lifecycle locks do not prevent a duplicate
in another checkout. No R1 start is allowed in the original checkout. Before any
later admission elsewhere, import the exact R1 claim/terminal/output bytes and
its committed source history; copying the same receipt is not another trial.
No claim or raw output is rewritten to change its origin. The original options
study, original worktrees and production remain untouched.

# Q3 operational closure: preserved source, no financial result

Q3 failed at2026-09-15T18:35:26.673051UTC with `ValueError: source must equal the
full current HEAD`. Execution source was1d3491fd6f04d98d84228031749072703ff8af24.
The coordinator committed unrelated Q4/Q6 preparation in the same checkout while
Q3 was active. The ordinary lifecycle checks the entire current HEAD on each
write, not just unchanged source-file hashes. That commit changed HEAD and the
next Q3 publication failed. This was an avoidable execution-isolation error by
the coordinator, not provider denial, a duration cutoff or economic failure.
It was detected later while preparing the financial contracts; intervening
progress comments incorrectly described the capture as continuing.

Session38640 is terminal(exit1). No worker remains. No restart, new repair,
backfilling, deletion or output overwrite has occurred. The failed claim remains
spent and its original frozen source/design/gate/raw evidence remain unchanged.

Retained:129outputs;59paired intents/receipts;48attempted singleRPC requests;
11suppressed requests;117101rawbytes. All four fixed boundaries have results.
The2023 boundary is unavailable;2024/2025/2026passed source-observability checks.
Only four additional skipped daily vectors (September3–6,2023) were published.
September7 has its two suppressed request receipts but no daily vector.
The native Ethereum interface summary is retained. No annual daily panel,
financial return, realized cashflow, implementability or economic rejection was
established. Independent raw/source review and the ordinary structural verifier pass the failed-run closure. The complete5363-cell audit retains33published complete,37published unavailable,7attempted receipt-only,2suppressed receipt-only and5284unclassified without a directQ3receipt. The latter includes derived/imported decisions, not just RPC slots. There are4184potential request slots without an intent. See q3-closure-audit.json and reviews/q3-postexecution-review.md.

Unused Q3 resources are released after this terminal closure, not converted into
another Q3 allowance. Public acquisition charges now total1841(1793prior+48Q3),
leaving23159of25000. Q3 raises known acquired raw bytes from637835to754936, plus
262144conservative Q2 unknown. Q4 separately read1140942existing metadata bytes;
its raw source paths remain immutable, with their recorded retention limitations.
Sourcequestions5/6 and successorclaims6/11 include Q1,Q2,R1,Q3,Q4,Q6. Financial
0/4;sharedrepair1/1spent. Documentary54/60 after the additional Aave source review.

## Correct execution route for future work

A future admitted empirical runner must execute in its own detached, fixed-HEAD
checkout. Its complete prior claims, gates and input bytes must be preserved and
independently checked there. Development commits occur elsewhere; no commit,
checkout or merge may change the execution root's HEAD while a claim is active.
The original raw/options work remains untouched. This corrects orchestration;
it does not weaken the lifecycle or make failed attempts resumable.

F1/F2/F3 financial questions remain possible only under their separately frozen,
indivisible first financial contracts and remaining allowances. They may not
masquerade as a restart of Q3 or R1. Their source plans must now account for the
missing daily headers as well as missing protocol fields, while excluding all
previously attempted keys. Retained boundary receipts can be reused. Exact
acquisition/economics/budget/history review still precedes any next run.

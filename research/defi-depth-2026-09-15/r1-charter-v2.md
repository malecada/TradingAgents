# R1 — exact Q2 batch compatibility repair

The reserved shared repair addresses the independently verified Q2 collector
defect: Base permits at most10 calls in one batch, whereas Q2 sent15/29-state
batches. This is a request-shape constraint, not an access authorization denial.
Q2's failed claim,46outputs and partial exposure remain immutable and charged.
See Q2-DEFECT.md and reviews/q2-batch-defect-review.md. No elapsed-time or economic
stop is inferred. No protocol state outcome was obtained from Q2's state batches.

Use the additive R1-only `research_defi_repair` copy of `research_amended`,
retaining its strict one-increment certificate.
The original family object prior1/cap3 remains byte-identical. The certificate
binds both Q1 and Q2 claim/terminal hashes, latest failed Q2 parent, exact R1
contract, independent approval and passing repair preflight. Effective budget4
permits only R1. Amendment chaining is forbidden. This consumes the sole shared
repair; six source/four financial slots and no-new-confirmation grant are unchanged.

All Q2 inputs, windows, exposed datasets, exact20,890cells, dates,365-daycohorts,
addresses, field order, canonical-left-block rule, ABI/identity checks, raw-source
scope and financial non-admission remain unchanged. The original q2-spec.json
is an immutable baseline input. No new field, endpoint, date, benchmark, cost,
economic allocation or statistical selection is introduced. All old gate objects
remain in gates-r1-v2.json unchanged.

The new separately hashed harness performs the following sole acquisition repair:
partition each fixed ordered state vector into consecutive chunks of at most10.
Fifteen daily calls become10+5; twenty-nine boundary calls become10+10+9. The
chain/header request shapes are unchanged. Each actual chunk publishes its own
intent and exact raw-byte receipt, retaining IDs and failure boundaries. Responses
are never reconstructed into invented combined raw bodies. Chunk failure leaves
its own fields unavailable; other registered chunks proceed unless HTTP access
is denied. Duplicate/extra IDs, missing members, errors, nulls, incomplete/oversized
bodies and unavailable headers retain the same rules. No fallback/retry/provider
switch. The inherited transport is unchanged; the repair harness additionally
rejects a batch larger than10 before network access.

Transport bounds are explicitly superseded ONLY for R1 by the reviewed harness:
max3,293 HTTP requests, unchanged18,689 logical RPC subcalls,256KiBperresponse,
max863,240,192 raw bytes. 32KiB request and10-second per-request deadlines remain;
no aggregate elapsed-time or CPU-duration cutoff is added. New outputs are7,684:
three chain files, headers intent/receipt plus two state-chunk intent/receipt pairs
and results for each ordinary day, an additional state-chunk pair at each of four
boundaries, then the summary. Every intended source cell remains in20,890.

The full replay includes the already exposed eight daily vectors and partial
ninth-day header calls, solely to obtain the unchanged complete panel after the
collector defect. It is not fresh evidence. Conservative cumulative acquisition
reservation is129(Q1)+168(failedQ2)+18,689(R1)=18,986/25,000logicalsubcalls. Actual
HTTP attempts remain separately counted. Known earlierrawbytes100,951+37,336 plus
262,144 reserved for the unreceipted Q2 intent and R1's reservation fit2GiB.
Documentary usage before R1 is41/60. Larger capital and financial variants are
not tested in this repair.

The amendment schema calls its preservation record an `economic_manifest`.
Here it preserves a source-only contract: no economic outcome or financial recipe
exists. It partitions unchanged ABI/accounting/transport/runtime-lock dependencies
from the old/new orchestration harness, and mechanically requires all nonmutable
experiment fields, including inputs/windows/cells, to be identical. Independent
review must inspect the harness diff so batching cannot conceal a scientific change.

Before actual capture: commit all certificate bindings; metadata admission must
pass the exact exhausted-family/latest-failed-parent grant. Focused invented
fixtures simulate the provider's10-member rejection, verify that the repaired
full20,890-cell/7,684-output matrix succeeds, and preserve partial/denied/missing
chunks without hidden retries. A compact disposable real-main/lifecycle check
with injected transport tests API/output routing; it is not an actual amendment
or empirical trial. The independently bound preflight and review plus actual
metadata admission support this one execution. No original worker/runtime edits.

After capture: verify every output/denominator and independently reconstruct raw
source evidence. Report endpoint-inclusive366marks per365-daycohort and separate
daily source coverage from boundary oracle/version/valuation validity. Q2's
executable-route cells remain explicitly unavailable. No annual return or cash
profit is computed until a separate financial recipe has its complete reviewed
cost/capital/benchmark/stress/multiplicity contract. Continue eligible Q3–Q6.

## Pre-admission historical-verifier compatibility correction

No actual R1 claim or network request existed when metadata admission discovered
that the old historical amendment verifier counts later same-family claims as
predecessors. The additive runtime reconstructs each historical certificate at
its own claim time: only strictly earlier same-family claims enter that
certificate's predecessor inventory; equal timestamps or non-UTC clocks fail.
Overall admission still enumerates and charges every current claim, including
later ones, with unchanged same-family identity and budget checks. The new
runtime accepts only R1 through gates-r1-v2.json. Original runtime files remain
byte-identical. No experiment date, field, financial rule or source query changes.

The earlier reviewed packet and metadata failures remain preserved. This v2
packet adds no empirical attempt and does not consume an additional repair: R1
has never started. New source/runtime hashes, manifest, preflight, independent
approval and certificate must all be rebound and committed before the sole
actual launch. Verify both the historical three-predecessor certificate and
continued visibility of its later family claims with read-only/synthetic checks.

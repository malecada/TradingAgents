# Draft integrated financial questions after source capture

Unexecuted proposal. No financial gate, registered acquisition or source outcome
is authorized by this draft alone. Reviews/financial-continuation-review.md
permits preparation of indivisible first financial attempts, with exact code,
inputs, history, budgets and independent review still required. Q3 is closed after an execution-root HEAD error (Q3-RESULT.md);
its immutable source files and spent attempt must not be changed. All original annual cohorts stay
visible; the latest fixed2025-09-01→2026-09-01cohort remains the primary economic
window and no better-looking year may replace it. Earlier cohort financial cells
may remain unavailable; no old failed source request is retried.

## Proposed rules before financial observations

Common $10000 initial committed USD value,365-day research horizon,10% net target,
2%-capital incremental hurdle,30% drawdown and50% market/stablecoin stress. These
are the accepted preferences and disclosed research assumptions, not inferred
from older options gates. No fresh confirmation or promotion with missing B1.

Proposed conditional wallet starting basis: prefunded0.005native ETH for gas,
remaining capital in native USDC; both marked in USD at inception and all capital
included. No outside contributions. Formation rounding must be explicitly retained
and charged, never discarded. This is not a confirmed personal starting balance.
A matched wallet cash control starts with the same gas balance. Original CEX
cash/crypto/allocation controls retain their costs and availability distinctions.

F1 proposal: supply all investable USDC after the fixed gas reserve, no leverage,
reward farming, rebalancing or exit-route switch. Deposit at the original
September2conditional execution clock, withdraw at the fixed terminal clock.
Use integer scaled units and ray rounding only under the disclosed source model;
fees, gas and any rounding loss remain in the signed ledger. Market-credit,
withdrawal and implementation limits stay separate from catastrophic exploits.

F2 proposal: one25% wstETH allocation, remaining USDC and fixed gas reserve,
no repeated trading. A qualified traded wrapper price reflects the whole claim; the captured oracle's treatment of conversion growth and discounts remains unverified. Do not add native staking accrual to its price return. Native historical conversion/queue waiting
remain unavailable, so a wrapper-market proxy screen cannot claim native
redemption cashflows. Exact route/cost assumptions still require review.

F3 proposal:50% of the investable USDC budget to one WETH/native-USDC Uniswap v3
0.3% full-usable-range position on Base, remaining USDC and fixed gas reserve.
Ticks−887220/+887220 at spacing60; no range optimization, recentering or
intermediate fee reinvestment. Size integer liquidity to the funded entry budget
including the WETH purchase costs, using exact mint rounding. Direct control holds
the identical funded entry-token quantities. Full-range attribution is conditional
on the reviewed fixed historical path; added-liquidity and path changes remain
unqualified. The LP sleeve fraction is proposed to leave room for inventory
stress and to start near the simple25%ETH/75%cash exposure, not from observed fees.

## Proposed cost scenarios (authored, not observed fees)

Three roles, fixed before results: primary; doubled execution costs; frictionless
trading/gas diagnostic. The primary remains the decision scenario. Reuse the
original$10 terminal USD-route charge ($20 doubled) rather than dropping it from
a diagnostic after the fact. The real route remains unavailable.

A proposed Base transaction-cost model is0.0001ETH per listed operation,
0.0002ETH doubled; exact operation counts must be fixed for each entry, approval,
mint/supply, burn/withdraw, collection and final sale. This is a combined gas-cost
scenario, not a witnessed L1/L2 invoice or a proven cost upper bound. All gas comes
from the prefunded balance; if it is insufficient, retain an unavailable cell.

Selected v3 swaps must at least include their0.3% protocol fee plus proposed
10bp adverse execution. A doubled-cost scenario tests additional friction; it
must not claim the pool's actual fee tier changed. The gas-reserve terminal sale
and token liquidation need their own exact fee/currency/rounding path. An LP's
initial purchase occurs before its mint and earns no self-credited LP fees;
terminal sales occur after burning/collecting the position. Costs are never both
embedded in prices and separately debited twice. These choices are draft pending
complete cash-event tests and review, not already admitted numerical gates.

## Reuse and necessary source acquisition

Reuse exact qualified retained Q3/R1 headers and preregister any strictly necessary
never-attempted daily headers as part of the financial attempt; never repeat any previously attempted method+
parameter key, including failed R1 observations. Q3 source values, R1 failures,
Q4/Q6 findings, original benchmark/price inputs and their exposure history must be
pinned. Acquisition is part of each indivisible financial attempt, not a new
reconnaissance stage. If prerequisites fail, the financial attempt remains
unavailable; it cannot be renamed or followed with an ungranted repair.

F1 needs normalized income, configuration, contract cash and scaled/total supply;
identity/decimals and implementation bindings are also required. A daily coherent
scaled-supply identity is useful evidence but not proof of unchanged deployed
user-account semantics. Current public contract cash is not unconditional
withdrawal/solvency assurance. Only fields necessary for the frozen recipe may
be acquired. Dollar marks and comparison clocks should reuse the retained
conditional source panel where appropriate; any oracle/CEX basis discrepancy
must be explicit and cannot be picked for favorable profit.

F3 needs slot0, active liquidity and both global fee-growth counters plus exact
pool/token/fee/spacing identities, bytecode, mint headroom at full-range boundaries
and protocol limits. Historical boundary-counter reconstruction is not necessary
for the reviewed full-range attribution identity, but mint capacity and actual
fee/price-path effects still matter. Fee counters remain unsigned modulo256;
only a single terminal position update is modeled. Daily snapshots cannot prove
an intraday liquidity floor or executable market capacity.

Both must preserve every committed balance, source failure and unavailable
comparison. Report conditional P, all D contrasts, discrete NAV/risk, full cash
reconstruction and the erroneous-log-return diagnostic separately. No log return
enters actual PnL. No Sharpe or alternate metric can rescue a primary failure.
Source deficiencies prevent implementability even if a conditional result passes.

## Additional official source-model review

Five documentary operations were used after the prior49: three initial opens
and two focused rereads, bringing documentary use to54/60. These identify a
source model, not the actual deployed implementation for every historical day.

The reviewed [reserve configuration](https://raw.githubusercontent.com/aave/aave-v3-core/master/contracts/protocol/libraries/configuration/ReserveConfiguration.sol)
places decimals at bit48, active/frozen at56/57, paused at60 and the36-bit supply
cap at116. These positions can support an explicitly version-qualified decoder.
The [scaled balance implementation](https://raw.githubusercontent.com/aave/aave-v3-core/master/contracts/protocol/tokenization/base/ScaledBalanceTokenBase.sol)
uses ray division for mint/burn quantities and retains scaled units; the
[aToken implementation](https://raw.githubusercontent.com/aave/aave-v3-core/master/contracts/protocol/tokenization/AToken.sol)
uses normalized income to express balances and transfers underlying on burn.
Deployed code/version mapping remains a separate source requirement. No contract
source, account state or production module was altered.

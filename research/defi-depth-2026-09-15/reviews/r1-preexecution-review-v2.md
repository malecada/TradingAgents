# Independent R1 v2 runtime and amendment approval

Disposition: **Approve the exact single-amendment v2 packet.** No blocking
correctness or preservation issue was found. Regenerate final certificate
references, commit them and pass actual metadata admission before the sole R1
capture. Earlier packets, approvals, failed metadata attempts and Q2's failed
claim remain historical evidence.

Reviewed September 15, 2026:

- Source SHA256:
  `0d1a8f4a331f8e21c1ad7dcc87927e33fa2518ba5ac08a73ed3c1795111b757c`
- Target contract SHA256:
  `7af74dedd1d9d7d5defb301e643483207a6b8c5e4fa723230d02f19e1150fb31`
- Preservation manifest SHA256:
  `aa7299cd660e42dec55afcb50d4fbb46f68dcdfddb4efb7a5e9ff70de4913318`

Only local source, retained metadata and invented inputs were examined. No
network request, source acquisition, financial calculation or options-worker
inspection occurred. Only this review and its exact approval JSON were written.

## Runtime correction independently verified

Compared every copied Python module against research_amended. Four modules are
byte-identical: initializer, amendment validator, lifecycle and structural
verifier. The only admission change is the early restriction to
`defi-depth-r1-20260915` and the exact gates-r1-v2.json route.

The historical verifier adds `earlier_claim` and applies it to same-family
predecessors before reconstructing a historical amendment. Explicit UTC clocks
are required; equal-time ordering fails. Later claims are excluded only from
that earlier certificate's predecessor set. Prior receipt/hash, exhausted-budget,
failed-parent, source-preservation and anti-chaining checks remain.

Independently compared the entire admission file after removing its two-line
route guard: it is identical to the original. Thus current claim enumeration,
same-mechanism accounting, failed/outstanding attempts and exposure history are
not filtered by the historical predicate. Later claims remain counted today.

The actual retained dated-book-amended certificate now verifies with its three
chronological predecessors despite two later claims. Targeted tests also reject
an omitted earlier predecessor, an earlier chained amendment, equal/ambiguous
clocks and non-R1 admission routes, while retaining later claims in current
enumeration. Nine metadata tests and eight batch-repair tests passed together:
17 passed. No original runtime files changed; their complete old registered
hashes were checked independently.

## Source semantics and exact bindings

The complete r1_source.py/r1_source_v2.py diff contains only the runtime import
and registration path. The already-reviewed deterministic ten-member batching,
per-chunk raw preservation, primitive decoding, canonical-left-block timing,
cohort coverage and unavailable rules are unchanged. No date, field, provider,
financial assumption or scientific calculation changes.

All historical Q2 registry family, dataset and experiment objects remain
identical. All nonmutable R1 fields match Q2 and the preservation manifest.
Registered source, runtime, charter and input hashes match current bytes.
Target-contract and preservation-manifest hashes were independently recomputed
and match the exact values above. The copied runtime receives its own hashes;
it does not substitute new bytes behind historical runtime identities.

The retained v2 preflight binds the reviewed source. Its full invented in-memory
panel has20,890 cells,7,683 artifacts before summary,3,293 HTTP requests and
18,689 logical subcalls, with three unavailable executable-route cells. Its
separate compact real main/research_defi_repair lifecycle fixture passes56 cells,
1 unavailable cell and20 outputs through8 HTTP requests. The compact fixture
uses an unamended invented family; it does not claim to have admitted the final
actual certificate. That remaining check must pass after final commit.

## Grant, preservation and limitations

The original source family remains prior1/cap3. The exact certificate permits
effective cap4 solely for the named R1 child of failed Q2, consuming the same
single reserved repair. The metadata compatibility failure preceded any R1
claim, so it creates neither another empirical attempt nor another repair
allowance. The original allocation188/189 and44 related DeFi records remain
preserved overlapping history.

Q1, failed Q2 and the maximum full R1 replay charge18,986 logical subcalls of
25,000. R1's physical limit remains3,293 HTTP attempts and863,240,192 new raw
bytes. The unresolved Q2 intent remains charged and separately reserved; no
duplicate request or replay becomes fresh evidence.

This exact approval adds no general admission facility, new source question,
financial recipe, repeated repair or confirmation. One authoritative execution
root and owner are required because worktree-local lifecycle locks do not
serialize two checkouts. All final claim/output/terminal bytes must enter future
cumulative history without alteration.

Live ten-member compatibility, archive coverage, continuous oracle and deployment
semantics, executable USD cashflows, gas, withdrawals, LP fee/inventory accounting,
strategy risk, profitability and power remain untested. The earlier Q2/R1
source-only qualifications remain. No different hash or later code change is
covered by this approval.

# Independent Q3 question and source-design review

Reviewer: broader_design_review, separate research-reviewer. No financial gate,
actual acquisition, network request or live-run change. Reviewed draft at2,229
RPCcalls and584,318,976rawbytes; independently checked all18function selectors
against Keccak signatures and reconstructed the request count.

Decision: useful staking-specific source question; refine before registration.

1. Freeze boundary-first acquisition: all four fixed Base boundaries, including
   their daily price/source observations. Only acquire a cohort's interior after
   both boundaries pass the frozen identity/unit/source/code checks. Retain
   skipped dates as unavailable. Do not choose dates/cohorts by price level.
   Ethereum's15interface calls proceed independently.
2. Reuse the eight exact R1 BASE_CURRENCY/BASE_CURRENCY_UNIT observations at the
   same boundary hashes, including unavailable outcomes. This reduces maximum
   new RPCcalls from2,229to2,221. No duplicate acquisition is informative.
3. Keep staking conversion/redemption in the question; merely another oracle
   panel would not establish a distinct family from Q1/Q2. A conservative ordinary
   route imports six known direct predecessors (old DEX source,Q1,failedQ2,R1,
   WBETH inputs and WBETH book), prior6/cap7 with one newQ3allowance. These overlap
   existing global history and are not six new trials. Preserve old families,
   original188/189closure,44related records and wider unknown multiplicity.
   Cross-family structural restrictions justify parent-null only with exact
   pinned provenance. R1's amendment runtime supplies no Q3permission.

R1's terminal/source/output history must be pinned before Q3 admission. The
revised q3-design.json implements the proposed staging, reuse and ancestry;
its exact registration and runner still need independent pre-execution review.

Daily oracle values cannot establish executable wrapper prices or separate
staking rewards from ETH exposure and wrapper discounts. Four code snapshots
cannot establish uninterrupted implementation history or upgrades behind an
unchanged proxy. The single Ethereum anchor does not establish historical
conversion or queue waits. Those qualifications remain explicitly unavailable.

# Independent R1 code and single-amendment review

Disposition: **Approve the single R1 amendment and exact source repair.** No
blocking code or preservation defect was found. Final certificate references
must be regenerated, committed and pass actual amendment metadata admission
before RPC execution. This approval grants no additional repair, financial trial
or confirmation.

Reviewed source SHA256:
`894f6df43753f83e3957b6c8e87fc96bec4c0f4f9c52bc2bd6599f57e24ec662`.
Target contract SHA256:
`99727c6b98fc8f843bac22366afbe4ce657df8546e651b24b3eb0bc0c85d398f`.
Preservation manifest SHA256:
`b652b970ed626b972b77c6bd4abefa9053638f140166860e5b40ab9b063d824c`.

Review date: September 15, 2026. Only invented inputs and local source/metadata
were examined. No network or financial experiment was executed. Only this
separate review and its exact approval JSON were written by the reviewer.

## Scientific contract preservation

The complete Q2/R1 source diff was inspected. Changes are limited to selecting
the amended lifecycle and R1 identifiers, enumerating chunk artifacts, deriving
transport reservations, enforcing at most ten members, partitioning the fixed
state payload, retaining per-chunk errors and selecting the phase repair slot.
Header selection, ABI interpretation, supply rounding, field coverage, cohort
handling and financial non-admission are unchanged.

Independent AST comparison found identical implementations of batch envelope
validation, header parsing, canonical brackets, action parsing, action selection
and cell enumeration. An additional independent check supplied identical
invented responses to Q2 and R1: every final cell value/status was identical.
The source preservation manifest labels the changed orchestration file as a
harness, but that label was not accepted in place of reviewing its scientific
logic.

All original family, dataset and experiment objects match Q2's registry. All
nonmutable experiment fields, including inputs, windows, cells, stage and reuse,
match the preservation manifest and Q2. The old spec bytes remain unchanged.
The new harness explicitly derives only physical HTTP/raw-byte limits; logical
queries and their18,689-subcall bound remain fixed. Source/input/charter/runtime
hashes and both prior claim/terminal hashes match their bindings. The independently
recomputed target-contract and preservation hashes match those above.

## Chunking, raw evidence and failure handling

At `r1_source.py:214`, ordered fixed state payloads are partitioned into10+5 or
10+10+9. A chunk cannot contain an unregistered member, change a block argument
or substitute a failed result from a different chunk. Each physical request
publishes a separate immutable intent before transport and exact raw receipt
before the next request. No synthetic combined raw body is produced.

Independent invented checks covered a denial in the second chunk, an ID belonging
to a different chunk, one omitted member and a partial second response. They
retained the full denominator and all required artifacts. Earlier receipts and
successful fields survived; missing/invalid fields remained unavailable. HTTP403
stopped later requests: the fixture charged4 HTTP attempts/23 logical subcalls.
The other cases charged8 HTTP attempts/49 subcalls without retries. All retained
raw byte counts and hashes reconciled. The complete focused target passed8 tests,
including the literal original provider-limit regression and the full repaired
inventory.

The preflight matches the reviewed source hashes. The complete invented panel
produced20,890 cells and7,683 outputs before summary,3,293 HTTP requests and
18,689 subcalls. Three executable-route cells remained unavailable. A separate
compact real main/research_amended lifecycle fixture produced56 cells,
1 unavailable cell and20 outputs through8 HTTP calls. That fixture used an
unamended invented family: it validates lifecycle routing, not the final actual
amendment certificate. The required actual metadata admission remains pending.

## Exact grant and budget

The unchanged amendment runtime requires one increment, exactly exhausted
original budget, complete prior inventory and the latest failed parent. Q1's
completed claim and Q2's failed claim satisfy the reviewed ancestry shape. The
original prior1/cap3 family remains unchanged; the certificate permits effective
cap4 solely for R1. It consumes the phase's only shared repair. Amendment
chaining is prohibited. Original allocation188/189 and the44 related DeFi history
records remain unchanged and are not counted anew.

Physical output and request bounds independently agree:

- 3,293 HTTP attempts,18,689 logical subcalls,7,684 final outputs.
- 863,240,192 maximum new raw bytes.
- Q1 plus failed Q2 plus full R1 replay:18,986 charged subcalls of25,000.
- Earlier100,951 and37,336 retained bytes, plus the conservative262,144-byte
  reservation for the unreceipted Q2 intent, plus R1's maximum remain below2GiB.

The original19 failed-Q2 HTTP intents/168 subcalls remain charged, including
the unresolved final request. Replayed dates and fields remain exposed
development evidence; the repaired run does not create an untouched sample.

## Remaining limitations

Ten-member live-provider compatibility and complete archive availability are
still empirical questions. A later HTTP denial must stop the endpoint; this
approval does not authorize another batch-size adaptation or provider fallback.
Unexpected process/storage failure may leave a published intent without a raw
receipt and must remain an incomplete, spent claim.

Daily source coverage does not establish boundary oracle validity, continuous
deployed-version semantics, actual USD redemption, gas, withdrawals, full LP
inventory/fee accounting, covariance, risk or profitability. Each365-day cohort
has366 endpoint-inclusive marks. Those qualifications remain inherited from Q2.
No further source/code change is covered by this exact approval.

# Independent F2 pre-execution review

## Disposition

**PASS for the bounded F2 contract and implementation reviewed at
`3ce3aef8f7b39c1675c779a87a9d007556e48715`.** The concrete findings raised during
review were corrected before acquisition. This is approval of one conditional,
indivisible development attempt, not approval of a strategy or a claim that
historical oracle marks are executable prices.

Execution still requires ordinary metadata admission at the final execution
commit and the specified detached, fixed-HEAD root with preserved predecessor
inputs. The coordinator reported an unfinished named offline suite with one
failure. That failure was not investigated in this bounded review; this report
does not certify the broad suite or waive resolution/classification before
execution. No actual F2 claim, network request or empirical financial calculation
was performed by the reviewer.

Reviewed implementation hashes:

- `f2_source.py`: `47e2bb1fa18e7177e4086ff319465ce78e727cc2181dde553926a9e1475ca85d`
- `wrapper_book.py`: `30ae064ce9fee97985bead7abf3fec31d4eadd238367af116a397fb09f62ed83`

## Scope and ancestry

F2 fixes the asset, chain, 25% initial-investable-USDC weight, dates, costs,
controls, missing-data rule and necessary source requests before fresh outcomes.
Its source work is subordinate to this one financial recipe. Missing indispensable
marks consume the attempt as unavailable; they do not reopen Q2/R1/Q3, create
another reconnaissance allowance, or authorize a repair. The first-source order
is inception, entry, terminal, then the fixed remaining calendar, without a
price-dependent choice. This is a legitimate use of the unused F2 financial slot
under the reviewed integrated-attempt route.

The narrow family imports seven named claims: original DEX source, Q1, Q2, R1,
Q3 and the WBETH input/book claims. Prior7/cap8 permits exactly this additional
attempt. Structural parent null does not conceal those cross-family imports.
The original allocation188/189 and44 related DeFi records remain overlapping
history, not independent tests added to or subtracted from this count. The shared
repair remains spent. No statistical multiplicity budget or confirmation is
reset by the family name.

Independent inventory reconstruction covered all four exact Q1/Q2/R1/Q3 output
directories, not only the author's listed files. It matched358 pinned attempted
Base intent files,1,569 unique request keys and182 R1/Q3 historical header keys.
Their charged Base subcalls are Q1=43, Q2=168, R1=1,496 and Q3=32; duplicates from
the authorized historical repair remain charged. Every listed intent hash and
literal request-key mapping matched. Failed/unreceipted attempts remain excluded.

## Source, clocks and finite denominator

- Two retained boundaries were checked against raw body hashes, lengths, HTTP
  completeness, response IDs and exact request identities. Adjacent block
  numbers, parent/hash links and two-second target brackets matched. The retained
  wrapper scalar ABI agrees with the published qualified source; the required
  USD unit/base-currency checks pass. No return was calculated from these values.
- Independent Keccak calculation confirms `getAssetsPrices(address[])` selector
  `9d23d9f2`. Dynamic calldata/return offsets, counts, address order, exact length
  and positive uint256 outputs are checked. The two retained dates request only
  ETH/USDC and reuse WST; other dates request the three-asset vector.
- New calls use the left canonical hash with `requireCanonical:true`. The
  bracket is provider-qualified, not an independently proved chain history.
  Historical left-block marks can precede the nominal midnight by one second;
  neither finality available at that instant nor actual fills are established.
- The final first-error logic suppresses the right header immediately if the
  left header fails. HTTP denial and recognized RPC throttling stop the endpoint;
  later requests retain explicit suppressed intents/receipts. No retry, provider
  substitution, batch, account request or transaction is introduced.
- Counts reconcile:728 new header calls +366 oracle calls =1,094 maximum RPC/
  HTTP operations,286,785,536 maximum retained raw bytes. Prior1,841 plus this
  reservation leaves22,065 of the phase's25,000 requests. Known raw754,936,
  unknown Q2 reservation262,144 and prior metadata1,140,942 remain charged.
  Documentary use is57/60. Per-request limits and five-second pacing remain;
  there is no overall elapsed-time kill.
- The final manifest contains1,532 unique cells and2,611 unique outputs:
  1,464 daily source cells, two history/panel cells,27 financial books,
  36 contrasts and three decision/implementation/confirmation cells. Outputs
  include27 durable before-book intents,27 book results,366 daily source
  records,2,188 source intent/receipt records and three aggregate files.

## Accounting, exclusions and decision rule

The earlier gross-versus-net wrapper-tail discrepancy is corrected by writing
off WST and re-liquidating surviving balances. Gross affected-position value is
separate. All nine invented policy/cost ledgers had already independently
reconciled funded atoms and terminal wealth; the prior review remains retained.
The optional log diagnostic now becomes unavailable without invalidating exact
cash accounting. Stress maximum peak drawdown is retained separately from the
row selected by maximum origin-loss fraction.

Authored feasibility exclusions now affect only their own financial cell;
unexpected arithmetic/accounting defects suppress subsequent calculations.
Independent fault injection confirmed that a zero-sized WST purchase leaves all
six ETH/cash control books available, and that failure during stress after a
funded entry preserves current balances, literal event reconstruction, current
clock and exact accrued cost attribution. A fatal first-book interruption also
preserves its before-book intent:19 intents and18 explicitly unavailable earlier
cohort results remained, with the started primary book lacking a terminal result.
No successful outcome was invented for that interruption.

Independent invented decision checks verified candidate-minus-comparator signs,
failure at a199-dollar advantage against an eligible comparator, continued
display of risk-ineligible comparators, and permanent unavailability of the full
decision when B1/implementation/confirmation are absent. P and D are separate;
doubled costs and the frictionless diagnostic cannot rescue primary failure.
The current13 focused tests also passed independently, including the complete
invented source/book denominator and throttle path.

## Preservation and limits

Every original family, dataset and experiment object in `gates-q3.json` remains
unchanged in the new gate. All19 declared source hashes,33 input hashes, the
charter hash and ordinary runtime hashes matched final bytes. The exposure bound
is the actual fixed preparation bound2026-09-15T19:24:00Z, not a future day. The
original financial panel and baseline file were hashed only in this review;
their empirical values were not reopened or recomputed. Their frozen schema was
checked from the existing producer code.

The screen remains conditional on prefunded wallet capital, native/WETH mark
equivalence, aggregate hypothetical swap output rounding, fee/gas assumptions
and a hypothetical USD route. It does not establish historical oracle adapter
behavior, continuous freshness, wrapper/native conversion, pure staking reward,
market discounts, executable route/depth, gas sufficiency in real transactions,
personal access, tax treatment or cash availability during a lock. Source/cost
conditions and unknown B1 prevent implementability or promotion even if numerical
conditions pass. No options worker, production source, order, external account
or additional resource was accessed.

## Subsequent narrow pre-execution precision review

The additional `financial` change uses `exact_net_cash_profit` rational records
when present, retaining the original frozen decimal-string basis for legacy
comparators. It also publishes `difference_exact`. This is approved as a
precision correction before outcomes: a true advantage of200 dollars minus
1e-80 must fail the unchanged200-dollar hurdle even when its displayed decimal
rounds to200. All seven F2 source tests passed independently after this change.
The updated `f2_source.py` reviewed hash is
`da456a757dba9b54e161ddd614e2c645f0a2a0777b1d9919ee598d1b52dbb763`.
The final registration/source hashes and execution commit must bind this updated
code before acquisition; the earlier commit/hash listing above documents the
earlier reviewed packet and is not a claim that its bytes are unchanged.

The reported broad-suite failure was independently reproduced without reading
actual Q4 inputs. Across100 invented same-size file rewrites during the second
`fstat`,92 had identical dev/inode/size/mtime_ns/ctime_ns fingerprints and returned
complete; eight had distinguishable fingerprints and returned unavailable.
Every returned digest and byte charge matched the exact old bytes read. Forcing
a different mtime made the intended detected-change fixture deterministic.
The test's assumption that every immediate rewrite changes this fingerprint is
false on this filesystem. The frozen inspector detects distinguishable metadata
changes, not every possible concurrent mutation or atomic-snapshot consistency.
This does not establish an actual mutation of Q4 evidence, and no historical
helper, gate, result or claim should be rewritten. A deterministic synthetic
fixture correction and explicit interpretation of that limitation are justified.
The complete named-suite result itself was not rerun by this reviewer.

### Final resolution

The deterministic fixture change and `heritage-fingerprint-limitation.md` were
reviewed. The combined focused suite passed independently:27 tests comprising
heritage6, F2 source7, wrapper8 and lending6. This resolves the identified
test/precision issues for F2 launch after the final commit, detached-root
content checks and ordinary admission. No further full-suite rerun is required
by these narrow changes. Preserve the reported original named-suite result as
1,911 passed,81 subtests and one fixture failure, followed by the independently
verified27-test resolution; do not relabel the original run as entirely passing.

# Independent Q4/Q6 pre-execution review

Reviewer: separate research-reviewer broader_design_review; no actual metadata,
article records, network requests, live-Q3 inspection or file edits.

Question/family review accepts Q4 prior45/cap46:44 dependent NLST/SMW/T7 records
plus original DEX source. Q6 prior12/cap13 imports the11 exact C1/C2/C3/P5 rows
and news-provenance-20260911. These scoped administrative counts are not complete
multiplicity; wider factor/P5/DeFi history and overlapping imports remain linked.
The exact seven producer files can establish a code contract, not historical
export identity, completeness or economic failure. Four had prior inspection.

Initial exact review found a bookkeeping defect before execution: a changed
file or partial OSError could discard the consumed-byte count and digest. An
invented35-byte example incorrectly charged zero. Pre/post file identities were
also not retained. The defect and its correction are preserved here; no actual
metadata was inspected with the defective helper.

Correction retains all read bytes/digest on failure, distinguishes full stable
files from partial prefixes, charges failures, persists pre/post identities and
uses no cap+1 sentinel. The original source path is explicitly not an immutable
captured copy. Eight target intents precede reads. A new invented partial-error/
concurrent-change test passes; a test's initial hardcoded expected length27 was
corrected to the actual29 bytes before execution.

Final PASS, conditional on committed metadata admission: all six tests pass.
Independent growing-file test confirms cap enforcement, consumed-byte/digest
retention and changed identities. Independent node/depth, FIFO and value
sanitization checks passed. All44 prior packet hash references and11 ancestry
rows/full-ledger hashes matched; refreshed final gate hashes match. Q4 has
24cells/33outputs; Q6 has14cells/15outputs. Requirement cells are explicitly
unproved, not failed alpha. No remaining blocker in this bounded review.

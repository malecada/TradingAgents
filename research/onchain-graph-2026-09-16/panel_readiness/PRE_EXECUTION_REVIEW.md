# Independent panel-readiness pre-execution review

September 16, 2026. **Approve one frozen metadata-only execution of `eth-panel-readiness-20260916` under the exact contract below.** No material blocker remains. Approval requires the declared committed, remotely verified source, detached fixed-HEAD checkout, locked local runtime and successful metadata admission before the single claim. This review acquired no archive data and performed no financial or actual-day motif calculation.

## Scope and history

The proposed question is substantively downstream of the completed one-day benchmark: current three-year object inventory, sampled schema and synthetic completion-day assignment. It does not repeat the failed motif experiment, modify its resource certificate or replenish its allowance. The new family imports precisely four named lineage claims once through prior_attempts 4 / attempt_budget 5. Original source/prototype/motif family objects and the exhausted single amendment remain unchanged. The forensic artifact remains inspected history, not an additional lifecycle claim. A null API parent is qualified by eight exact scientific-parent claim/terminal inputs.

Independent hash comparison verified all 16 inputs, 17 source files, six base lifecycle runtime entries, the charter, all 81 inherited metadata files and the exact inventory of 41 local claims. This local inventory does not establish global independent-hypothesis multiplicity. The 2022–2024 window remains exploratory exposure; no existing observation becomes fresh confirmation. The reviewed plan has 30 lifecycle cells, 111 outputs, 2,192 date/table inventory entries and 24 fixed schema samples.

## Acquisition and evidence

The source path is restricted to the existing anonymous HTTPS archive. The inherited transport disables proxies and redirects, discovers no credentials, performs no retries and stops additional network acquisition after 401/403/429. Six annual listings have no pagination; truncation or unexpected paths invalidate the corresponding year, while missing and multiple-object dates remain explicit. Exactly matching January 2024 tail/footer receipts are reused without fallback acquisition. Other samples use bounded Range and If-Match requests, with exact response range, ETag and length checks.

The finite request sequence cannot substitute dates or reuse spare slots. Successful exact reuse makes 50 network calls within the 54-slot ceiling. The plan freezes a 32 MiB cumulative response allowance, 4 MiB footer limit, 4 MiB + 8 response allowance and 30-second network-operation timeout. The charter now correctly records the inherited one-byte overflow sentinel: a failed response/total may exceed the allowance by that byte, which is retained but not admitted. Intent precedes each operation, raw response bytes and hashes follow, and remaining output slots explicitly record not attempted. Abrupt process termination can leave an intent without a response; that is unavailable evidence, not a claim that unrecorded bytes were preserved.

Footer parsing reads metadata only. Required Arrow types, required-column presence, positive row count, projected column offsets and nonoverlap are checked before the schema cell is complete. The reported projected sizes describe sampled footer metadata; they do not prove all-day schema, row validity, transaction completeness, chain canonicality, exact wei or a measured multi-year transfer/memory bound. The reviewed 8 GiB/two-CPU/no-elapsed-kill launcher is reused with its dependencies pinned.

## Independent synthetic verification

The final targeted suite was independently rerun: **29 tests passed**. It covers missing/ambiguous/leap dates, truncation and unexpected paths, footer byte projection, changed January identity without fallback, failed-source denominators, complete fixed acquisition and transaction type drift. Boundary tests cover all 216 ordered three-edge shapes at five boundary placements, 60 invented 18-event sets at four deltas, future invariance, adjacent-window additivity, missing overlap and invalid event identity.

An additional independent fake-HTTP reconstruction exercised the actual inherited Capture transport rather than replacing its methods. Invented Parquet files and complete synthetic annual XML listings produced:

| Scenario | Network calls | Retained outputs | Unavailable lifecycle cells |
|---|---:|---:|---:|
| Valid metadata and exact retained January receipts | 50 | 111 | 0 |
| First response HTTP 403 | 1 | 111 | 30 |
| HTTP 412 on both first-date table tails | 48 | 111 | 2 |

All scenarios preserved 2,192 date/table rows and 24 sample slots. No January replacement call occurred. This independently checks complete processing, denial stopping, conditional-read failures, no retry and output retention without relying only on the author's fixtures.

The boundary helper preserves the extended-window node union, globally unique event IDs and timestamp/event ordering. Integer-vector subtraction `C([D−delta,E)) − C([D−delta,D))` assigns each occurrence to its final-event timestamp in `[D,E)`, with an inclusive delta. Target-day active nodes are a separate statistic; prior-hour-only participants are retained. Supplied coverage bounds remain caller assertions, and the first January 2022 day lacks an audited December 2021 prefix. No actual panel or repeated one-day motif count is authorized by these synthetic checks.

## Exact reviewed bytes

| Artifact | SHA-256 |
|---|---|
| gates.json | `871974b1dea5addd33a3b3c11d86e871e03ae1b15b399be60654eaf989650214` |
| CHARTER.md | `4af7e035125f8b912ef1d427b11eb86064fc43834ef80a46537047c67595d5e6` |
| plan.json | `968183b750153bb166a7a4147a2d2cc50dda8641e26572a624d87a7295cf340d` |
| history.json | `730482a706c9d28328b59870a2760d8a3a42fada9cd8a892ca17ce3612a85bab` |
| audit.py | `60b260603049b83182a2e6f2779d223ee6fa175de85f4bafd085884f5a1a44c7` |
| boundary.py | `c98b619fea3680ff4d5d11207d4ef7fa2cb414fbaf38b52f7b6f221e6b06beff` |
| launch.py | `3a4999159213831db253babfd5ba9d28a735d14e793ba85804cdfdfc8c150db4` |
| preparation-tests.json | `93eae3af385bc837e23261d20df9d02c864b1f9080045572c6bebc83791243e1` |

The gate binds the remaining inherited transport/guard/runtime/dependency/test hashes. Focused verification is proportionate to these additive metadata and boundary changes; a broad unchanged financial suite is not required to repeat. Actual source availability, retained response integrity, terminal completeness and byte-exact import require independent review after execution. Historical publication timing, full panel integrity/scalability, causal predictive value and financial performance remain untested. A complete lifecycle with unavailable cells cannot be described as passing readiness, and this allowance supplies no automatic successor.

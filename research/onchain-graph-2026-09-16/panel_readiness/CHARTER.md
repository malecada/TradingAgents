# Multi-day panel readiness: fixed metadata audit and boundary semantics

September 16, 2026. The user instructed “nice, keep working on it” after the successful one-day 8 GiB benchmark. The next bounded engineering question is whether a fixed three-year archive inventory and sampled schema support planning a causal daily panel. It is not a repeat motif benchmark or a price-prediction experiment. No prices, targets, forecasts, returns, full transaction columns, labels or entity clustering are opened.

## History and allowance

The exact four prior lifecycle claims are the source audit, strict projection prototype, failed 2 GiB motif attempt and completed 8 GiB successor. Each is imported once as prior_attempts 4; one new panel-readiness allowance makes cap 5. This is a downstream metadata/temporal-boundary question, not an extension or reuse of the exhausted motif amendment certificate. Original source/prototype/motif family objects, limits, failed outcomes and receipts remain unchanged. The separately retained static forensic artifact is inspected history, not a fifth prior lifecycle claim. The new family mechanism is `ethereum-multiday-panel-readiness`; lifecycle parent is null because the helper forbids cross-family parents, while four explicit upstream claim/terminal inputs bind scientific ancestry.

All 41 local claim metadata records and available terminal hashes are pinned in history.json. The broader wallet, allocation and DeFi history described in the original source charter remains inherited, with unknown global multiplicity. Local counts are not independent hypotheses. No financial allowance, fresh confirmation, unrelated active run, provider/account action or old failure is reopened.

## Acquisition frozen before metadata outcomes

Use only the already established anonymous HTTPS AWS public Ethereum archive. Six fixed annual prefixes cover blocks and transactions for 2022, 2023 and 2024. Exactly 1,096 calendar dates × two tables = 2,192 date/table inventory cells are retained, including absent and ambiguous dates. Each annual listing requests max-keys 1,000 once; no pagination, retries, substitution or adaptive date selection. Truncation or unexpected paths invalidate the annual inventory. Multiple Parquet objects on one date are explicitly unsupported, not silently reduced to a preferred object. Current listed objects do not establish chain completeness.

Sample exactly January 1, April 1, July 1 and October 1 of each year, for both tables: 24 footer/schema cells. Require the same five block and nine transaction columns/types observed in the admitted January 2024 source. Preserve type drift and missing fields as unavailable. Amounts remain DOUBLE, so exact wei and balances remain excluded. No data pages are decoded. Record footer row counts, compressed projected-column bytes, largest projected row group, range count, object bytes/ETag/last modification and footer hash. These are sampled metadata, not all-day integrity or measured panel memory/runtime.

For January 1, 2024, reuse the four retained tail/footer receipts only if the newly listed key, size and ETag match exactly. A changed object makes that sample unavailable; no replacement fetch. For the other 22 samples, request the last eight bytes and then the declared footer with exact Range and If-Match. Maximum 54 requests includes six listings plus 48 theoretical range reads; normal exact reuse permits only 50 actual calls. Unused slots cannot fund other requests. Freeze 4 MiB maximum footer, 4 MiB + 8 maximum response, 32 MiB total raw-response allowance and 30-second per-network-operation timeout. HTTP 401/403/429 stops further acquisition; 412/range/ETag/length failures are retained without retry. The inherited transport reads one overflow sentinel byte: a retained failed response or total can exceed its allowed byte limit by one byte. This detects and records overflow; it does not admit the response or permit a retry. A prefix retained after a byte/network failure is not a complete response.

Publish request intent before each network operation, response bytes/hash/status/time afterward, and explicit not-attempted records for unused slots. Retain all 108 fixed intent/response output slots plus inventory.json, schemas.json and summary.json. The 30 lifecycle cells comprise six annual inventories and 24 sampled schemas. A completed lifecycle with unavailable cells is not a passing readiness verdict. Exceptions/resource termination preserve partial evidence and prohibit replay.

Execution uses a new detached fixed-source checkout and its own locked Python 3.13.13 runtime. The reviewed 8 GiB sampled process-tree guard, two logical CPUs and no elapsed/CPU-duration kill remain. Freeze and remotely verify the exact source/charter/plan/gate after independent review; preserve execution HEAD through closure. Import all raw/terminal evidence byte-for-byte and independently reparse before reporting source-readiness conclusions.

## Completion-day motif semantics, synthetic only

For a UTC target interval [D,E) and inclusive delta 3,600 seconds, count Local40 over [D−delta,E), subtract counts over [D−delta,D), and align integer vectors over the extended-window address union. This assigns each motif to its final event exactly once. Keep globally unique event IDs and timestamp/chain ordering. An address with no target-day event can still participate in a cross-midnight triangle, so the target-day active-address set is a separate activity statistic, not the output-node filter. Apply subtraction before aggregation, normalization or concentration calculations.

The new boundary adapter is tested only on invented events against direct triple enumeration, including tied timestamps, exact delta, both midnight endpoints, future-event invariance and adjacent-window additivity. These tests authorize no new full-day motif replay. A supplied coverage interval is a caller assertion, not source proof. The first January 1, 2022 day needs December 31, 2021 overlap, outside this audit; omit that first candidate day or separately admit its prefix. Never synthesize missing history as zero.

## Decisions and limits

Passing all inventories and sampled schemas supports a planned multi-day integrity pilot; it does not admit the full feature panel. Missing/ambiguous days, type drift or denied access are retained feasibility findings, without adapting the sample. Any projected multi-year transfer estimate must be labelled an extrapolation from fixed quarterly samples; measured bytes of all listed full objects are a separate quantity. Do not multiply one-day motif timing or peak RAM into a promised full-history bound.

Current modification dates and daily-delivery documentation do not prove historical publication times. A completed-day feature can be consumed only after event-window closure plus admitted finality, publication and computation delays. An assumed conservative lag supports a conditional retrospective replay only. No historically executable or prospective result follows. Future M0 market / M1 ordinary activity / M2 graph comparisons require matched dates, past-only transformations, fixed model capacity, chronological holdout rules and separate source/financial admission.

Primary documentary anchors: [AWS public dataset registry](https://registry.opendata.aws/aws-public-blockchain/), [S3 ListObjectsV2](https://docs.aws.amazon.com/AmazonS3/latest/API/API_ListObjectsV2.html), [conditional/range GetObject](https://docs.aws.amazon.com/AmazonS3/latest/API/API_GetObject.html). No AWS account, SDK, credentials, purchase, provider contact, order, deployment or background schedule is used.

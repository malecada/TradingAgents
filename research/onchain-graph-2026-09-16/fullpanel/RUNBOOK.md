# Full-history extraction operation

The user authorized full 2022–2024 compact feature extraction and previously
requested monitoring/resumption. This finite job includes its independent
closure; matched price/label/model evaluation is a separate next decision.
The completed source-download and seven-day pilot monitors stay paused.

## Fixed execution and observation

Identity: `eth-full-history-feature-panel-20260922`, cumulative allowance 14/14
consumed when claimed. Execution checkout:
`/home/malecada/Data/onchain-research/TradingAgents-onchain-fullpanel`.
Use the exact source in `launch-observation.json` and claim.json, not the current
coordinator HEAD. Interpreter:
`/home/malecada/master_thesis/TradingAgents-audit-fixes/.venv/bin/python`.
The launch observation binds PID, start ticks and boot ID. Check all three and
process descendants before declaring an interruption. Read `compute.log`, the
1096 daily outputs in the identity's research_runs directory, artifacts/checks,
and resource.json. An empty resource receipt means the guard has not recorded
an exit; it is not proof of completion. Normal execution produces a complete
terminal even if scientific cells are unavailable; inspect summary.json.

Two CPUs and 8 GiB sampled aggregate RSS apply throughout, without elapsed kill.
Both filesystems require 20 GiB free. New exact-hash scratch at
`/home/malecada/master_thesis/onchain-fullpanel-hash-scratch-20260922` has a
40 GiB allocated ceiling; Data retained artifacts and daily scratch each have
2 GiB phase-boundary ceilings. Never clean old raw/derived stores. Only the
runner's fully checked new daily scratch can be automatically released.
Expected graph-unavailable edges are 2022-01-01 and 2024-12-31.

## Stop and continuation

A live worker has exclusive ownership: never start a duplicate. Do not replay
this claim, alter frozen source, delete receipts or automatically retry an
identity after terminal completion. A stopped process requires preserved
partial evidence and diagnosis. The user's standing resumption instruction
permits preparation of an evidence-based continuation, but not blind replay;
any distinct allowance, source mapping and retention changes need their own
committed admission and independent review, with completed dates excluded.
Unexpected source/check/graph/disk failures stop later dates explicitly.

## Independent closure

After the compute worker has exited, a complete/failed terminal exists, and
resource.json contains valid JSON, start the frozen final checker exactly once.
Check that no review process, independent-resource.json or independent-report.json
already exists. In the Data checkout, set PYTHONPATH to that checkout and use the
pinned interpreter to run `fullpanel/launch.py --source <exact-source> --review`
under the research/onchain-graph-2026-09-16 directory. Preserve its log and a
separate PID/start/boot observation. Review creates no new claim.

The checker independently reconciles retained daily reports, prefixes, output
and cleanup digests, every source-day hash stream, all global hash bucket
segments, exact duplicates and the complete denominator. The daily independent
raw reconstruction precedes array deletion. Full-day stars/triangles have
bounded-oracle and conservation checks, not exhaustive independent recounts.
A failed review is preserved; never weaken or replay it silently.

Before closure, import compact outputs, prefixes, reports, receipt copies,
logs, resources and lifecycle files immutably to the coordinator, verifying
source/mirror sizes and SHA256 and preflighting disk and Git object sizes.
Keep original scratch digest manifests: their declared recomputable arrays
were intentionally released, not backed up. Global exact-hash scratch remains
until independent closure and a reviewed release; never import 36.4 GiB of
buckets into Git or make a second disk copy. Any interrupted partial evidence
requires separate capacity review before copying. Record import manifest,
result, independent closure, study STATE and exact remote-verified backup commit.
Raw bodies remain local; Git metadata is not an off-device raw backup.

Success requires 1096 independently checked source dates, 1094 admitted graph
dates, precisely the two boundary exclusions, exact global uniqueness and clean
compute/review resource exits. Historical available_at remains null. No model
accuracy, financial return or strategy is established.

## Monitor behavior

A 30-minute thread heartbeat should remain quiet while healthy and unchanged,
notify meaningful progress/failure/completion or needed action, and advance to
frozen review after terminal compute. It should pause after independently
reviewed preservation and closure. The latest STATUS.md and launch observation
supply the exact source and monitor identity after launch.

# Seven-day offline numerical pilot — preparation

The user authorized step 1 on September 22, 2026. The fixed window remains
January 2–8, 2024, with January 1 context and January 9 closing blocks. The
source download is complete and its monitor remains paused. This pilot makes
no network requests and opens no market prices, labels or model outcomes.

Identity: `eth-seven-day-offline-pilot-20260922`.
Planned fixed execution checkout:
`/home/malecada/Data/onchain-research/TradingAgents-onchain-pilot2`.
The original failed pilot and all twelve predecessor claims are preserved.
The explicit one-use numerical allowance is cumulative 12 to 13.

The offline adapter uses exact hash-bound retained captures. The unchanged
numerical definition is supplemented by independent raw reconstruction, every
new shard/manifest check, complete dyad recounts and bounded full-role oracles.
Known missing/invalid prerequisites remain unavailable in the 17-cell grid.
Resource guard: 8 GiB sampled process-tree RSS, two CPUs, no duration kill.
Derived allocation is checked against 8 GiB between phases; 20 GiB free disk
must remain. Scratch projections are temporary and remain on Data.

Commit source/gate/charter/approval and verify the remote branch before creating
a detached execution checkout. Compute through `pilot2/launch.py --source HEAD`.
Only after its terminal and resource records exist, invoke that same frozen
launcher with `--review` for the independent check. Neither invocation may be
replayed on an already-existing output identity. Preserve every partial artifact
if interrupted, and reconcile a distinct continuation before any retry.

At closure retain lifecycle files, full derived-artifact manifest, independent
report/resource records and exact source references. Do not copy or delete raw
stores. Verify source-to-mirror hashes before Git backup and remote verification.
Full-panel processing and forecasting are outside this seven-day step.

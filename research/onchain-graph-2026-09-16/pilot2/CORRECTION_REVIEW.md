# Independent activity-annotation correction review

Decision: approve one separately committed, guarded completion of the retained
pilot's independent verification using `check_activity_v2.py`. The original
compute, claim, terminal, frozen checker and failed first review remain unchanged.
This approval creates no new numerical experiment or attempt allowance. Final
contract hashes, committed source and remote identity must pass before execution.

The material defect is the unconditional whole-dictionary assertion at frozen
`check_independent.py:264`. Production `prototype/graph_math.py:49` includes a
constant `qualification` annotation; the independently reconstructed summary
omits that field. All eight retained activity objects have exactly that one
additional key, with the expected text and string histogram keys. A synthetic
three-event graph reproduces the failing comparison despite equality of all
shared fields. This establishes a checker schema defect, not real-data numerical
agreement. No fresh raw decoding was needed for diagnosis.

The wrapper pins the original checker SHA-256
`3809fbe4b0b7605c265076b7b41466b9b164df79f233eeec0d2d0c1a465735ac`
before importing it from the declared execution root. It wraps only the
independent `static_summary` return, adding the exact frozen annotation literal.
It does not change actual outputs, numerical values, source admission, identity
checks, comparisons or the original gate. An unexpected annotation already
present in the independent summary is rejected. Strict whole-dictionary equality
therefore still rejects omitted fields, arbitrary extra fields, altered
annotations and numerical discrepancies.

The frozen main retains its original execution-root `HERE` and all source,
input, claim, terminal, artifact and numerical checks. Its report's
`reviewer_script_sha256` identifies the correction wrapper through an explicit
`__file__` assignment; the original checker remains separately hash-bound both
by the wrapper constant and original claim. An existing destination report is
rejected before loading the checker, with exclusive publication retained at
completion. The interface is `--root EXECUTION_ROOT --source ORIGINAL_SOURCE
--report EXECUTION_ROOT/research/onchain-graph-2026-09-16/pilot2/independent-report-v2.json`.

The inspected `ACTIVITY_CHECK_CORRECTION.md` and `launch_activity_v2.py` preserve
the failed first review and original source. The controller checks its committed
contract, new-source bindings, exact frozen execution HEAD and retained evidence
hashes before loading the guard. Its contract must explicitly include both guard
modules loaded from the execution checkout. It invokes the unchanged 8 GiB
sampled process-tree guard, two-CPU affinity and Data scratch, with no elapsed
kill. The new report/resource names are exclusive; no compute runner or lifecycle
claim is invoked. The retained original checker does no network acquisition.

Nine synthetic correction tests passed in 0.29 seconds. They cover the reproduced
schema failure and corrected equality, missing/altered/extra annotations, a
numerical fault, unexpected expected-side annotations, changed original code,
existing report preservation, and separation between the frozen execution root
and the actual wrapper report identity. The combined four pilot test files then
passed 47 tests in 2.29 seconds. The broad 2,311-test / 97-subtest result remains
the earlier baseline; it was not repeated for this schema-only correction.

No empirical checker was rerun while preparing this review. Real retained-byte
numerical equality, seven-day independent closure and the new guard result remain
pending. The scope still excludes independent exhaustive full-day stars/triangles,
canonical-chain proof, historical publication-time admission, financial costs,
returns, predictions and strategy validation. Any further failure must remain
evidence and must be diagnosed without silently relaxing checks. No unresolved
correctness question currently requires a higher-effort review.

Reviewed wrapper SHA-256:
`014b58dffa585652b1749425f5c02d43111ca5d73c01fdab2fccd2f789f3f4cf`.
Reviewed controller SHA-256:
`7d10e34e6ef23cfe124f819294acf6c356c735fc734bad487fbc8bb317abdac8`.

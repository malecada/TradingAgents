# Offline seven-day numerical continuation

The user selected step 1 on September 22, 2026: validate a seven-day pilot before
full-panel processing or predictions. This single new claim completes the frozen
January 2–8, 2024 UTC numerical question after the old pilot was interrupted.
January 1 supplies context; only January 9 blocks supply the closing boundary.
The failed old identity and all twelve ancestor claims remain immutable. This
explicit continuation grants one numerical engineering attempt, cumulative
prior 12 / cap 13, preserving the old pilot cap 6 and source cap 12. This is not
a financial trial, fresh holdout, new acquisition permission, or automatic retry.

## Frozen input and computation

No network access is permitted. The plan binds original January 1 receipt inputs
and exact metadata manifests for January 2–9. January 2–5 captured ranges are
selected from bulk-reuse.json (January 5 raw acquisition had finished even though
its old decoding did not). January 6–8 reuse the complete resume3 captures;
January 9 reuses the original closing-block capture. Absolute external source
directories are declared explicitly; manifest members must remain confined to
them. Every selected stored/raw body, length, receipt, object ETag, conditional
range, projection and schema is verified before decoding. No raw store is
modified or replaced, and the completed storage benchmark is not rerun.

The unchanged nine-column definition and numeric.py perform transaction identity,
block hash/height/timestamp/index/count, per-day uniqueness, order and category
checks. Exactly literal recipient `None` is normalized and counted. DOUBLE value
is only an eligibility flag, never exact-wei accounting or monetary inference.
Every January 1–8 transaction hash must be unique across the eight-day population.
All eight sources must pass; missing coverage makes dependent cells unavailable.
Adjacent block height, parent hash and strictly increasing timestamps must link
both ends of every target day. Internal archive consistency is not independent
canonical-chain verification or historical publication-time admission.

Retain the original Local40 definition: positive, successful, non-self native
transfers; integer-second event time and `(block_number << 32) | transaction_index`
order; inclusive 3,600-second triple span. Completion-day vectors are counts on
[D−3600,E) minus [D−3600,D), retaining overlap-only nodes. Isolated-day counts are
a diagnostic for cross-midnight contributions. Each day retains local vectors,
role sums, squares, maxima, nonzero counts, concentration and ordinary activity.
Nonnegative subtraction and role conservation are required. The fixed bounded
oracle uses top three shared boundary addresses by minimum incident count,
address tie break, last 15 prior/first 15 following events; direct enumeration
must match. No date/parameter selection or expansion follows outcomes.

## Resources, lifecycle and independent closure

Use the pinned Python/lock environment, 8 GiB sampled aggregate process-tree RSS,
two CPUs and no elapsed-time kill. Separate per-day subprocesses release memory.
New derived artifacts have an 8 GiB phase-boundary allocation ceiling; the 20 GiB
free-disk floor is checked before each phase. This is not an atomic per-write disk
quota; a phase that overruns remains evidence and blocks later phases. Temporary
sparse projections reside on Data and are explicitly recomputable scratch, not
raw evidence. Canonical raw and all derived scientific outputs remain preserved.

All 17 cells (context, boundary, seven source, uniqueness, seven motif cells) and
20 registered JSON outputs remain in the denominator. Known prerequisite failures
produce unavailable cells; unexpected failures retain the claim and failure
receipt. No same-identity replay is permitted. Source, charter, exact inputs,
lineage, amendment and independent approval are committed and remotely verified
before claiming in a new fixed Data checkout. No unrelated family is modified.

Independent closure runs under the same 8 GiB/two-CPU guard after terminal
publication. It re-decodes all eight days from retained raw bytes independently
of production numeric code, reconciles events/activity/hash identities/boundaries,
checks every new shard and manifest, and independently recounts completion-day
dyads and fixed bounded full-role subsets. Full-day star/triangle counts retain
bounded and consistency evidence, not an independent exhaustive recount.
Success requires all seven target-day numerical/boundary/count cells available
and independent closure passing. Otherwise state the precise failed requirement.

Source-only structural checks do not constitute this pilot's numerical success.
Any scientific result is retrospective engineering on already exposed data.
No prices, targets, models, prediction accuracy, costs, return/PnL claim, paid
resource, external contact, orders or production change is included. The download
monitor stays paused. Full-panel processing and matched prediction evaluation
remain subsequent steps. Preserve exact outputs/reviews and push metadata and
bounded derived artifacts using the existing backup route; verify the remote
commit, and never equate that with a verified off-device full raw backup.

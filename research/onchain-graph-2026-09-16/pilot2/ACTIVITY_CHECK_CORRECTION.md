# Independent activity-schema correction

The original numerical run remains complete from source
`d6b556de45859a1cdd098ad69aad7e161caad96c`, with all 17 cells and 20 outputs.
The first independent review failed at whole-activity-dictionary comparison,
before establishing numerical correctness. Its original source, log, resource
receipt and all scientific outputs remain immutable. It is not a passing review.

Static inspection and a synthetic example identify one schema discrepancy:
production includes a constant explanatory `qualification` field; the independent
static summary reconstructs all numerical fields but omits that annotation.
All eight retained activity objects have exactly the additional expected key.
That observation does not prove the as-yet-uncompleted raw numerical comparison.

The correction is a separate, precommitted independent-check wrapper that adds
only the exact frozen annotation to the independently computed summary. The
original whole-dictionary equality remains, rejecting missing or extra fields,
changed annotations and every numerical discrepancy. No production algorithm,
raw data, source date, event, count, prior gate, budget or scientific output is
changed. This is completion of the authorized pilot verification, not another
numerical claim, family allowance or fresh sample. All history remains spent.

The correction contract binds the complete claim/terminal, first resource and
failed-review evidence, original checker, new wrapper/launcher/review and this
charter. Commit and verify the remote source before execution. The corrected
report is a new exclusive `independent-report-v2.json` and the guard publishes
exclusive `independent-resource-v2.json`; the original names cannot be replayed.
Use the unchanged original guard, 8 GiB sampled process-tree RSS, two CPUs and
Data scratch, without an elapsed-time kill. The wrapper verifies the original
source/inputs and all original numerical checks. No data acquisition is allowed.

Success requires the new report's numerical_pilot_success:true and clean guard
exit. Any new failure is retained, diagnosed and described without silently
weakening assertions. Closure must explicitly disclose the original check failure
and this schema-only correction. Independent full-day dyads, source reconstruction,
bounded star/triangle subsets and artifact checks retain their original scope;
no full-day exhaustive star/triangle recount, canonical-chain/publication-time
admission or prediction/financial result follows.

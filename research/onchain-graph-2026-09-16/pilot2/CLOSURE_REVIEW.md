# Independent final closure review

Decision: pass the fixed seven-day numerical engineering closure, with the
published scientific limits. No material discrepancy remained after clarifying
that the production extraction/counting run was not replayed; the independent
reconstruction was rerun under the separately committed schema correction.
This closure inspection did not rerun either checker or decode raw transactions.

All 755 members of `closure-import.json` were independently hashed at both the
original Data execution root and coordinator mirror. Every byte length and
SHA-256 matches, totaling 466,621,203 bytes. The manifest has no duplicate paths.
All 755 files are present in the Git index with matching Git blob identities,
including the 16 phase-result JSON files otherwise excluded by the default
ignore rule. The scientific artifact tree has exactly the registered 723 files
and 465,008,462 bytes. Its 707 compressed members match the report's roundtrip
denominator, with no unbound blobs reported. This inspection checked stored
bytes; the retained corrected report supplies decompression/numerical evidence.

The execution checkout remains at
`d6b556de45859a1cdd098ad69aad7e161caad96c`. Claim source/design, original gate,
terminal, resource report and corrected independent report agree. All 37 source
and six runtime bindings still match the execution files. Terminal membership
contains exactly 17 complete cells, zero unavailable cells and 20 output hashes;
every output agrees with both the terminal and corrected independent report.

Correction source `fae09ecf519f59d21cf7b70da95c3ed748d1d046` contains the exact
correction contract and every bound new-source object. All contract-bound
execution evidence and both guard modules also match. The v2 guard binds that
correction source and contract, while the report identifies the actual wrapper
SHA-256. The original frozen checker, first failing review log and nonzero guard
receipt remain intact; no original passing report was manufactured. The v2
report records `passed: true` and `numerical_pilot_success: true` for seven days.

The `RESULT.md` table was independently reconciled against retained source,
feature and independent-check JSON. All seven transaction/event totals, motif
totals, two-decimal boundary shares and one-decimal phase times match. The totals
are 8,626,480 unique transaction hashes across eight source days, 1,574,715,401
motif occurrences across seven completion days, and 29,769,808 cross-midnight
occurrences (1.89%). Cross-day uniqueness lists exactly January 1–8 with no
unavailable or duplicate days. Each independent day report records a complete
dyad recount and three bounded full-role subsets; full-day star/triangle recount
remains explicitly false.

Compute and corrected-review guard receipts respectively report 537.20 and
251.66 seconds, peak sampled process-tree RSS 2,043,731,968 and 1,723,760,640
bytes (1.903 and 1.605 GiB), exit zero, no limit reason and the fixed 8 GiB cap.
The reported minute conversions and artifact MiB conversion reconcile. The
guard source preserves two-CPU affinity and no elapsed-time kill; the earlier
startup observation independently checked active scientific-process affinity.
No compute, source/count worker, original reviewer or corrected reviewer process
was present in the process inventory at `2026-09-22T09:10:39.537598+00:00`.

The result appropriately limits success to retrospective engineering for this
fixed week. It does not infer full-history worst-case performance, a full-panel
duration, fresh confirmation, independent observations from overlapping motifs,
prediction accuracy or financial value. Independent exhaustive full-day stars
and triangles, canonical-chain validation and historical publication timing
remain untested. Prices, labels, fees, funding, returns and trading feasibility
were not evaluated. Full raw-history off-device recoverability is not established.

The final branch push and remote verification are the remaining publication
steps after this local review. Staging alone is not an external backup. Existing
failed attempts, original limits and the consumed cumulative 13/13 allowance
remain preserved; this closure authorizes no restart or subsequent experiment.

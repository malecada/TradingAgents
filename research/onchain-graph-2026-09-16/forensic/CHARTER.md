# Frozen offline forensic graph reconstruction

This is a diagnostic closure check supporting the user's authorized one-day prototype. It is not a new lifecycle experiment, family-budget extension, successful rerun, financial trial or admitted feature panel. The original eth-graph-prototype-20260916 remains COMPLETE with117complete and2unavailable cells; its2/2allowance is consumed and unchanged. The source family1/1 and all other grants remain unchanged. No method is renamed to reset a budget. The complete retained39local claim inventory remains; no40thclaim is created. Record this post-hoc check separately and retain both raw and normalized counts.

Independent closure identified exactly777to_address values equal to the literal string `"None"`, with no other invalid recipient values. This is consistent with the pinned AWS producer's string conversion, but individual contract creation and deployed producer revision are not proven. See prototype/CLOSURE_REVIEW.md for source links, counts and limitations.

Before calculating any diagnostic graph summary, commit this charter, exact code and contract.json. Read only the same117retained range-response files, footer, block sample, plan and bound parent evidence. Zero network requests, new source fields, alternate windows, prices, monetary aggregates or model selection. Every input byte and source file is SHA256-bound. Verify the original parent run structurally before processing. Preserve the source/input hashes at start/end, exact source commit, an exclusive start receipt, result or failed receipt and separate resource receipt. Never overwrite or repeat the same forensic artifact directory.

Normalize only exact case-sensitive `"None"` in to_address to PythonNone. No trimming, general null-string conversion, other field changes or sender normalization beyond the already frozen originalhex-case rule. Preserve all777affected rows in the denominator and exclude every one from edges; label them missing recipient, not proven contract creation. All other original row/block/identity/order checks and exclusion precedence remain unchanged. Expose the original raw integrity report alongside normalized diagnostics. The observed777count is a frozen forensic consistency check, not a tuned performance threshold.

Reconstruct the same sparse temporary projection with the same acquired-interval reader, footer validation,2GiBlogical/128MiBcolumn+4MiBfooter limits and row groups. Reuse the original unchanged graph math for static count/topology metrics and fingerprint. No temporal motifs, monetary weights, balance reconstruction or predictive/economic claims. If normalized integrity fails, graph metrics remain absent. Even if checks pass after normalization, label outputs diagnostic_only/source_admitted_feature_panel=false; they cannot override the original gate or supply confirmation data.

Execute once in a dedicated detached fixed-HEAD checkout with local lockedPython3.13.13/uv.lock, two logical CPUs and reviewed sampled2GiBmemory-only guard, no elapsed/CPU-duration kill. No other job is inspected or changed. Synthetic exact-sentinel cases and no-network sparse reconstruction must pass; independent review of this boundary/code is required before execution and raw/result reconciliation before reporting. The already passed named offline2060tests+81subtests is inherited for unchanged core code; run all three focused graph test modules for this narrow adapter. Do not repeat the expensive full suite without an additional material reason.

Interpretation is limited to whether an explicitly normalized, fixed address subgraph can be reconstructed computationally from the captured day. Historical publication, independent canonicality/day-boundary completeness, full nativeETH flow, temporal motifs, multi-day feasibility and forecast/trading value remain unadmitted. Any later empirical experiment requires its own separately authorized and frozen admission route; this artifact supplies none.

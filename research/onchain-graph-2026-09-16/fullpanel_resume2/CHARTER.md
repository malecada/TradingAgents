# Continuation after the recorded memory-pressure kill

The standing user instruction authorizes monitoring and resuming the existing
2022–2024 numerical feature extraction after interruptions. This successor is
`eth-full-history-feature-panel-resume2-20260922`, cumulative prior 15 / cap 16.
It preserves all fifteen predecessor claims and terminals, their unchanged
criteria and exposed sample. It is one operational continuation, not another
financial hypothesis, a fresh sample or a change to old attempt budgets.

## Evidence and the single resource correction

The immediately preceding continuation restored all 206 daily outputs and
entered graph counting for 2022-07-26, but published no newly completed date.
At 2026-09-22 18:05:58 UTC, systemd-oomd killed its service because the user
ancestor's memory pressure exceeded 50% for over 20 seconds with reclaim.
The recorded pressure was 52.41%. Its final guard recorded 16,136 memory.high
events, no memory.max or kernel OOM events, a 4.142 GiB sampled cgroup peak
and approximately 7.318 GiB host MemAvailable. The 4 GiB lower throttle likely
contributed to pressure; other processes' contribution is not separated.
The earlier reboot's cause remains unknown.

The successor raises only the local memory.high watermark from 4 GiB to the
existing memory.max of 6 GiB. The 6 GiB kernel hard limit, 512 MiB swap maximum,
3 GiB running host reserve, 9 GiB startup requirement, two CPUs, two-thread
library settings, 15-second monitor lease and whole-unit cleanup are retained.
No system-wide oomd configuration, exemptions or persistent services are changed.
Optional unit/ancestor PSI, anonymous memory, file cache and swap observations
are added. Missing optional telemetry is explicit; it does not substitute for
the mandatory kernel controls or initial/terminal OOM evidence. No new pressure
abort threshold is introduced. The absence of throttling below the hard limit
is intended to avoid the observed pressure mechanism. Success is unproven:
July 26 has materially more eligible events than July 25 and the exact graph
counting substage and unrestricted working set were not measured.

## Unchanged numerical and preservation contract

The original fullpanel/plan.json and numerical source remain byte-identical.
The original fullpanel charter's eligibility, Local40 definition, boundary
rules, independent daily reconstruction, source provenance limitations and
expected populations remain binding: 1,096 source dates, 1,094 required graph
dates, 2,193 cells and 1,099 outputs. The expected transaction population is
1,221,389,903 rows. Only 2022-01-01 and 2024-12-31 are expected unavailable graph
dates. Historical available_at stays null. Full-day stars and triangles have
bounded independent subsets and conservation checks, not exhaustive recounts.

The frozen preceding continuation pipeline is reused through a thin wrapper
with hash-bound dependencies. Only its identity and output namespace change.
Every old day, raw capture, attempt and partially written artifact is preserved.
The new archive binds all 1,747 stopped-run files. Its exact 1,623-file seed
contains 206 published outputs, 593 prefix files, 206 checks, 206 cleanup records
and 412 hash receipts. Outputs remain byte-identical through both predecessors.
The 112 unpublished July 26 source shards and three orphan prefixes are retained
outside the execution seed. New extraction starts July 26 and processes only
the remaining 890 dates after metadata republication; the previous startup
spent approximately 12.5 minutes on this source-checked restoration.

The original hash root owns indices 0–205 and remains immutable. The stopped
first continuation's separate hash root is empty and remains preserved; it is
explicitly excluded from the numerical union only while independently verified
empty. A fresh resume2 root owns indices 206–1095. The exact full-32-byte union
and independent alternative sort retain the complete source population,
including graph-unavailable edges and reused pilot days. Any missing source,
unexpected daily failure or duplicate blocks whole-panel admission; every
remaining cell stays explicit. No old hash payload is copied to disk or Git.

Original disk ceilings remain: 20 GiB free on both partitions, 2 GiB retained
new artifacts/metadata including the new preservation archive, 2 GiB daily
scratch and 40 GiB combined participating hash roots. Pre-existing predecessor
archives remain on disk and are reflected in free-space floors. Combined
corresponding hash buckets are bounded to 256 MiB and chunks to 32 MiB; new
append capacity subtracts the old payload. Only newly completed daily scratch
may be released under the unchanged exact-manifest contract. Failed partials,
prefixes, compact reports, receipt copies and global hashes remain preserved.

## Release and conclusion

Before claiming, commit the charter, exact gate/plan/history, source/test and
input bindings, single amendment, named offline verification and independent
release approval. Verify the exact source remotely and admit the exclusive
Data resume2 checkout. Never replay a claimed or terminal identity. The first
continuation's failure-only terminal and frozen independent failure review
remain evidence of a failed attempt, not numerical success.

The finite process remains under the user's existing 30-minute monitor.
Success requires every original numerical criterion, clean mandatory guard
evidence and independent final closure. A further resource stop is retained
rather than hidden or retried under this identity. Compact evidence is imported
immutably, committed and remotely verified at closure; global hashes remain
until reviewed release. Raw stores are local on the same physical NVMe and
have no verified off-device full backup.

No source acquisition, prices, labels, models, paid services, trading or
production mutation is added. Historical availability, paper-level accuracy,
financial returns and a validated strategy remain unadmitted.

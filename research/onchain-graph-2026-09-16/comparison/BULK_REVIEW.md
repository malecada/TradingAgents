# Independent remaining-source preflight review

September 16, 2026. Scope: one finite, raw-only download of the remaining
2022–2024 graph source dates to a new Data checkout. The operative instructions,
graph state, bulk charter, day capture, runner, launcher, independent checker,
reuse inventory and cumulative history were inspected directly. No network
requests, numerical graph reconstruction, prices, labels or model outcomes were
opened by this review. This review does not change registrations or ledgers.

**Final preflight conclusion:** no unresolved material blocker was found for
this fixed remaining-cohort raw acquisition after the corrections below. All
final gate/input/source bindings passed independent review. Commit the reviewed
tree, verify its remote identity and perform metadata admission from the new
fixed execution checkout before invoking the finite starter. A raw capture
cannot admit a numerical panel or establish historical publication availability
or predictive value.

## Independent checks

The calendar was independently reconstructed: 1,096 dates in 2022–2024 minus
12 retained source dates equals 1,084 new dates. The exclusions are January 1–7,
2022 and January 1–5, 2024. Every one of the 2,154 referenced reuse members
matched its retained size and streaming SHA256. These members include
1,293,321,848 bytes of raw storage (including the old body-bearing JSON receipts)
and 1,254,076 bytes of other listed metadata, consistent with the conservative
2 GiB raw baseline. January 9's separate block receipt and stored/raw hash
references also matched. No old raw source is silently replaced or its failed
numerical status relabelled by these checks.

Ten focused synthetic bulk tests independently passed in 8.56 seconds. They
exercise a full 32-row-group day with all 291 requests, incremental accounting,
denial and low-space latches, storage admission, retained paths, block reuse,
metadata tampering and fixed schema preservation. No actual requests are made
by those tests.

Day acquisition reserves space before requests, charges actual stored raw file
lengths and allocated metadata estimates, and checks every immutable
intent/receipt/body binding. A shared stop survives later disk recovery. There
is no retry loop or automatic reuse of an existing date directory. New dates
remain explicit unavailable cells after a stop. An interrupted claim and partial
day require failure closure and a separately reviewed continuation; relaunching
this identity is not supported.

The independent `check_bulk.py` imports the reviewed standalone raw checker,
not production capture/storage/projection helpers. It reconciles each day's
manifest, all raw/stored hashes, exact received/request denominators and graph
column ranges reconstructed from the retained footer. January 9's reused block
is independently hash/path checked before inclusion in the verification view.
Checker failure preserves the original capture record nested in an unavailable
lifecycle output. The checker handles at most one bounded source day's raw
payload at a time; it does not retain the whole corpus in memory.

The new source family imports seven exact predecessors once and proposes one
remaining-cohort allowance at prior 7/cap 8. Old exhausted families, failures,
spent financial exposures and first-tranche identity remain unchanged. The new
Data location is local storage on the same physical NVMe. Existing remote
backup of the first tranche is not a backup of this future bulk corpus. The
explicit absence of a bulk off-device backup is preserved as a limitation.

## Findings corrected during preparation

1. Lifecycle result files, index and summary initially bypassed the shared
   metadata accounting. The runner now charges a dedicated 128 MiB lifecycle,
   stdout and guard reservation up front, in addition to 128 MiB prior metadata.
   Individual results are bounded to 16 KiB, the index to 32 MiB and summary to
   1 MiB. This retains room for unavailable-date reporting after an acquisition
   stop. Day manifests and request metadata remain charged separately.
2. The January 9 reuse manifest contained a raw-hash field beyond the helper's
   four-field provenance contract. The runner now passes the exact four fields;
   the additional raw hash stays bound in the reuse input and is independently
   checked. Without that correction, the retained block would have been rejected
   and that date unnecessarily marked unavailable.

## Final gate and starter review

The final gate SHA256 is
`3c0544be6b5c83e1192255c947ded86102e06a4deff24f2057f26c8d2b3f2c34`.
All 36 source-file hashes, 21 input hashes and the charter hash independently
matched the retained files. The source-plan and inventory paths match the paths
used by the independent checker. The exact calendar yields 1,085 unique cells
(1,084 dates plus index) and 1,086 unique outputs (including summary). The seven
unique parent identities and all 14 original claim/terminal hashes reconcile;
the new family remains prior 7/cap 8.

Five additional independent-checker integration tests passed separately in
1.09 seconds. They verify ordinary and reused-January-9 synthetic captures,
detect altered bodies/manifests, and preserve unavailable source status despite
valid denial evidence. The latest ten day-capture tests also passed again in
8.19 seconds. These are synthetic checks only.

`bulk_start.py` performs admission, rejects an existing claim directory and
exclusively creates its launch intent and progress log before spawning exactly
one finite guarded launcher. A second starter cannot overwrite the intent/log;
the launcher independently reserves its resource receipt and ResearchRun claims
the identity. No scheduler or restart loop exists. The launch receipt records
PID and Linux process start ticks, so status inspection can detect PID reuse.
Creation failure preserves the intent rather than permitting an implicit retry.
The starter's return is not evidence of acquisition: the fixed source claim,
matching live process and first independently checked dated output must be
inspected before reporting operational progress. Neither actual bulk acquisition
nor a starter invocation was performed by this reviewer.

## Limits of the review

The final raw ceiling is 120 GiB including its prior-storage baseline; metadata
is capped at 2 GiB including both prior and reserved lifecycle usage. Per-request
free-space checks include the 20 GiB floor and working reserves. These are
serial accounting checks, not a reservation against unrelated disk writers.
Publication can still fail under real I/O failure or exhaustion; the lifecycle
record must preserve the unavailable status and missing-manifest distinction.

Actual remaining-date availability, future capture rates/size, machine crash
recovery, external bulk backup, canonicality/day boundaries, duplicate and
overlap semantics, full-panel numerical resources, motif features and forecast
inference have not been tested. No fees, funding, return/cashflow convention or
profitability result is produced by this operation. December 31, 2021 context
remains a separate boundary prerequisite. No higher reasoning effort is needed
for the reviewed code questions; remaining admission and closure checks require
the actual final metadata and later retained evidence.

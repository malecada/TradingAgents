# Fixed matched comparison operations

**Closed compute and independent review: do not replay either command.**
The single comparison completed all 77 cells/36 fits/366 predictions and passed
independent checking. Its statistical screen is negative. Execution source is
baa8129ed58096a202f5b909777eacc736c8631c. Read RESULT.md, independent-report.json,
evidence-manifest.json and the terminal resource receipts. Historical launch
instructions below describe the completed attempt and supply no new allowance.
Reviewed preservation/remote-backup closure is complete, recorded in backup.json.
All62manifest artifacts match remotely verified commitb0936674e6f2913b2805c1ea769293c2b439bf69.
The completed monitor was deleted; no automatic continuation remains.

Graph computation, global identity reconstruction and independent full-panel
review are terminal and never replayed. Full retained graph evidence is backed
up at d785a49824a02957399022eb732ff0d89de28189; exact remote observation is in
../../fullpanel_resume2/full-import-20260924/backup.json. All original raw/hash
roots and the original Data execution checkout remain immutable.

## Current preparation

The runner and independent checker passed synthetic tests and a second
independent source review. Four initial preservation/admission findings and
corrections are retained in REVIEW_HISTORY.md. The named offline suite passed2,746tests and97subtests under
verification-resources; its once-only launcher was
/tmp/verify_matched_release_20260924.py in the coordinator's pinned Python.
Use live.json, child.log and final.json there to observe it. Do not duplicate it.
verification.json and verification.log were published after the clean guard
closed, with identical before/after source hashes. This is synthetic engineering, not a claim.

Gate-draft.json is deliberately incomplete and must never be admitted. Bind the
passing verification receipt/log, independent RELEASE_REVIEW.md, approval and
single additive amendment into gates.json. The contract hash excludes only the
three mutually referring release objects. All131 input identities, sources,
charter,77cells,38spotmonths,16priorclaims/4failures and spent windows must be
reviewed. Commit and push the final release, verify exact remote HEAD, then
record metadata-only admission before observed prices/labels or fits.

## Once admitted

Use the active coordinator checkout directly, with its exact pinned
.venv/bin/python and committed source SHA. The command is
`.venv/bin/python -B research/onchain-graph-2026-09-16/comparison/evaluation-20260924/launch.py --source SOURCE`.
The launcher validates the locked local runtime, registration and history before
claim and runs a unique transient user service. Compute receipts are under
resources/compute; no existing receipt directory or claimed run is reused.
Preserve6GiBhigh=max,512MiBswap,twoCPUs,9GiBstartup/3GiBrunning RAM reserves,
15secondlease and20GiBfree on both partitions. There is no workload duration kill.

ResearchRun claims eth-matched-direction-20260924 once. The eight registered
JSON outputs and immutable fit-checkpoints live under that single research_runs
entry. Partial fits are checkpointed before prediction/another fit; failed
checkpoint storage halts later fitting. Inference failures retain forecasts
and descriptives but do not retry or become a passing screen. No tuning,
additional fits, download or neural follow-up is authorized.

After one unique terminal and compute guard final exist, invoke the same
launcher once with `--review` added, from the same committed source. Its own
resources/review guard contains the independent checker. A qualified failure
review is distinct from a passing complete comparison. Interrupted claims with
no terminal require explicit forensic closure of the existing attempt before
review, never automatic retry or removal. Missing startup capacity is not a
failed empirical trial if no claim exists; preserve its preclaim guard evidence
and assess a new operational receipt namespace before any later launch.

## Closure

Reconcile all77cells, all8outputs, all fit checkpoints, the fixed366-test-day
coverage and both resource guards. Independently verified negative screening is
still a valid completed result; unavailable inference stays unavailable. Record
accuracy descriptively and M2−M1 log loss/paired interval/negative-month count
against the original rule. Historical availability is assumed; the sample is
spent; no profitability or replication claim follows. Preserve errors and all
attempted/partial cells, commit/push and verify the remote head. Update study
STATE.md and report the result. Retire the monitor after reviewed closure or a
terminal decision. A neural experiment requires a separate decision.

# Second continuation closure

`eth-graph-source-resume2-20260918` closed2026-09-18T00:43:56.188451UTC with
zero complete and446unavailable dates. The13successful October8responses were
reused, but four new TLS/read-timeout attempts failed;16,833network bytes were
retained and445later dates had zero requests. The terminal receipt SHA256 is
`215b393be985bcc1fcebc5e470815b3ec1ea45703c7daaf157086821f328733f`.
All447cells and448outputs are preserved. This lifecycle closure is not coverage.

The guard exited0 without a resource limit after1355.433083seconds; sampled peak
RSS270139392bytes. Both old launcher/worker are gone. All448output hashes,
446pairs of logical/physical manifests and listed source files were independently
reconciled. The1345metadata imports total2615154bytes and match exact sources;
see resume2-closure/import.json. Raw bodies remain in their immutable Data store.

Final logical family accounting is66588262965raw bytes and1211813888metadata
bytes. The new63GiB/1160MiB prior baselines exceed these values. The already
charged128MiB control reservation is separately audited before reuse: closed
runtime/log files and coordinator lifecycle/closure/startup mirrors consume
11067392allocated bytes across1805files. That scope excludes static Git checkout
replicas, which consume physical disk space separately under the free-space floor.
A TLS-only check at00:55:19UTC succeeded without HTTP/data; this is transport
health at one instant, not proof of future download success. Numerical/prediction
work and verified off-device raw backup remain absent.

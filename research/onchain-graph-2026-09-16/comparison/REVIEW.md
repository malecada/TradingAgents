# Independent pre-capture review

September 16, 2026. Scope: the first seven 2022 training-source days and 38
ETHUSDT spot archive/checksum pairs, plus the prepared matched comparison.
The operative instructions, current graph state, governance/provenance skills,
protocol, capture charter, gate, plan, six-parent history, source adapters,
runner, launcher and inherited storage/projection helpers were read directly.
No network requests, observed prices, graph reconstruction or experiment replay
were performed for this review.

**Admission conclusion:** no unresolved material blocker was found for this
bounded raw-input capture after committing the final source and matching gate
hashes. This is not admission of feature data or model evaluation. Preserve the
fixed source HEAD, inspect the named offline verification result, and use only
the reviewed launcher. A completed source result requires independent closure
verification; the raw capture itself is not a financial experiment.

The gate retains six exact predecessor claim/terminal pairs, including the
failed pilot, and grants one distinct downstream allowance at prior 6/cap 7.
The captured 2022 dates do not replay the interrupted 2024 pilot. The 46 cells
retain all seven dates, all 38 months and the manifest cell. Existing ETH
forecast exposure is disclosed as spent. The initial gate check independently
matched all 27 initial source references, 15 input references and charter bytes.
The final check including the independent checker matched all 28 source
references, all 15 inputs and the updated charter without a mismatch.

The graph request ceiling is 2,037 with 1,904 MiB received bodies; spot adds at
most 76 requests and 16 MiB. Conditional ETags, exact byte ranges, immutable
intent/receipt/blob publication, roundtrip hashes and source-specific denial
stops were inspected. Graph acquisition latches a low-space stop above the
20 GiB floor with 544 MiB reserve. Spot subsequently gained its own latched
floor stop. These checks bound this process; they cannot reserve space against
unrelated concurrent disk writers. The launcher uses exclusive resource-receipt
creation and the inherited aggregate 8 GiB/two-CPU guard. Lifecycle admission
and exclusive capture-directory creation prevent a same-identity replay.

The capture runner's manifest uses the same storage reader as acquisition.
Consequently a separate `check_capture.py` was added for closure: it imports no
production capture or lifecycle modules, checks raw/stored zstd hashes, intent
binding, complete manifest membership, all 46 source cells, archive checksums
and calendar timestamps, and independently reconstructs graph column ranges
from retained Parquet footer metadata. It never parses price/volume fields or
decodes transaction rows. A successful checker result remains capture integrity
only; post-capture execution and review were pending when this document was
written.

## Review findings resolved before capture

- `model.py:55`: late feature clocks originally aborted the whole comparison,
  contrary to the common-row exclusion rule. They now preserve explicit late
  exclusions shared by all arms. An independent synthetic March 1 example
  verified use of February 28 inputs and invariance to later close mutations.
- The generic model functions allowed a zero-day purge and a bootstrap interval
  on a 14-day consecutive subset. The protocol-bound `evaluate` wrapper now
  fixes the configured one-day purge, exact folds/feature sets, target interval
  and dense decision calendar, and withholds primary inference unless all 366
  exact 2024 days survive. Generic helper output alone is not the registered
  primary result.

Independent focused checks initially passed 21 model/spot synthetic tests. The
capture worker subsequently reported 12 combined capture tests passing after
fixing a test-only ZIP timestamp instability: independently regenerated ZIP and
checksum fixtures could cross the ZIP clock's two-second boundary. The
capture-specific tests separately cover missing inventories, denial, latched
low space, immutable storage, safe ZIP parsing and a synthesized Parquet range
capture. An additional independent end-to-end synthetic capture checked all 46
cells and 535 files with the closure checker; an added orphan file was rejected.
These are engineering checks, not financial evidence.

The user-selected execution checkout is now on the Data partition; source-relative
artifact paths remain unchanged. The partition is on the same physical NVMe,
so the new capacity is not an independent external backup.

## Claims not tested

Actual provider availability, disk usage during acquisition, remote recovery,
full-history retention capacity, original historical publication/vintage,
canonicality and numerical graph integrity, the unfinished seven-day motif
pilot, real feature coverage, predictive effect/inference, paper replication,
execution, fees, funding and profitability remain untested. No PnL convention
is implemented or claimed here. No higher reasoning effort is indicated for
the reviewed capture; the outstanding checks require actual retained capture
evidence and a later admitted numerical panel.

## Admission-only correction review

The initial committed preparation failed the read-only admission check because
the `eth-spot` window lacked a mapped input. The separate ADMISSION_NOTES.md
preserves that failure and the preceding rejected short source hash. Independent
inspection found no capture claim directory, capture artifacts or resource
receipt in either the consolidated or Data execution checkout. This is a
pre-execution correction, not a consumed capture or a replay.

The successor adds `spot-plan.json` as a hash-bound `eth-spot` input and checks
its exact cohort/URL sequence before any acquisition. Independently generated
month and URL lists matched all 38 months and 76 anonymous URLs. All 28 source
hashes, all 16 input hashes and the charter hash matched the corrected files.
Bounds, windows, cells, allowance and scientific rules are unchanged. No new
material blocker was found. The existing unused execution checkout may move
to the reviewed successor commit before claiming; retain the original commit
and admission notes and verify the successor's full HEAD through admission.

# Active missing-source recovery checkpoint

The user-authorized finite recovery started at 13:42:36 UTC on September 17,
2026 from fixed source `bd162e1f26450386dab17cb8df3364b1f60b88c2`. Source, original
closure, gate and explicit single source-only amendment were committed, pushed
and matched remotely before launch. Ordinary and custom amendment admission
passed in both checkouts; 34 focused synthetic tests passed. DNS resolved before
any new claim. This is not a new financial experiment or a reset of prior history.

Execution checkout:
`/home/malecada/Data/onchain-research/TradingAgents-onchain-recovery`.
The launch receipt identifies PID30581, process start ticks151133 and boot ID
`18ed8946-0984-4865-8cf5-c701a0f7ed65`. At 13:43:32 UTC, its guarded worker was
PID31243 with CPU affinity0–1 and 166,820 KiB instantaneous RSS. The unchanged
8 GiB aggregate sampled guard remains active; this snapshot is not peak usage.

The first two missing dates, April19 and June6,2022, are recovered and independently
verified. The [independent startup review](RECOVERY_START_REVIEW.md) at13:44:16UTC
confirmed a third completed date, June29, with the same active worker. Their valid original source responses were copied unchanged, retaining
original clocks and explicit reuse provenance; only missing responses used HTTP.
The original563complete dates are excluded. The full recovery denominator is521
dates and one index cell; first success does not establish whole-run completion.
At observation the job remained active without a terminal receipt, with about
105.5 GiB free on Data. Future progress requires a fresh live check.

Launch receipts, the new active claim and the first two date outputs/manifests
are mirrored in the coordinator under `recovery-launch-evidence/` and the new
research run directory. Exact source/destination hashes were compared. The
explicit single source-recovery allowance is consumed at cumulative9/9; the local
inventory now contains46claims, which are not independent hypotheses. The old
capture's terminal failure cells and original cap8 remain immutable.

Use [RECOVERY_RUNBOOK.md](RECOVERY_RUNBOOK.md) for process identity, checkpoint and
closure checks. A first transport error latches a stop across later dates, with
unattempted cells recorded and zero further requests. No request retry, automatic
restart, recurring task or reboot recovery is configured. Do not rerun either
the original or recovery launcher under a claimed identity.

Raw bodies remain local on Data without a verified off-device backup. The 120 GiB
family-body ceiling, 2 GiB metadata ceiling and 20 GiB free-space floor plus
working reserves remain in force. Actual copied prefix bytes are charged again.
After capture, whole-panel numerical integrity, boundaries, duplicates and motif
features still require admission before the prepared matched M0/M1/M2 prediction
evaluation. No prediction accuracy or economic result has been produced here.

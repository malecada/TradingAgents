# Source continuation and inherited control reservation

The standing user instruction authorizes one more source-only continuation after
four transport failures. `eth-graph-source-resume3-20260918` consumes cumulative
prior11/cap12. The old family prior10/cap11, zero-complete/446unavailable outcome,
all eleven ancestors and prior byte accounting remain unchanged. A separate
mechanism label only routes this explicit amendment through the unchanged base
lifecycle; it cannot reset budgets or sample exposure.

The retry engine, source inventory and resource ceilings are unchanged. A TLS-
only preflight now precedes any claim. The63GiB/1160MiB prior baselines exceed the
parent's recorded totals. The parent already charged128MiB for controls; a frozen
physical-allocation audit leaves enough unused space to earmark64MiB runtime
controls and44MiB compact closure mirror, plus8MiB overhead cushion. No previous
counter is decreased and no new reservation is added. This is explicitly scoped
logical family accounting, with static checkout replicas constrained separately
by actual disk free space as in the inherited contract.

The policy binds the complete reserve denominator and all hashes, allocations,
parent-origin charge, exact446missing/638excluded dates and13response prefix,
source/charter/review and target contract. Future inherited-pool use must include
cumulative spending by all consumers, not restart its denominator. This certificate
is consumable once; future continuations require a concrete reviewed decision.
No numerical/financial result, new provider, paid data, credentials, orders or
production changes are authorized by this source operation.

# Finite source recovery operation

Execution checkout: `/home/malecada/Data/onchain-research/TradingAgents-onchain-recovery`.
Identity: `eth-graph-source-recovery-20260917`. The original bulk checkout and all
its completed/unavailable records are immutable. Never rerun its launcher.

Before launch, commit and independently verify source, gate, charter, approval,
amendment and predecessor closure; push and verify the exact remote hash. Create
the new fixed checkout with no `bulk-artifacts/` directory, then run the custom
`admit_recovery` function using the pinned Python interpreter. It supplements
ordinary lifecycle admission with the explicit one-use source-recovery policy.
The starter performs a DNS-only health check before any launch receipt/claim.

`recovery_start.py --source FULL_HEAD` starts one finite supervised job. It uses
exclusive intent and log files and rejects any existing claim. The immutable
`recovery-started.json` records source, PID, process start ticks and boot ID.
The guard remains 8 GiB sampled aggregate RSS and two CPUs, without duration kill.
The resource receipt is reserved at launch and filled at termination.

The new checkout's `comparison/bulk-artifacts/DATE` contains the recovery bodies
and original-clock copied prefix evidence; the path name is shared with the
existing independent checker, but the checkout is new. Per-day lifecycle outputs
appear immediately. Successful prefixes are read only from the exact absolute
original paths and verified against the frozen manifest before any new HTTP.

For status, compare boot ID, PID, start ticks and expected command. Read dated
outputs for status and separate logical/reused/new-network counts. Do not infer
failure merely from an empty live resource receipt, or full data coverage merely
from `complete.json`. A first transport failure latches a global stop; later
unattempted dates remain explicit with zero new requests. There is no scheduled
assistant task, request retry, automatic restart or replay.

After termination, independently reconcile terminal/output hashes, raw manifests,
reuse provenance and all unavailable dates. Import small closure/progress
metadata separately; do not put the bulk raw corpus in Git. The old and new raw
stores have no verified off-device backup. The original one-use source-recovery
allowance is consumed once claimed; a further attempt needs another concrete
reviewed decision preserving cumulative history.

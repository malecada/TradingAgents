# Remaining graph-source acquisition

The user authorized starting the data download and supplied the Data partition.
This is one finite raw acquisition under that authorization, following the
independently verified first input tranche. The destination is a new preserved
checkout under `/home/malecada/Data/onchain-research/`. Existing source checkouts,
raw stores, registrations and outcomes remain immutable. No scheduler, automatic
restart or same-identity replay is part of this operation.

## Cohort and source identity

The fixed candidate calendar is January 1, 2022 through December 31, 2024,
inclusive. The January 1–7, 2022 source tranche and January 1–5, 2024 retained
sources are excluded from new acquisition. January 5 remains an interrupted
pilot day with no numerical admission; sufficient raw receipts exist for later
independent reconstruction. The exclusions preserve existing evidence and are
not a success-only financial sample. The remaining 1,084 dates are acquired in
chronological order, once each. January 9, 2024 reuses its retained full block
object while acquiring its missing transaction ranges. The committed reuse
inventory records exact locations and hashes. All other block objects and the
nine inherited transaction columns are acquired from the same retained AWS
public-blockchain object inventory, with exact ETag and range checks.

The old raw days are references for later panel assembly, not silently relabelled
as successful numerical outputs. The December 31, 2021 overlap context remains a
separate missing boundary prerequisite. This job opens metadata and raw source
bytes only. It does not decode transactions into graph events, interpret spot
prices, create labels, fit models or judge forecast accuracy. The existing
comparison protocol and financial sample exposure remain unchanged.

## Bounds and stopping

The inherited limits per date are 291 requests, 272 MiB received bodies,
32 MiB per response, 256 MiB selected-column projection, 16 MiB full blocks,
4 MiB footer, 32 row groups, 2,000,000 transactions and an 8 GiB logical object.
The 1,084-date maximum is 315,444 requests and 294,848 MiB received bodies.
An overflow detection byte is included in the existing aggregate response cap.
HTTP timeout is 30 seconds; there are no retries, redirects, proxies, credentials
or alternative providers. The frozen source plan binds column types and base URL.

The shared retained raw ceiling is 120 GiB, including a conservative 2 GiB
baseline for prior retained source bodies. The metadata ceiling is 2 GiB,
including a conservative 128 MiB prior baseline and a separate 128 MiB reserved
for lifecycle outputs, launch/resource receipts, failure closure and stdout.
Day lifecycle records are bounded to 16 KiB each, the final index to 32 MiB
and summary to 1 MiB; the same bounded day records form the progress log.
These together leave substantial headroom in the dedicated reservation, which
is charged before acquisition and allows unavailable-date records after a stop.
Day admission reserves 544 MiB,
and each network request reserves its maximum stored-response expansion and
8 MiB metadata allowance before acquisition. Actual stored bytes are charged
after each request; manifests and result records are also charged. The disk
check requires at least 20 GiB plus 544 MiB available before every request.
These reserves protect normal writes and filesystem overhead, but do not reserve
space against unrelated concurrent writers. The job does not allocate the
proposed 16 GiB numerical scratch or 48 GiB hash-check scratch.
The prior raw baseline includes legacy JSON receipts containing base64 bodies,
not only `.zst` files. The referenced prior bodies occupy less than 2 GiB;
ordinary prior metadata fits within its separate 128 MiB allowance.

Low disk, aggregate storage exhaustion or HTTP 401/403/429 latches a stop for all
later dates. Other failures make their date unavailable without retry, while
later scheduled dates can proceed. Every date receives its original scheduled
cell, including unattempted dates after a stop. Completion means acquisition
ended with all statuses recorded; it does not mean every input was available.
The unchanged sampled process-tree guard enforces 8 GiB RSS and two logical
CPUs, with no elapsed-time or CPU-duration kill.

## Checkpoints, history and recovery

Each requested response has an exclusive immutable intent, raw zstd body and
receipt. Where persistence reserves permit, each date has a manifest reconciling every intent/receipt/body, raw and
stored hashes, object ETags and range boundaries. A dated lifecycle output is
published immediately after the day finishes. A missing manifest remains explicit
in an unavailable lifecycle cell; it never becomes a successful source day.
The final index binds those
per-day manifests; it does not scan all raw bytes into memory. The separate
per-day checker uses the previously reviewed independent archive reader to
reconstruct footer ranges and reconcile raw/stored bytes before publication.
A failed check preserves original capture fields and marks the final source
cell unavailable. This raw check is still not numerical panel admission.
Crash recovery preserves a partially written date and its consumed claim;
continuation requires a reviewed contract using retained responses, not an
automatic retry. Never restart the launcher when a claim or launch receipt exists.

Identity: `eth-remaining-graph-capture-20260916`. The remaining-cohort acquisition
family imports seven exact predecessor claims once, including the failed 2 GiB
motif and interrupted pilot. Its cumulative prior count is 7 and fixed allowance
8, allowing only this single new acquisition. All old family caps and outcomes
remain unchanged. This is a distinct bounded source prerequisite, not an
additional forecast parameter trial or renewal of the first-tranche allowance.
The local claim inventory was 44 before launch; these are not independent
hypotheses. All ETH historical forecasting exposures remain spent/exploratory.

The source, gates, reuse references and launch plan are committed, reviewed and
remote-hash verified before launch. Raw output remains on Data, which shares a
physical NVMe with the workspace. The first tranche has a verified remote Git
backup; this bulk raw corpus does not yet have an independent backup destination.
That limitation is recorded without claiming local hashes or a second partition
are a recoverable off-device copy. No roughly 100 GiB Git import is attempted.
Small progress/closure receipts can be preserved separately. No paid resource,
provider contact, account action, order, production mutation or unattended
schedule is introduced.

Success requires all new source dates complete and independent reconciliation.
Missing dates remain missing; they cannot be imputed into a prediction panel.
Full numerical integrity, overlap handling, exact uniqueness, motif counts,
historical availability qualification and the actual matched comparison remain
subsequent work under their fixed contracts. This operation yields no economic,
profitability or prediction-accuracy verdict.

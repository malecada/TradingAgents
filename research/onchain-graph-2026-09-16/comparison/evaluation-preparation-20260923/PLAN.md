# Matched comparison continuation — September 23, 2026

The user authorized proceeding with final graph verification and the matched
M0/M1/M2 comparison. This record continues the approved September 16
[protocol](../PROTOCOL.md). It creates no empirical claim and is not an
executable lifecycle gate. The frozen extraction checkout remains unchanged.

## Current evidence and preparation

Extraction completed all 1,096 source days and 1,094 graph days, with only the
registered first/last graph-boundary exclusions. Its compute/global identity
audit passed; independent final review is still pending. The current source is
`87b6ac39d12a4f9a2ac1832f34f68647c52c53c8` in
`/home/malecada/Data/onchain-research/TradingAgents-onchain-fullpanel-resume2`.
No extraction replay is permitted.

The [readiness observation](readiness.json) binds the unchanged protocol,
configuration, feature builder, learner, spot parser and prior capture review.
All 267 spot manifest members and 76 compressed response blobs match their
retained size/hash metadata. All 38 captured months are present. No observed
price fields or labels were parsed and no model was fitted during preparation.

The new [input adapter](../input_adapter.py) accepts already supplied bytes and
summaries. It checks archive checksums, calendar/timestamp units, positive finite
OHLC and nonnegative volumes, quote-volume selection, role conservation,
denominators, graph exclusions and review/terminal/panel content links. January
2025 uses microseconds. Only January 1, 2025 is decoded for the final target
endpoint; later January prices remain uninterpreted. Original stores are not
mutated. There is no empirical command-line entry point.

Null original availability is retained in source evidence. Separate adapter
frames explicitly label their clocks as protocol assumptions, with historical
availability unverified. Any supplied later graph clock survives conversion and
propagates through trailing windows. These retrospective assumptions never
establish historical publication or executable midnight fills.

The unavailable January 1, 2022 graph row is retained as an exclusion. With the
unchanged all-feature common mask, the January 9 training decision consequently
lacks a complete seven-day graph window. January 10 is the first potentially
complete training decision. No date shift, imputation or boundary acquisition
is introduced. This consequence follows from source coverage and synthetic
tests, without observing labels or model performance. The 2024 test calendar
remains all 366 days; any later discovered missing prediction makes primary
inference unavailable.

## Remaining execution sequence

1. Wait for at least 9 GiB host MemAvailable and both 20 GiB disk floors. Check
   the exact process/receipt state before launching the existing frozen graph
   `launch.py --review` once. Keep its 6 GiB memory high/max, 512 MiB swap and
   two-CPU controls. Require a passing full-panel report and clean final guard
   receipt; a failure-only report with `passed=true` is insufficient.
2. Complete the full compact-evidence import, manifest verification, closure
   and remotely verified Git backup after capacity preflight. Main-partition
   space must cover files plus Git overhead above the 20 GiB floor. Preserve
   raw/hash roots and all predecessor failures. No off-device raw backup is
   asserted by this step.
3. Finish the bounded comparison runner and independent outcome checker. The
   runner must open bytes only through the admitted lifecycle, check exact
   report/guard/source provenance, graph calendar and two boundary exclusions,
   preserve all input/fold/row exclusions, and invoke the existing fixed
   `features.build_panel` and `model.evaluate`. The adapter alone does not
   perform lifecycle, full-calendar or resource-provenance admission.
4. Create an additive comparison charter, exact input/source/runtime bindings
   and lifecycle gate. Bind every monthly metadata/ZIP/checksum input, graph
   panel/terminal/report/guard, protocol/configuration and inherited source
   dependencies. Preserve the 16 prior graph claims and spent ETH sample
   history. Independently review the explicit single downstream allowance
   (proposed cumulative 16 prior / 17 total); do not reset prior budgets or
   alter any old gate. Commit and verify the remote source before claiming.
5. Complete synthetic runner/adversarial checks and the named offline target
   when memory permits. The full target has not been rerun for this preparation.
   Admit the exact gate, execute once under the reviewed resource guard, and
   retain attempted, failed and unavailable cells. No outcome-driven rerun or
   settings change is authorized by a failed cell.
6. Independently verify dated predictions, labels, purged training boundaries,
   identical rows across arms, monthly/pooled metrics and paired inference.
   Preserve all results and issue the conditional information-content verdict.

## Unchanged comparison and decision

M0 uses seven market features; M1 adds six ordinary activity features; M2 adds
seven motif features. Each uses the fixed LightGBM 4.6.0 configuration, 100 trees,
seed 42 and two threads. Twelve expanding monthly 2024 folds give 36 planned
model fits, with the training-majority baseline reported separately. No tuning,
neural model or added baseline is silently introduced.

The target is next-day ETHUSDT spot opening-price direction, with ties class
zero. All features use source day D−2 or older; training labels are purged by
one day at each monthly boundary. This is exploratory retrospective analysis
of previously exposed history, not untouched confirmation or paper replication.

Primary inference is M2 minus M1 log loss with the fixed paired 14-day moving
block bootstrap, 2,000 draws and seed 42. Neural follow-up is supported only
when the entire 95% interval is below zero and at least eight of twelve monthly
differences are negative. Accuracy, balanced accuracy, Brier score and activity
increment are reported without substituting for that criterion. No PnL or
validated-strategy claim follows.

## Resource checkpoint

At 19:13:57 UTC, available RAM was about 2.0 GiB, below the 9 GiB verification
startup requirement. Main-partition free space was about 20.75 GiB, still
insufficient for full evidence import plus Git overhead above its reserve.
No user process was terminated and no limit was lowered. The existing monitor
remains responsible for checking capacity and continuing this authorized
sequence. No additional user permission is required for the sequence above.

# First matched-comparison inputs captured and independently checked

The first tranche completed all 46 cells and 47 lifecycle outputs, with zero
unavailable cells. It contains seven January 1–7, 2022 Ethereum source days and
38 Binance ETHUSDT spot monthly archives from December 2021 through January
2025. This is source-byte/calendar admission, not graph numerical admission or
a forecasting result. No price/volume fields, labels or predictions were evaluated.

| Measurement | Result |
|---|---:|
| Ethereum transaction rows in captured footer metadata | 8,420,991 |
| Selected transaction-column bytes | 994,164,683 |
| Spot daily rows with verified calendar timestamps | 1,158 |
| Actual anonymous requests | 295: 219 graph, 76 spot |
| Received response bytes | 1,032,495,329 |
| Retained capture bytes, including metadata | 754,265,130 (719.32 MiB) |
| Retained capture files | 940 |
| Whole guarded run elapsed | 703.16 seconds |
| Sampled aggregate peak RSS | 287,535,104 bytes (274.21 MiB) |

The successful source HEAD is `4ea20258a72286721157736b12c7147423b26214`,
preserved at `/home/malecada/Data/onchain-research/TradingAgents-onchain-comparison`.
The existing 8 GiB/two-CPU guard reported exit zero and no limit event; no
elapsed/CPU-duration kill was introduced. These timings include acquisition,
compression and lifecycle checks; they are not graph reconstruction timings.

Independent closure reconciled original compressed bytes, raw/stored hashes,
request intents, conditional ETags and content ranges, footer-derived projection
spans, exact spot ZIP checksums and UTC calendar timestamps, all source cells,
the full manifest and the terminal lifecycle. The independent checker imports
no production capture or lifecycle code. It passed in 3.66 seconds with
281,169,920 bytes sampled peak RSS. The structural verifier separately reports
46 cells, 47 outputs and zero unavailable cases. See CAPTURE_REVIEW.md/.json.

The full offline target passed 2,160 tests and 97 subtests; final focused
comparison/capture tests passed 40 cases. Synthetic checks establish chronology,
common-row exclusion, fixed-fold inference and source handling, not economic
predictability. The broad unchanged-core suite finished successfully while the
independently reviewed, focused-tested source capture was running.

## Preservation and history

The Data partition is now in use. The existing original workspace, failed pilot
and other raw stores were not moved or replaced. The complete imported set is
990 files and 754,630,488 bytes: source artifacts, claim/terminal/outputs and
the resource receipt. Every cross-filesystem copy was published exclusively
and checked against its original SHA256. The import manifest records local
copy verification only; external backup is established separately by the
subsequent pushed and remote-verified result commit.

The prior six graph claims remain counted once; this downstream allowance is
now consumed at 7/7. The consolidated local inventory is 44 claims, not 44
independent hypotheses. No failed/completed identity may be replayed. The
pre-execution source-metadata correction and rejected admission-only checks
remain documented in ADMISSION_NOTES.md; no claim preceded the correction.

## Remaining work

The matched M0/M1/M2 protocol, fixed feature builder, learner and evaluation
wrapper are prepared, but the full graph panel is absent. Capture success does
not prove transaction integrity, canonicality, motif counts, historical
publication timing, predictive value or the paper's reported accuracy.

The next preparation is a full-cohort capture/retention contract with bounded
checkpointing, followed by numerical graph and completion-window admission.
RETENTION_PLAN.md explains why raw data plus summaries should be retained while
newly generated intermediates are declared recomputable scratch in advance.
Exact full-history duplicate checking needs an external-memory implementation.
The interrupted 2024 seven-day pilot remains terminal failed and its retained
inputs require a reviewed continuation, not replay. No full-history job or
forecast run remains active after this finite tranche. Historical financial
samples remain spent/exploratory; zero strategies are validated.

# Missing-source continuation with bounded transport retries

The September17 user instructions “resume it” and “going forward monitor the
process and when it stops resume it” authorize source continuation and recurring
operational monitoring. This contract grants the next finite acquisition only:
`eth-graph-source-resume-20260917`. No price parsing, graph event decoding,
features, labels, model fitting or economic conclusion is authorized by this
source contract. All original criteria, raw stores and terminal outcomes remain.

The parent `eth-graph-source-recovery-20260917` closed at14:20:23.551103UTC after
14dates completed and507 remained unavailable. A connection reset on request20
for2023-08-08 stopped further requests;506later dates had zero requests. Parent
resource receipt reports2264.53seconds,513454080bytes sampled peak RSS, exit0
and no resource-limit reason. Lifecycle closure does not imply full coverage.

## Exact scope and preservation

`resume-cohort.json` freezes exactly507unavailable dates,2023-08-08 through
2024-12-31 excluding previously retained complete reference dates. All577complete
original-bulk/recovery dates are excluded. Older first-tranche/reference inputs
are retained separately. Nineteen successful August8responses (57files,
15607925bytes including metadata) are hash-bound for original-clock reuse.
The failed twentieth response remains in its original directory. No failed or
truncated response is accepted as a complete source body. January9,2024 reuses
the prior complete blocks through the unchanged bound input reference.

The endpoint, fixed object inventory and ETags, conditional If-Match headers,
byte ranges, nine transaction columns, block schema, source timestamps and
historical availability limitations remain unchanged. No provider substitution,
credentials, paid data or object-version substitution is permitted.

## Request retry and checkpoint policy

For each logical source request, at most four actual network attempts are
permitted. Retry only transient transport failures: DNS, connection reset,
TLS/read timeout, truncated response. Delays before attempts2–4 are5,15,45seconds.
Every attempt retains its own immutable intent, receipt and response body,
including partial/empty failures; a mapping identifies which successful attempt
supplies each logical response. Original reused prefixes retain original clocks.
Successful body hardlinks may share storage between logical and physical evidence
only with explicit same-inode/hash validation and unique-body accounting.
Metadata copies and copied historical prefixes are charged again.

Exhausting four transport attempts stops further requests for the run. HTTP401,
403 or429, storage floor/cap, changed object identity and independent integrity
failures are not repaired by automatic retries. Deterministic source errors
remain unavailable. All507dates remain in the denominator, including unattempted
dates after a stop. Per-date outputs survive interruption; a terminal identity
is never relaunched or overwritten. No process-level automatic restart is built
into this launcher.

The separately requested thread monitor checks every30minutes. When a job stops
from a transient transport problem or reboot, it may prepare, independently
review and launch the next source-only continuation without another user prompt.
It must reconcile all retained evidence, preserve or close the previous identity,
exclude all complete dates and reuse valid prefixes. Each subsequent continuation
requires a concrete committed contract and cumulative accounting; this contract
cannot be reused or silently expanded. Disk/cap, access denial, object mutation,
integrity failure or unknown stop cause requires diagnosis before any restart.
No repeated network requests during persistent denial or disk exhaustion.

## Bounds

Logical per-date limits remain291requests and272MiB response bytes, including
reused logical bodies. Physical retry limits are1164requests and1088MiB response
bytes per date, or at most590148requests and551616MiB across507dates before
reuse reductions. Individual responses32MiB plus overflow sentinel, blocks16MiB,
footer4MiB, selected columns256MiB,32rowgroups,2million rows,8GiB logical object,
30second timeout. Retries cannot bypass physical byte or request limits.

The shared retained raw cap stays120GiB and metadata cap2GiB. Prior raw baseline
is conservatively56GiB, above parent accounting59262745960bytes. Prior metadata
baseline768MiB exceeds parent682995712bytes; an additional128MiB reserves new
lifecycle/log/guard overhead up front, giving896MiB initial metadata charge.
All new failures, successful bodies, copies and metadata count. Shared hardlinked
bodies count once, with independent link validation. The20GiB actual free-disk
floor plus working reserves is checked before each physical request/copy.
Guard remains8GiB sampled aggregate process-tree RSS and two logical CPUs with
no elapsed/CPU-duration kill. Daily output16KiB,index32MiB,summary1MiB.

## Admission and closure

`SOURCE_RESUME_AMENDMENT.md` and `resume-amendment.json` bind the source-only
cumulative9-to10allowance, parent gate/family/claim/terminal, all nine ancestors,
cohort, exact target contract and independent approval. Shared economic lifecycle
schemas and old gates are unchanged. A fresh fixed checkout on Data is used.
Source, frozen gate and review are committed, pushed and remote-hash verified
before launch. The starter records PID/start ticks/boot ID and exclusive intent.

The independent raw checker reconciles logical source spans, all attempt evidence,
failed attempts, prefix provenance, byte accounting and every manifest. Independent
check failure latches a stop. At termination all available/unavailable outputs
and small closure evidence are imported and backed up. Raw bodies on Data have
no verified off-device backup. Numerical panel admission and the matched M0/M1/M2
prediction evaluation remain subsequent work.

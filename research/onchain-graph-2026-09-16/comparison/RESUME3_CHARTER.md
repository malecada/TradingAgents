# Third bounded source continuation

The standing monitor-and-resume instruction authorizes this source-only
continuation, `eth-graph-source-resume3-20260918`, for the same 446 missing dates.
The parent `eth-graph-source-resume2-20260918` closed at00:43:56.188451UTC September18
with zero complete and446unavailable dates after four TLS/read timeouts. All
failed and partial bodies remain retained; the later445dates made no requests.
A DNS/TLS-only health check at00:55:19UTC connected using TLS1.3 in0.538seconds,
without any HTTP request or data body. This indicates transport recovery at that
instant, not reliable future downloads. The new starter requires a15second TLS
handshake before admission/launch intent/claim; failure consumes no new identity.

Exactly638previously complete bulk/recovery/continuation dates remain excluded.
The same13successful October8,2023 responses from the original retry-enabled
store are frozen for original-clock reuse. No failed response is accepted as a
complete body. The fixed AWS endpoint, inventory keys/ETags, If-Match conditions,
ranges, schemas, nine columns and January9block reuse remain unchanged. The
acquisition engine is unchanged `resume_graph.py`; the independent checker differs
only by cohort filename. There is no graph-row decoding, price/model work,
financial inference, provider substitution, credential or paid service access.

## Retry, source and resource limits

At most4physical attempts per missing logical request, delays5/15/45seconds,
with every attempt retained. Retry only the existing transient transport class;
certificate/protocol/unclassified/HTTP failures are not retried. Exhaustion stops
subsequent acquisition; denial401/403/429, storage and independent integrity stops
cannot be bypassed. Every unavailable/unattempted date remains in the denominator.
291logical requests/272MiB and1164physical attempts/1088MiB per date remain.
Responses32MiB plus sentinel; blocks16MiB, footer4MiB, projection256MiB,32rowgroups,
2million rows,8GiBlogical object. The30second transport timeout is socket/read
inactivity, not a total-request deadline. The8GiB sampled process-tree RSS guard,
twoCPUs, no duration kill and20GiBactual free-disk floor plus reserves remain.

The unchanged Budget implementation keeps the120GiB raw and2GiB metadata source-
family accounting ceilings. New prior baselines63GiB raw and1160MiB metadata
exceed every parent's recorded total; no prior counter is reduced. The metadata
limit covers acquisition/attempt artifacts plus carried prior accounting and
reserved controls. It is not a cap on every duplicated JSON/source file across
all Git worktrees. Static inherited checkout/evidence replicas consume actual
filesystem space, constrained by the20GiBfloor, separately from this logical
family counter, as already distinguished in RECOVERY_CHARTER.md64–65 and the
checkout allowance in RETENTION_PLAN.md. No blanket all-worktree physical
metadata claim is made. New failed bodies, prefix copies and acquisition
metadata remain charged; selected/physical successful body hardlinks count once.

## Audited reuse of an already charged control reservation

The immediate parent charged128MiB for lifecycle/log controls before acquisition.
`resume3-reserve.json` inventories its closed lifecycle/log files plus coordinator
closure/startup mirrors:1805exact files,11067392allocated bytes. The custom validator
checks the full declared path denominator, every size/hash/physical allocation,
and that the origin cohort actually charged128MiB. An8MiB additional overhead
cushion remains. This continuation earmarks64MiB of the existing unused pool for
runtime controls and44MiB separately for a compact closure mirror. Their sum is
less than the original128MiB, leaving1515520bytes unallocated. No additional128MiB
charge is added; the entire original pool remains in the carried1160MiB baseline.

Runtime controls are bounded by twice446daily16KiB records (output and compact
log),32MiBindex,1MiBsummary and4MiBother controls, at most53411840bytes below64MiB.
The closure mirror includes only claim, dated lifecycle outputs, index, summary,
terminal/resource receipts and a compact per-date logical/physical manifest hash
index, with import metadata inside4MiBextras. Its bound is46104576bytes below44MiB.
Do not copy the live progress log or all raw/day manifests into that mirror.
Full immutable manifests and bodies remain on Data, referenced by hashes.
Before any closure import, calculate physical allocated copy bytes and require
that they fit44MiB; verify actual allocations and hashes after copying. If they do
not fit, preserve source files and diagnose before importing; do not exceed or
silently expand the pool. Metadata preservation is not full off-device raw backup.

Future continuations that reuse this pool must preserve its origin in resume2
and cumulatively audit controls/mirrors from resume2 and every later consumer.
They cannot reset to the immediate parent's files or invent another128MiBcharge.
A new concrete review is required if the inherited remaining pool is insufficient.

## Admission and continuation

SOURCE_RESUME3_AMENDMENT.md and the certificate grant exactly one additional
source-only claim, cumulative prior11/cap12. All eleven ancestors, old caps,
parent gate/family/claim/terminal, cohort, reserve audit, source, charter and
independent approval are bound before launch. Shared lifecycle schemas and old
evidence are unchanged. Source is committed, pushed and remote-hash verified;
a new fixed Data checkout is used. The starter records PID/startticks/bootID.
No terminal identity is replayed. The existing30minute task monitor remains
active under standing user authorization, with no duplicate worker or blind
restart against disk/resource/denial/integrity problems. Numerical admission and
the matched prediction comparison remain separate future stages.

# Second bounded source continuation

The user's standing instruction authorizes monitoring and resuming recoverable
source-download interruptions without another confirmation. This finite contract
continues exactly 446 missing dates under `eth-graph-source-resume2-20260918`.
It preserves the previous run, all ten ancestor claims, raw sources, failures,
spent exposure and cumulative accounting. It does not authorize numerical graph
reconstruction, prices, features, labels, models or economic evaluation.

The parent `eth-graph-source-resume-20260917` recovered 61 dates, August 8 through
October 7, 2023. October 8 exhausted four read-timeout attempts at logical request
14 (physical requests 14–17). Its 13 earlier successful logical responses are
eligible for exact frozen-prefix reuse; all four failed bodies remain preserved.
The other 445 unavailable dates received no subsequent network acquisition.
The parent must finish and its receipt/output hashes must be reconciled before
this continuation can be admitted or launched. Never interrupt or relaunch it.

`resume2-cohort.json` freezes the exact missing dates, October 8, 2023 through
December 31, 2024, excluding already retained reference dates. The cumulative
638 complete bulk/recovery/continuation dates are excluded from new requests.
The October 8 successful prefix is copied byte-for-byte with original receipt
clocks and explicit provenance. Its selected logical evidence was derived from
physical attempt evidence in the previous run; that previous mapping, hardlinks
and failed attempts remain immutable and independently checked. No failed or
partial response becomes an accepted body. The original January 9, 2024 blocks
remain the same bound reusable input.

The reviewed `resume_graph.py` acquisition engine is unchanged. It retains at
most four physical attempts per missing logical request, delays 5/15/45 seconds,
immutable attempt evidence, selected-response hardlinks, unique-body accounting,
and exact old inventory keys/ETags/conditional headers. The checker differs only
in reading this continuation's cohort filename. TLS certificate/protocol errors,
HTTP errors and unclassified failures are not retried. Four exhausted transport
attempts stop further acquisition; denial 401/403/429, storage or integrity stops
cannot be bypassed. Every unavailable and unattempted date remains recorded.

Logical bounds remain 291 requests and 272 MiB per date including reused bodies;
physical bounds 1,164 attempts and 1,088 MiB, before any further aggregate cap.
Individual responses 32 MiB plus overflow sentinel; blocks 16 MiB, footer 4 MiB,
selected columns 256 MiB, 32 row groups, two million rows, logical object 8 GiB,
and a 30-second socket/read inactivity timeout. This is not a 30-second
overall-request deadline; partial progress can keep a request alive longer.
No numerical transaction rows are decoded.
The 8 GiB sampled process-tree RSS guard, two CPUs, no duration kill, 20 GiB
free-disk floor plus working reserve, 120 GiB raw and 2 GiB metadata caps remain.

The conservative raw baseline is 62 GiB, above the parent's final accounting.
Prior metadata is 1,024 MiB, conditional on independent confirmation that the
parent's final accounting does not exceed it; another 128 MiB reserves new
lifecycle/log overhead up front, giving 1,152 MiB initial metadata charge. No
prior charge is reclaimed or reset. All new failed responses, copied prefixes
and metadata count, with same-inode selected body hardlinks counted only once.
Existing caps may stop acquisition; the monitor may not raise them implicitly.
Daily output 16 KiB, final index 32 MiB and summary 1 MiB remain bounded.

The explicit source-only amendment `SOURCE_RESUME2_AMENDMENT.md` and certificate
bind this one additional claim, cumulative prior 10 / cap 11, old family/gate,
parent claim/terminal, all ten ancestors, exact cohort, source/charter and review.
Old gates and shared lifecycle schemas are unchanged. Source and metadata are
committed, pushed and remote-hash verified before launch in the new preserved
Data checkout. The independent checker reconciles every logical and physical
receipt, manifest, prefix, attempt and storage count. Failed independent checks
stop subsequent acquisition. All terminal and small metadata evidence is retained.

The existing 30-minute task monitor remains authorized. A further interruption
can lead to another concrete reviewed continuation preserving cumulative history;
this one-use certificate cannot be reused. The monitor diagnoses unknown causes
and does not blindly retry resource, disk, denial or integrity failures. Raw
stores on Data still have no verified off-device backup; Git metadata backup
does not imply a raw backup. Numerical admission and prediction work remain
separate future stages.

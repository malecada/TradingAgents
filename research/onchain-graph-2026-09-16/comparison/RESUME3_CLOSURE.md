# Source acquisition completion — September 18, 2026

The last continuation, `eth-graph-source-resume3-20260918`, completed at
11:54:11.237533 UTC (13:54 Prague time) from fixed source
`c27a93d7f736cd05e41277d966572bd931e659b8`. All 446 scheduled dates completed,
with zero unavailable cells. The terminal contains 447 cells and 448 output
hashes; the standard lifecycle verifier reconciles all structural hashes and
denominators. No source identity was replayed. Launcher 1479563 and worker
1480459 are gone; the guard exited normally with no resource-limit stop.

The preceding bulk/recovery runs supplied 638 distinct completed dates. This
continuation brings the bulk cohort to 1,084 completed dates, with zero missing.
The 12 earlier reference dates are separate from that bulk denominator; their
union covers all 1,096 calendar dates in 2022–2024. All
old failed, unavailable and partial attempts remain preserved unchanged.

The final continuation made 26,651 actual network requests and reused 13 valid
responses, covering 26,663 logical requests. One timeout retry remains retained. Every completed date records a verified independent source check.
These checks establish retained source structure and transport evidence; they
do not admit numerical graph reconstruction, historical publication timing,
predictions or economic performance. No model or price evaluation was run.

## Resources and preservation

The guarded continuation ran for 38,525.575 seconds (approximately 10 h 42 min).
Peak sampled process-tree RSS was 800,628,736 bytes (0.746 GiB), below the 8 GiB
cap. The worker used two CPUs. No elapsed-time kill or automatic job restart
occurred. The final source-family accounting totals are 113,303,913,734 raw
bytes (105.52 GiB) and 1,800,077,312 metadata bytes (1.676 GiB), below the
120 GiB and 2 GiB ceilings. These are cumulative contract counters, including
carried baselines and retained attempts, not unique dataset size. Approximately
49.6 GiB remained free on Data at the completion observation.

The compact closure import preserves 453 byte-exact objects (450 run files
and three control receipts); its hash index references 446 pairs of immutable
logical/physical manifests on Data. The earlier claim and first-date mirror
were matched without replacement. Full day manifests and the progress log were
not copied. Allocation preflight passed before import: 86,016 existing bytes,
3,317,760 estimated new bytes and 1 MiB reserved for final records fit within
the 44 MiB mirror allowance. The post-import mirror initially occupied
3,403,776 bytes; runtime controls occupied 3,575,808 bytes within their 64 MiB
allowance. The final allocation audit adds closure/review/import records.
All spending remains inside the original resume2 128 MiB reservation with its
11,067,392 previously spent bytes and 8 MiB cushion; no new charge or counter
reduction occurred. See `resume3-closure/allocation.json` and final allocation.

Terminal SHA256:
`5f4062cea0b7d431cda40dfde26152682a4a13c1c76ae44159942644d4b3a874`.
Evidence: `resume3-closure/import.json`, `manifest-hashes.json`, original
`research_runs/eth-graph-source-resume3-20260918/complete.json`, and the independent
`RESUME3_CLOSURE_REVIEW.md`.

Raw bodies remain local under `/home/malecada/Data/onchain-research/`; no verified
off-device raw backup exists. Git backup covers source and compact metadata.
Independent closure review passed and the monitor is paused. The next research
stage is numerical integrity and day-boundary validation, then construction of
the admitted feature panel for the matched M0/M1/M2 comparison. Source completion
alone does not authorize or establish those downstream results.

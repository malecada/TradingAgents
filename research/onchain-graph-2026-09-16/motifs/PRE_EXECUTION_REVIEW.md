# Independent pre-execution motif review

September 16, 2026. **No material blocker was found in the reviewed candidate.** One execution of this engineering benchmark is admissible after the reviewed files are committed, remotely verified and checked in the dedicated fixed-HEAD checkout with its own locked runtime. This verdict does not claim that real motif counting has succeeded or authorize a retry, different day, timescale search, financial evaluation or historical feature-panel admission.

The reviewed candidate is based on `68a3e414e0be69cf6e17f0c238c576fa15720de5`. Source review covered CHARTER, gate, history, benchmark, oracle, launcher, tests and the additive optional dependency/lock change. Existing projection, integrity, transport, resource and lifecycle dependencies retain their registered hashes. Real retained inputs were hashed for identity without decoding transaction data or calculating real motif outcomes in this review.

## Lineage and semantics

The new question is the cost and correctness of temporal Local40 enumeration, a substantive downstream operation after acquisition and static aggregation. The separately authorized allowance imports the source and prototype claim once each: prior attempts 2, cumulative cap 3, one new attempt. Original source 1/1 and prototype 2/2 allowances and original 117-complete/2-unavailable result remain immutable. The explicitly recorded forensic inspection is neither another lifecycle claim nor fresh confirmation. API parent=null is explained and scientific parents are separately pinned.

All 128 input, 12 source, six research-runtime and 77 history hashes matched. The charter hash matched its gate. The local inventory remains 39 claims and no motif identity existed at review. This is not a global inventory or independent-hypothesis count. Five cells and 44 outputs are registered, including 39 shards needed for the known 381,191-address denominator.

The new consumer converts only the exact recipient string `"None"` to missing. Reused integrity checks and the previously reviewed forensic integrity/pair fingerprint must agree before motifs. Every affected row remains in the denominator and is excluded from graph edges. The resulting graph retains successful, positive-value, non-self top-level address events only; it does not represent all ETH flows or establish individual contract creation.

Eligible events are ordered by block number and transaction index. Integer Unix seconds retain physical elapsed-time units; an explicit globally unique event ordinal supplies tie order without timestamp perturbation. Recovered times must be nondecreasing. The graph's temporal-event, pair and node denominators are checked before full counting. The boundary is inclusive last-minus-first <=3,600 seconds, with all three distinct events inside the retained UTC day. Midnight-crossing motifs and rolling-feature completeness are outside this contract.

Local40 roles are correctly distinguished from unique occurrences. Stars count at their center, two-node patterns at both endpoints and triangles at all three vertices. Export retains zero vectors, deterministic address ordering, every slot, integer sums/squares, maxima, concentration diagnostics and a content fingerprint. Complementary endpoint totals and triangle divisibility are checked; they supplement rather than replace the synthetic oracle. The bounded real-event oracle samples are explicitly event subsets, not claims to validate full-day local counts at those centers.

## Independent checks and verification scope

The four graph modules plus lifecycle and extended-lifecycle tests passed independently: **117 tests in 75.30 seconds**. This includes exhaustive 216 three-edge shapes, 80 mixed 14-event graphs at four deltas, all eight triangle slots, same-second multiplicity, insertion-order reversal, inclusive boundaries, offline projection and corrupt-receipt rejection. The oracle was reviewed as direct event-triple enumeration rather than a copy of Raphtory's sliding-counter algorithm.

Additional independently invented checks passed before or during this review:

- Forty mixed-direction 13-event star graphs agreed with a separately written combinatorial Local32 reference under forward and reverse insertion. Times included 0, 1, 3,599, 3,600, 3,601 and 7,200 seconds.
- Four same-second same-pair events preserved all four three-event combinations per endpoint. Two-node and triangle participation checks accepted exactly 3,600 seconds and rejected 3,601; tied-time triangles retained their participation count.
- A 10,001-node all-zero export crossed a shard boundary correctly, retained all nodes, preserved canonical ordering and fingerprint, used the declared top-node tie rule, and returned undefined shares when the slot denominator was zero.

The coordinator runtime check passed Python 3.13.13, checkout-local package origin, locked synchronization and installed versions including Raphtory 0.17.0. Independent lock comparison found 17 additions and no existing package-version changes. Changes are an optional dependency, a new isolated consumer and its registration/tests; accounting and core run helpers are unchanged. **The prior named offline result of 2,060 tests plus 81 subtests can be inherited for those unchanged paths. Repeating that full suite is not required for this candidate.** This is a scoped release conclusion, not a claim that the full suite was rerun. The detached execution environment must repeat its runtime/lock and focused preflight checks; dependency drift or a later core change would invalidate this reasoning.

## Resource and preservation boundary

The launcher performs admission before invoking the benchmark through the previously reviewed process guard. The child inherits an explicit checkout PYTHONPATH, two-logical-CPU affinity and a sampled 2 GiB process-tree RSS limit, with no elapsed/CPU-duration kill. Resource setup or monitor failure rejects execution; guard termination retains the consumed claim and partial outputs for reviewed failure closure. A successful process alone cannot substitute for complete cells, all local shards and independent terminal reconciliation. The runner has no source acquisition call; no additional chain bytes are authorized.

Preserve the fixed source commit, runtime and guard receipts and every output, including partial evidence on failure. Import terminal artifacts byte for byte after execution. Sampled RSS can miss brief peaks. No real-day resource or motif result has been inferred from the toy checks.

| Reviewed file | SHA-256 |
|---|---|
| CHARTER.md | `2fea0e9ef7045ee520fc92ff76abc657df6cc1a5dd54259e866c741cdd19c000` |
| gates.json | `c0c025c487352bf8c257f6c1d5ade3b97f8aef0a16540367d6133be74aef9a6f` |
| history.json | `093d7871629dff262c32d0689610c79b080951112069450583fe4986ce814fa7` |
| benchmark.py | `fd62f10d81d977cc6797dd796a9b101f5d285d3f43b1d081a18188e308d307aa` |
| oracle.py | `a0fa3bbdbd32165356724285be378bf67c76070b68bd838ba6169c944b9af249` |
| launch.py | `ba6621b8e1b2d7d7f2766b9a552acac8fd38dc6302cceafd32de133108023cc6` |
| tests/research/test_onchain_graph_motifs.py | `cefcd11c74450af964bcde17ec9216f27c81fd06dea511ff5855fb64e1e54275` |
| pyproject.toml | `fa319e95452e538ac22d78584f87f5f13a24d4b8bb83064b51d015706ef594b9` |
| uv.lock | `f7a1829c0ae554fb00923eb07c3c5e5370c1eadb52698ea657446c64d6c83b9a` |

Independent chain canonicality/day-boundary completeness, historical availability, multi-year resources, exact wei, null-adjusted significance, incremental prediction, fees/funding/cashflows and profitable low-exposure trading were not tested. None is admitted by this review. No unresolved correctness question currently warrants a higher-effort pre-execution review; full-day completion and evidence reconciliation remain the next empirical engineering question.

# Frozen one-day local temporal-motif engineering benchmark

September 16, 2026. The user's instruction to proceed after the one-day graph result authorizes this downstream computational benchmark. The question is whether the same normalized, retained Ethereum day can be represented without event loss and counted using Raphtory's local three-edge motifs within the existing two-CPU, sampled 2 GiB envelope. No forecast, price relationship, economic result or full platform is being tested.

## Ancestry, budget and exposure

This is a new substantive operation after source extraction and static graph aggregation, not a repeat of either original gate. The new mechanism `ethereum-local-temporal-motif-engineering` has cumulative prior_attempts=2 and attempt_budget=3: the source and prototype claims are imported once each, leaving exactly one new authorized motif attempt. Source allowance1/1 and prototype allowance2/2 remain unchanged. Their original registrations, receipts and 117-complete/2-unavailable prototype outcome remain immutable. The separate forensic artifact is declared prior inspection, not a third lifecycle claim or an admitted panel. Its normalization is independently specified for this new engineering consumer.

The API requires a parent to share the same mechanism. Therefore parent=null is a representation limit; the two scientific parent claims and terminal records, forensic result and input lineage are explicitly hashed inputs. This cannot reset broader budgets. The pre-start local inventory is 39 claims, not a global or independent-trial count. No other family, active external job or deferred case is changed. The day is already exposed development data; reuse is exploratory, never fresh confirmation. One attempt, no automatic retry, alternate date, timescale sweep, motif pruning, shuffled null or budget extension follows.

## Input and event semantics

Use only the 117 retained January 1, 2024 transaction column responses, retained footer and block sample, and named parent evidence. No chain network request or new field. Preserve and validate request URL/ordinal, exact ranges, ETag, byte lengths and hashes; use the existing acquired-interval sparse reader and footer plan. Revalidate every transaction against the retained blocks with unchanged original hash/address/status/value/position/chain checks.

Only exact case-sensitive recipient text `"None"` becomes missing. No trimming or other null-string substitution. All 777 affected rows remain in the 1,101,465-row denominator and are excluded from edges. This is not proven contract-creation attribution. The complete normalized integrity report and deterministic static pair fingerprint must match the independently reviewed forensic result; otherwise fail before motif counting. Ordinary valid hexadecimal addresses are lowercase-normalized as before. No values are summed: DOUBLE amounts only supply the existing zero/positive inclusion predicate.

Eligible events are successful positive-value non-self top-level transactions with valid addresses, as in the preceding graph. Exclude reverted, zero-value, self-transfer and missing-recipient rows with unchanged precedence. No token/internal transfers, entity attribution, monetary weights, fee or balance inference. This graph is a declared subset, not complete ETH flow.

Sort eligible transactions by `(block_number, transaction_index)` and verify uniqueness plus nondecreasing block time. Pass exact integer Unix seconds to Raphtory and assign a globally unique ordinal as `event_id`. This preserves repeated same-second same-pair events and same-block execution order without timestamp perturbation. It defines strict total chain ordering of events; it does not assert strictly increasing physical timestamps or causal relationships between transactions.

## Motifs and boundary

Pin Raphtory0.17.0 in the optional onchain dependency and uv.lock under Python3.13.13. Use only `local_temporal_three_node_motifs(graph, delta=3600, threads=2)`. Count three ordered, distinct events on up to three nodes with last-minus-first timestamp <=3600 seconds, inclusive. All three events must lie inside `[2024-01-01T00:00:00Z,2024-01-02T00:00:00Z)`. There is no external lookback or overlap: motifs crossing midnight are outside this day-induced benchmark. A later rolling feature would need explicit context and completion-time attribution; this result is not such a feature.

Retain all 40 local role counts for every incident address, including all-zero vectors. Slots0–23 are PRE/MID/POST star-center roles with direction order III,IIO,IOI,IOO,OII,OIO,OOI,OOO. Slots24–31 are two-node endpoint roles in that direction order. Slots32–39 are the eight temporal triangle patterns listed in the pinned documentation; each triangle appears at all three vertices. Do not treat the sum of Local40 as unique motifs. Unique occurrences equal total star-center roles + half total two-node roles + one-third total triangle roles. Check complementary two-node sums and triangle divisibility. Preserve local sums/squares/maxima/top-node shares and top10 addresses by local-role participation. Concentration is a diagnostic, not an adoption cutoff or a prediction statistic. No motif configuration is selected from these counts.

## Checks, outputs and resources

Before real data, pass exhaustive enumeration of all216 ordered three-edge combinations on three invented nodes, 80 seeded14-event graphs at four deltas, same-second duplicates, reverse insertion and exact3600/3601 boundary cases, plus offline projection/export fixtures. The exhaustive oracle is a small independent specification implementation, not a replacement production counter.

During the registered run, verify up to six bounded induced event samples: select the union of the three highest distinct-neighbor out-degree and three highest in-degree addresses, breaking ties by address; take each center's first30 eligible incident events. Enumerate all event triples independently and compare all local vectors with Raphtory. Retain exact sample events and expected counts. These subset checks do not claim to reconstruct those centers' whole-day counts.

Publish integrity/ordered-event fingerprint, bounded oracle evidence, graph build receipt, 39 deterministic address-sorted local JSON-lines shards of at most10,000 addresses, complete motif summary and phase timings. Five cells are registered: input-semantics, ordered-events, bounded-oracle, local-motifs and local-export. All must reconcile; retain every claim, failure, unavailable or incomplete output. Unexpected exceptions fail the run without retry. Resource termination may leave a claimed incomplete attempt; retain it and close only through reviewed failure handling, never erase or relaunch.

Commit and remotely verify charter, exact gate, code and lock before actual outcomes. Run once in a dedicated detached fixed-HEAD checkout using its own locked .venv, two logical CPUs and the previously reviewed sampled2GiB process-tree RSS guard. No elapsed/CPU-duration kill. External requests are allowed only for dependency/documentation preparation before execution, not within the benchmark. Guard resource receipts are separate from run outputs. A guard pass supplies an observed cost, not a worst-case bound. Source and input hashes must remain stable; import all resulting members byte-for-byte and independently reconcile the result before any success claim.

The unchanged named offline suite passed2060tests+81subtests before the prior prototype. For this additive dependency and isolated runner, revalidate runtime/lock, unchanged existing dependency versions, all four graph test modules and lifecycle/admission tests; inherit broad unchanged-core verification subject to independent release review. No code-path change to accounting, existing run helpers or original graph math is intended.

## Interpretation and sources

Success means the exact Local40 operation completed with preserved event identity, passing synthetic/subset checks, complete local exports and no resource violation on this day. A source-semantic, library-correctness, memory or execution failure is a specific benchmark outcome. Neither result establishes multi-year coverage, worst-window scalability, null-adjusted motif significance, publication timing, independent canonicality/day boundaries, incremental forecasting value or profitable low-exposure trading. Source feature-panel and financial admission remain false.

Primary references: [Raphtory0.17 algorithm roles](https://raphtory.readthedocs.io/en/v0.17.0/reference/python/raphtory/Modules/algorithms/), [explicit event_id API](https://raphtory.readthedocs.io/en/v0.17.0/reference/python/raphtory/Classes/Graph/), [pinned local implementation](https://raw.githubusercontent.com/Pometry/Raphtory/v0.17.0/raphtory/src/algorithms/motifs/local_temporal_three_node_motifs.rs), [pinned motif counter](https://raw.githubusercontent.com/Pometry/Raphtory/v0.17.0/raphtory/src/algorithms/motifs/three_node_motifs.rs). Synthetic tests verify the consumed semantics rather than relying solely on prose documentation. The published forecasting paper and original spec remain distinct methods; this benchmark is not a reproduction of their predictive results.

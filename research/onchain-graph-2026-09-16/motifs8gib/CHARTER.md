# User-authorized 8 GiB resource-only successor

September 16, 2026. The user explicitly instructed: "raise that to 8 GiB" after the 2 GiB Local40 memory failure. This authorizes one resource-only successor of eth-temporal-motifs-20260916. The previous [motif charter](../motifs/CHARTER.md), original gate, failed claim, three partial outputs, guard receipt and closure remain unchanged. The computation is not redesigned to obtain a favorable result.

The sole substantive change is the sampled process-tree RSS allowance from 2,147,483,648 to 8,589,934,592 bytes. Keep two logical CPUs, 20 ms nominal sampling and no elapsed/CPU-duration kill. Sampled peaks can overshoot and the allowance does not reserve host RAM. Observe resource failure if the guard triggers; no automatic retry follows. A resource failure may require the same separately reviewed failure-only closure, never deleting or replaying a claim.

Run the original byte-identical benchmark.execute using the same pinned Raphtory0.17.0, Python3.13.13/uv.lock, 128 hashed inputs, inclusion/normalization rules, sorted block/index event IDs, integer-second timestamps, inclusive delta3600, day-induced boundary, six bounded exhaustive checks, Local40 role definitions, all39local-vector shards, five cells and 44outputs. Original benchmark/oracle/projection/graph math/dependency hashes are preserved. The new wrapper only supplies amended lifecycle identity; the new launcher supplies the requested memory allowance and preserves the resource receipt if a child exits between a TERM timeout and KILL escalation. This guard race fix does not change the computation or resource threshold. The original raw-data interpretation, 777 sentinel exclusions and historical/financial non-admission remain intact.

The existing single-amendment API preserves the same family mechanism and prior_attempts2 / attempt_budget3 object. Its certificate binds the one existing failed motif claim and grants exactly one additional attempt, effective cap4. The scientific parent is explicitly eth-temporal-motifs-20260916. Source/prototype claims remain imported once through prior_attempts2; the separate forensic artifact remains inspected history, not a lifecycle trial. No family rename, financial trial or fresh confirmation is introduced. The 40 local pre-start claims and their terminal metadata are recorded in history.json; this is not a global independent-hypothesis count. The registered windows, datasets and original experiment object remain unchanged. Amendment chaining is prohibited by the helper.

The API calls its immutable source partition economic_source_files for historical reasons; this benchmark has no economics. That partition preserves the original computation and dependencies. The source partition manifest records the old versus new launch/wrapper harness separately. Inputs, cells and experiment invariants are canonical-byte-equivalent to the parent. Source/input binding, resource receipts, all attempted and absent outputs, and final raw import verification remain required.

Before real execution: independent review must verify the resource-only change and full certificate/source preservation; synthetic guard tests must cover below8GiB, exact8GiB, above8GiB, monitor/setup failure, ignored-TERM escalation and arbitrarily long simulated completion without an elapsed-time kill. Pass the unchanged graph correctness tests and amended-lifecycle tests, plus the isolated locked runtime preflight. Inherit the already independently reviewed broad unchanged-core tests rather than repeat them without cause. Commit and remotely verify the full gate/certificate/source before one execution in a dedicated detached fixed-HEAD checkout with its own local locked environment.

Retain every Local40 vector if the run succeeds, then independently reconcile raw event/order identities, bounded samples, local roles/concentration and available full-day arithmetic checks. If it fails, preserve the exact partial evidence and guard result without calculating missing full-day counts during review. No prices, fitting, new chain downloads, provider contact, account action, paid resources, orders or background schedule. Success would establish only computational completion of this fixed one-day motif operation under the new resource envelope; full-history scalability, historical availability, statistical significance and incremental forecasting/economic value remain untested.

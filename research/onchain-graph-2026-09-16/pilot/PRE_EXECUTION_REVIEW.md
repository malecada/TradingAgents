# Independent seven-day pilot pre-execution review

September 16, 2026. **Approve one execution of the exact frozen `eth-seven-day-pilot-20260916` contract below**, after committed/remote source verification, isolated locked runtime and normal admission. No material blocker remains in the reviewed candidate. No actual new transaction rows, empirical pilot outcomes or prices were inspected during preparation.

## Scope, history and limits

The user's explicit choice of the seven-day integrity/boundary pilot and lossless storage comparison supports this distinct downstream engineering question. The family imports the five exact predecessor claims once as prior_attempts 5 / cap 6. Original family objects, spent allowances, the 2 GiB failure and exhausted motif amendment remain unchanged. The forensic artifact is inspected history rather than a lifecycle attempt. All 42 inherited local claims and 83 metadata hashes are retained; these are not global independent-hypothesis counts.

Independent hash checks verified 134 registered inputs, 27 sources, six lifecycle runtime hashes, the charter, plan and inherited metadata. The target is January 2–8 UTC; retained January 1 supplies prior context and the fixed compression baseline, and only the January 9 block object closes the last boundary. The broader registered exposure window contains the inventory and boundary observations. There are 17 cells and 21 lifecycle JSON outputs, including seven source cells, seven motif cells, context, closing boundary and storage. Binary evidence has a separately verified complete manifest.

The fixed acquisition uses existing listed object key/size/ETag, with no relisting, retry, replacement date or source. Limits are 2,100 actual requests, 2 GiB aggregate newly received bytes, 32 MiB response payload plus an overflow detection byte reserved inside the aggregate ceiling, 30-second network-operation timeout, 272 MiB and 291 requests per target day, 256 MiB selected transaction columns, 16 MiB block objects, 4 MiB footer metadata plus trailer, 32 row groups, two million transactions per day and 8 GiB logical sparse-file size. Maximum normal bounded requests are 2,038. The reviewed launcher retains 8 GiB sampled aggregate process-tree RSS, two logical CPUs and no elapsed/CPU-duration kill. Worker children inherit the guarded process group; they do not create an unmonitored memory allowance.

## Resolved pre-execution findings

The initial draft lacked final raw-blob validation, explicit cross-day transaction uniqueness, effective per-day limits and block-schema checking; an ordinary unavailable worker phase also returned a status that would abort the root immediately. These have been corrected before any claim. Final validation reopens every metadata-bound compressed blob, checks raw/stored lengths and hashes, verifies bounded decompression and rejects unbound blobs or unresolved acquisition intents. Exact 32-byte transaction-identity shards support cross-day uniqueness including context, with covered/unavailable source-day denominators retained. Daily limits, required block types and matching tail/footer trailers are enforced. Known unavailable phases now publish their immutable result and return normally, allowing all fixed cells to be retained; unexpected process failures still fail the run.

The new evidence is exclusively published using fsync and non-overwriting links. Old base64 JSON is not replaced. Before completion, the final manifest covers acquisition intents, response receipts, compressed raw bodies, event/prefix/transaction-identity shards, local vectors and phase records. Abrupt resource termination may leave partial evidence without a terminal manifest; that state is not successful completion and cannot be replayed automatically. A subsequent failure-only preservation review would need to bind the existing evidence. Lifecycle JSON verification alone does not verify the separate binary tree.

## Numerical and storage semantics

Raw daily rows retain the existing category precedence, with exactly literal `None` normalized to missing recipient and explicitly counted. Other invalid fields fail admission. Integer-second eligible events use stable `(block_number << 32) | transaction_index` IDs. Within-day transaction positions, hashes, counts, source order and block linkage are checked; adjacent block-number, parent-hash and time links are required on both sides. These remain internal archive checks, not independent canonical-chain verification.

Completion-day Local40 is the inclusive-one-hour extended-window count minus the preceding-hour count, retaining overlap-only nodes. The fixed isolated-day diagnostic measures cross-midnight contributions. Completion and isolated sums are retained separately, with nonnegative differences and role conservation. Three fixed boundary-centered induced subsets per available day provide exhaustive triple checks; they do not establish a complete independent recount of whole-day star/triangle motifs. The closure checker will reconstruct raw events, all exports and whole-day completion dyads without rerunning the production full Local40.

The compression benchmark compares precisely the same 117 retained January ranges as original JSON receipts, raw payload, theoretical base64 payload and zstd level-3 payload. Round-trip byte equality, codec/runtime binding and raw/stored hashes are required. Blob metadata and derived-artifact overhead remain separate from payload compression ratios; neither eight observed days nor the baseline establishes a full-history storage guarantee. Phase timers include graph construction, validation/subtraction and collection as applicable, and are not represented as isolated library-call times.

## Verification and exact reviewed bytes

The focused pilot/worker/storage/boundary/projection/motif suite independently passed 53 tests and 16 subtests in 2.26 seconds. An additional independently constructed root-level unavailable/denial case retained all 17 unavailable cells and all 21 outputs, and made no source child calls after the boundary denial. This specifically checks the repaired worker/orchestrator interface. Existing tests cover binary corruption, bounded round trips, no overwrite, partial-body retention, denial/request limits, schema drift, sparse-hole prevention, cross-day duplicates and both closing links, plus invented completion-day topology enumeration. No broad financial suite or actual numerical experiment was repeated.

| Artifact | SHA-256 |
|---|---|
| gates.json | `ba525b15cfa20abf89170051825434808eff1143d44651a1372d36c634f21693` |
| CHARTER.md | `621d823603d2843f20cbb25c54e697214d90f60a190e235a66269e84a122fccc` |
| plan.json | `2b923052fec66ddc2120e78e65d0815e950dab27e4d01f1745f261bfeaa0613a` |
| history.json | `3cdb0a33c097841106a4fa4e0595cafe5ced77d373f5bb974bb0008a04a96c52` |
| numeric.py | `3343a8fb9fe77b71cfdb35ec6113d0eb7c4b24e18a42436bc0a277f277bdd11f` |
| storage.py | `bb7fedc103949d58a7ff27665e102126f5784f40f76f0794eae4fb056a5dd086` |
| day.py | `b96e3f02847d1270b9ec3aefa6fa4d71cdd3ae456d15caa74e309d4ecc43ebae` |
| run.py | `3aa24d9a9e15fcfa4093f75e9162b313093b2866d8bf782fa3ef41161d058659` |
| launch.py | `ac9ec904d772640c7828d7bfa0b35a2bdf86ddd2fe10ed1bc401f94b34062a85` |
| preparation-tests.json | `4b426c3e17a37d15466e4cf6d00e9d868b7614b7d41fca82dd3ffda8b54a89c1` |

Actual acquisition, unavailable outcomes, raw recoverability, cross-day empirical identities, compressed exports, terminal/import integrity and external backup remain to be checked after execution. Historical publication availability, feature usefulness, predictive inference, prices, returns and financial admission are outside this verdict. Success or failure consumes the single declared allowance; no automatic follow-up is granted.

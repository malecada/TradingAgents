# Interrupted full-panel continuation

The user authorized resuming feature generation after a reboot and requested
ongoing monitoring and recovery. The interrupted predecessor is retained as a
failed attempt with 206 published, independently checked source days through
2022-07-25 and 205 graph days. It contains 234,828,370 source rows. The original
resource receipt is empty. The user reported OOM while other processes were
running; the responsible process, peak memory and termination signal are unknown.
No causal attribution to feature generation is made.

This is one operational continuation, `eth-full-history-feature-panel-resume-20260922`,
with cumulative prior 14 / cap 15. All fourteen predecessor claims, terminals,
original gates and source identities remain immutable. It is retrospective
numerical engineering on the same exposed sample, not a new financial hypothesis
or a reset of prior budgets. No prices, labels, models, acquisition or trading
are authorized by this continuation.

## Retained checkpoint and unchanged numerical contract

The original fullpanel/plan.json and numerical source remain byte-identical.
The original charter's transaction eligibility, Local40 definition, boundary
rules, independent daily reconstruction and expected populations remain binding.
There are 1,096 source dates, 1,094 required graph dates, 2,193 cells and 1,099
outputs. Only 2022-01-01 and 2024-12-31 are expected unavailable graph dates.
Historical available_at remains null. Full-day stars and triangles have bounded
independent subsets and conservation checks, not exhaustive independent recounts.

A separately preserved archive binds all original files, including partial
July 26 evidence. The new plan's 1,623 seed files bind 206 published daily outputs,
593 prefix files and their daily checks, cleanup and exact-hash receipts.
Original output bytes are republished without numerical reconstruction. Only
these completed-date artifacts are restored into the new execution namespace.
The 112 unpublished July 26 scratch files and three orphan prefix files remain
in the preservation archive and original checkout; none enter the execution seed.
New extraction begins 2022-07-26 and processes only the remaining 890 dates.

The original hash root retains global day indices 0–205 and 7,514,507,840 payload
bytes. It is immutable. A separate exclusive root receives indices 206–1095.
No second full hash copy is made. Exact global uniqueness covers the concatenated
old/new bucket population, including all graph-unavailable dates and reused
pilot source days, with full 32-byte identities. Independent closure uses a
separate implementation and sort, reconstructing per-day streams and exact
receipt/segment attribution. Any missing source, duplicate or unexpected daily
failure blocks whole-panel admission and leaves every later cell explicit.
The strict reviewed phase-v2 histogram serialization correction is reused only
for reconstructing original phase hashes; numerical code stays unchanged.

## Resource and ownership contract

A single transient local user systemd cgroup contains the entire compute tree,
including daily extraction and independent subprocesses. Kernel memory.max is
6 GiB, memory.high 4 GiB and memory.swap.max 512 MiB. The 6 GiB hard limit is
stricter than the user's 8 GiB upper ceiling. There is no claim that the original
sampled 8 GiB guard produced a valid exit receipt after reboot. Two CPU affinity
slots and two-thread library settings apply. No elapsed-time job kill applies.

Startup waits at most 300 seconds for 9 GiB MemAvailable before releasing the
worker; running MemAvailable below 3 GiB stops the whole cgroup. A 15-second
monitor lease stops the child unit if its external monitor disappears. Kernel
controls are read back before release, receipts preserve actual memory events,
and the unit must be empty at terminal cleanup. This bounds the feature job;
it cannot guarantee that unrelated processes do not exhaust the machine.
A failed guard or missing terminal evidence admits preservation only.

Original disk ceilings remain: 20 GiB free on both filesystems, 2 GiB retained
artifacts/metadata, 2 GiB daily scratch and 40 GiB combined allocated hash roots.
The continuation's retained accounting also includes its preservation archive
and resource receipts. Both roots' corresponding buckets have a combined 256 MiB
ceiling; each chunk is at most 32 MiB. The new append total cap subtracts the
old immutable payload, while phase preflight also counts allocated metadata.
No existing raw store, prefix or global hash payload is deleted. Only newly
completed daily scratch is released under the original exact-manifest contract.

## Admission, monitoring and closure

Commit the exact gate, charter, history, plan, source/test bindings and independent
approval before any claim or new raw decode. The named offline target must pass
and the frozen source must be remotely verified before launch in the exclusive
Data continuation checkout. A live job has one owner. A further interruption
requires preserved evidence and separately reviewed continuation; claimed or
terminal identities are never replayed.

Success requires every original full-panel numerical criterion plus clean
kernel-guard evidence and independent final review. Retain failed/unavailable
cells and all attempts. Import compact evidence immutably with byte hashes,
commit and verify remote backup before closure. Global hash scratch remains for
independent closure; raw stores remain local on the same physical disk, without
a verified off-device full raw backup. Matched prediction evaluation is a later
user decision. No prediction accuracy, return or validated strategy is established.

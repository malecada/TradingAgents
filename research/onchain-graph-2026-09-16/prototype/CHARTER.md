# One-day Ethereum graph projection and resource benchmark

Authorization: September 16, 2026, “good, go ahead”, following the source result and its proposed one-day extraction, integrity check and graph-computation benchmark. Exactly one downstream source/engineering claim is authorized here. No prices, models, forecasts, monetary volume, balances, exact wei accounting, paid services, account actions, provider contact or production work is included.

## Lineage and budget

The scientific parent is terminal `eth-graph-source-20260916`, execution `5f318f545d230babdfee995b720a548b9e940052`, imported and independently reviewed at coordination commit `d948c9f761eb4fc7d28418330b5498a70dd99a1f`. Its original family remains exhausted at 1/1 and unchanged. This is a new downstream projected-column/integrity/topology measurement, not a rerun or rescue of the old full-object sampling request. All parent source observations, unavailable full transaction sample and sample exposure remain retained.

The successor mechanism `ethereum-native-transfer-graph-prototype` imports that original attempt once as prior_attempts=1 and permits cumulative cap=2, leaving exactly this one new attempt. The lifecycle API cannot represent cross-mechanism parents or extend a frozen family dictionary; API parent=null therefore does not mean scientific independence or zero ancestry. The exact scientific parent's claim, terminal receipt, source audit, footer, block response and import manifest are hash-bound inputs, and its structural verification is required before execution. No original family grant is changed or unrelated unused slot consumed. All 38 consolidated local claims are pinned in history.json, with original37 history and separate active external F1 scope inherited; these are administrative records, not independent statistical trials or a global total. Wallet10, broader allocation188/189, overlapping related44 and DeFi successor8/11 remain inherited without summation or reset. No financial allowance follows.

## Fixed capture and reconstruction

Use the exact January 1, 2024 transaction object, ETag, nine columns and 117 nonoverlapping physical column ranges frozen in plan.json, derived from the already retained footer. No new listings, HEAD requests, footer downloads, alternate dates or objects. Expected 1,101,465 rows in 13 groups. This explicitly supersedes the parent's 100,000-row sample restriction for this separately authorized projection; one group has 105,192 rows. The prior complete-file skip remains unchanged.

117 GETs maximum, one per range, sequential, anonymous HTTPS at the same public AWS host. Request every range once unless source denial suppresses later requests. Send If-Match and require HTTP206, exact Content-Range/ETag/body length. No automatic retry, redirect, ambient proxy or credential discovery. HTTP401/403/429 stops further requests to the source. Twenty-second per-request socket timeout; no overall elapsed/CPU-duration kill. Maximum response16MiB+1 sentinel, retained raw body total128MiB+1 sentinel; planned successful bytes118,730,958. Preserve all response/error prefixes and every durable request intent. Unknown external-kill outcomes remain unknown and nonrepeatable.

Validate frozen offsets against the retained Parquet footer before network: reject external file references, overlaps, inconsistent group/column/row denominators, invalid bounds and budget excess. Reconstruct a sparse temporary local projection with original logical length1,572,869,635bytes (hard maximum2GiB), acquired columns and retained footer/header only. Its data+footer allocation is at most128MiB plus4MiBfooter and filesystem bookkeeping. An acquired-interval reader rejects any decoder read touching holes; the retained metadata is supplied directly to Arrow. This is not the full original Parquet file. Delete the temporary projection on ordinary exit; after a forced kill, the anonymous temporary file closes with the process. Immutable raw range receipts plus retained footer and implementation suffice for deterministic reconstruction. Do not redownload for review.

Use pinned Python3.13.13/uv.lock, two CPU cores and reviewed sampled aggregate2GiB RSS. Preserve resource report outside the lifecycle run. Run only in a new detached fixed-HEAD checkout with all38 inherited claim metadata/hash anchors verified, local locked runtime and parent structural verification. No coordinator checkout mutation may alter execution HEAD. Copy exact terminal evidence back and verify all member hashes before any successor.

## Row integrity and graph definitions

Decode only the nine registered columns in row groups. Interpret the source's timezone-naive timestamp[ns] values explicitly as UTC and compare integer nanoseconds to retained block timestamps. Admit only if all117ranges, all13groups and all1,101,465rows decode, expected total agrees with block transaction counts, and every integrity condition passes:

- Retained blocks have valid distinct hashes, consecutive heights, increasing timestamps within [2024-01-01T00:00:00Z,2024-01-02T00:00:00Z), and consecutive parent links.
- Transaction and block hashes are 0x-prefixed64hex; sender and non-null recipient are 0x-prefixed40hex. Normalize hex case to lowercase for graph identity and binary transaction hashes for uniqueness. No entity clustering or label mapping.
- Each transaction hash is unique. Block number/hash/timestamp must match a retained block. Integer index lies in [0,block.transaction_count), every index appears exactly once, with no missing/duplicate positions. Total rows and unique hashes reconcile.
- Receipt status is integer0 or1; value is finite, nonnegative. Floating value is used only for zero/positive classification, never monetary sums or thresholds. Null recipient is an explicit exclusion, not an invalid field.

Measure source-file order inversions. Unsorted source rows are allowed because this static graph is order-invariant; the unique (block_number,transaction_index) supplies canonical event order for a later temporal study. No block-external boundary headers are fetched. Internal consistency does not prove independent canonicality, full-day boundary completeness, original publication or historical ingestion availability. Only the retained day participates; no prior-day state or cross-day edges.

Mutually exclusive category precedence: invalid row, then reverted receipt, then null recipient, then zero value, then self-transfer, else graph event. Also retain nonexclusive flags for failed/null/zero/self cases. Category counts must sum to every decoded row. Any integrity failure makes graph unavailable; valid subsets cannot become an admitted graph.

Each graph event is a successful, positive-value, non-self top-level transaction between address-identity nodes. Contracts are included where recipient is non-null; internal transfers, gas and token events are absent. Repeated directed pairs retain event counts. Degrees count distinct counterparties, not events, and include zero in/out-degree for all endpoint nodes. Freeze these static metrics: nodes, directed pairs, events, repeated events/events, reciprocal directed pairs/directed pairs and reciprocal dyads, in/out-degree histograms/sums/maxima, and sender/recipient event-count HHI=sum(count²)/events² with numerator retained. Ratios are null for empty denominators. Preserve a deterministic SHA256 of lexicographically sorted normalized (sender,recipient,count) lines. These are exploratory count/topology features, not temporal motifs. Raphtory motif compatibility/counting is a separate later dependency; no hand-rolled motif algorithm or GNN is introduced here.

## Outputs, verification and interpretation

Complete denominator:117range cells plus integrity and graph (119cells). Preserve234request intent/response JSONs,13group results, integrity.json, graph.json and benchmark.json (250outputs), including not-attempted/unavailable cases. An exception/external kill retains failed/incomplete lifecycle evidence and consumes the allowance; never rerun this identity. Resource/preflight/import records remain separate and hash-bound at closure.

Record acquisition seconds, combined decode/integrity/pair-count seconds, graph-summary seconds and sampled aggregate peakRSS. No linear full-history or motif scaling claim is permitted from one day. Synthetic checks must exercise real Parquet sparse projection, hole-read rejection, source denial, complete exclusion accounting, duplicates/missing indices, block/time mismatches, invalid status/amounts, normalization and order independence. Independent pre-execution and raw-evidence closure review are required.

Success means a byte-recoverable, internally reconciled one-day static graph within the resource envelope. Failure classifications are access, object change, incomplete extraction, row integrity, decoding or resource failure; none establishes absence of predictive value. Historical availability, independent canonicality/full-day completeness, multi-day scalability, temporal motif cost and M0/M1/M2 predictive/economic comparisons remain unadmitted. Review this bounded result before selecting the next dependency; no additional empirical child is preauthorized by this contract.

# One-day projection result

Extraction and decoding are feasible within the registered resource envelope. The strict graph-admission gate did not pass:777recipient values use the literal string `"None"`, which the frozen validator correctly rejected as a non-address. The original result remains COMPLETE with117complete and2unavailable cells; no graph metrics were admitted.

All117range requests succeeded, retaining118,730,958rawbytes. All1,101,465transactions decoded with unique hashes, complete block indices and matching block hashes/timestamps. No other integrity error was found. Independent forensic reconstruction confirmed the sentinel diagnosis and every imported byte; see[CLOSURE_REVIEW.md](CLOSURE_REVIEW.md).

Whole guarded run:570.36seconds, sampled peak process-treeRSS1,029,378,048bytes (approximately982MiB), no resource kill. Acquisition/preservation phase525.40seconds; decoding, integrity checks and pair counting8.46seconds. Timings include the registered single-request transport and retention overhead; they are not motif benchmarks or full-history scaling estimates.

The raw dataset and original failure are preserved. A separately frozen offline forensic reconstruction may classify only exact `"None"` as absent and keep all affected rows excluded from edges. Such diagnostic graph metrics cannot override the original gate or be called a newly admitted feature panel. No financial outcome, source timing guarantee, independent chain completeness or forecast advantage follows.

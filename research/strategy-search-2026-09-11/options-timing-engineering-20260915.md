# Timing successor engineering checkpoint

September15,2026. The conditional information-value review supports preparing one
fixed acquisition-phase correction after Decision20. No sixth allowance, new
absolute window, empirical claim or new VPS collector is admitted yet.

The source/closure/readiness checkpoint46edb5d39512b502e9824f9a4193ea4338b15cf5
was pushed and independently queried equal. Old research_options_capture and all
four legacy runtime packages, original policy and all20terminal claims are frozen.
New source copies live only in tradingagents/research_options_timing. Economic
engine/selection/batch source remains the previously frozen signed-cashflow code.

## Ownership and interfaces

- Capture worker: successor schedule/worker/journal/transport and focused/full
  invented lifecycle/resource fixtures. Journal and transport may remain
  byte-identical if their existing three-second slot support suffices.
- Lifecycle verifier: successor control/independent_verify/closed_history, exact
  grant/closed-parent schema, disposable20-history attacks and actual read-only
  history resource verification. No actual certificate/claim publication.
- Coordinator: adapter/funding/returned/analysis, bootstrap/package/analyze scripts,
  policy/gates/certificates, integration, state and commits. No actual grant until
  independent source/financial/resource review passes.
- Independent reviewer: policy, history, timing, financial/source and resource
  adjudication. No implementation edits or market requests.

Hourly slot scheduled_ms is N+2000; deadline_ms is N+5000. The validated
schedule.nominal_ms(entry_ms,slot) recovers N. Adapter.decode accepts nominal
scheduled_ms plus explicit hourly=True to enforce the phase separately from the
unchanged deadline/freshness clock. Initial selection and BATCH.assemble receive
nominal N. Funding _read and announcement-age tests use the nominal helper;
prospective announcement validation uses actual retained receipt availability.
Returned benchmarks independently enforce the new phase while keeping nominal
hourly denominators. Every consumer of slot time needs this explicit distinction.

Target options-timing-20260915, sole host pck-preds-1, sole new data root
/opt/thesis-research/options-timing-20260915/data. Protocolschema3 adds exactly
hourly_acquisition_delay_ms=2000. Assignment format remains1 with exact copied
claim binding; worker/returned admission checks the claim's schema3 phase.
The new package has the same eight source-role members under its new namespace.

## Required sequence before source admission

1. Complete isolated interfaces and adverse timing/restart/entry/exit/funding tests.
2. Independently reconstruct closed twentieth protocol and all20predecessors;
   prove corruption, omission, wrong quiescence and duplicate-descendant refusal.
3. Complete representative full-calendar retained-prefix/staging/seal proof,
   realistic shared-three-second entry/selection budget, returned-source/known
   eight-book wrapper and actual20-history/output/terminal resource envelope.
4. Run named offline target and focused later changes; retain failures without
   mutating original evidence or claiming old tests cover later source changes.
5. Independently review exact final policy/source, choose one future UTC hour,
   bind closed-parent/history/target and effective6 certificates, commit and push,
   then separately query remote equality. No phase or date optimization on outcomes.
6. Admit exclusive new claim, independently verify, build and verify distinct
   release/runtime/data/host identities. Complete finite VPS launch/return contract.
7. After that one experiment, diagnose, independently close, record and re-rank.
   No automatic seventh allowance or financial look during capture.

Value_rev readiness is separately preserved:20tests+4subtests,54independentchecks,
704verified raw members and actual VPS backup. Its source/clock clarifications
remain unregistered; it is not a current P0 or additional financial experiment.

## Engineering checkpoint at 09:34 UTC

The new source/funding/returned/accounting tests pass66cases in13.00seconds.
The distinct release/bootstrap/analysis-entry tests pass10cases in27.68seconds.
The lifecycle owner reports24control/independent/closed-history cases passing
in149.87seconds; an initially malformed extra-claim fixture was corrected to
exercise a committed unadmitted descendant. These are invented offline checks,
not empirical reruns. Full named offline validation remains pending.

The closed-parent-only certificate options-closed-parent-20260915.json binds the
actual failed twentieth claim, all eight unavailable cells, zero financial
outputs, no analysis intent, original fifth grant and independently verified
external quiescence. This certificate grants no sixth attempt or new window.
Its committed binding is the next read-only verification step. Full lifecycle
compressed source-loop timeouts are retained and resumed only at future unused
slots; the first overlapping synthetic-timestamp fixture is not stronger
chronology evidence. Corrected fixture v2 distinguishes sequential known/selected
raw receipt clocks while preserving their shared absolute deadline.

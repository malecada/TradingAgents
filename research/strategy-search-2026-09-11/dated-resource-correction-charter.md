# Resource correction of the failed dated episode

Experiment `dated-book-resource-correction-20260911`, child of the preserved
failed `dated-book-20260911`. This is the third and final new dated investigation;
historical one + archive one + failed book one + correction one exhaust cap four.
This attempt is not a new economic hypothesis or an additional independent sample.

The original run used source d287a1420d70e298bae9b0f7e69fdc79c665720e, pushed and
remotely verified before execution. It failed at the lazy statistics import with
MemoryError under shell `ulimit -v 524288`. No declared outputs were written.
The eight intended cells and failed receipt remain immutable. Computed in-memory
books are not fresh or unseen data; the entire original exposed episode remains
exposed. No financial value was inspected by the coordinator before this correction.
The command excerpt and independent recovery review retain that distinction.

All economic rules in dated-book-charter.md, all four source input bytes, eight
case identities, dates, quantities, fees, cost stresses, numerical screens,
statistics and cash-book/runner source remain EXACTLY unchanged. The correction
runner preloads the real statistics dependency before ResearchRun admission,
then calls the unchanged evaluate function once. It declares books.json,
summary.json and resource.json. A separate exclusively created monitor report
is retained outside the run ledger; it never overwrites declared outputs.

## Explicitly changed resource enforcement

The earlier charter's 512 MiB memory metric was ambiguous. Its actual invocation
limited virtual address space, which can include large unloaded/shared mappings.
This correction explicitly uses resident memory, not that virtual cap. This is
a disclosed resource-procedure change, not a claim the original contract already
specified RSS. The financial child clears inherited RLIMIT_AS, is restricted to
at most two allowed CPUs and sets BLAS/OpenMP/NumExpr to two threads. No network
or empirical scope expansion is introduced. Wall time remains 120 seconds.

The financial runner admits no detached/background children or subprocesses
launched by secondary threads; its transient Git children are synchronous.
The standard-library parent samples this leader-thread process-tree RSS every
20 milliseconds and terminates the process group on a sample above 512 MiB,
wall timeout or monitor error. Shared pages can be counted more than once;
brief transient overshoot between samples is possible. Linux ru_maxrss is also
reported, with units explicitly KiB. It is the largest individual child's peak,
not aggregate memory. The financial child records its own historical RSS peak
and refuses completion above 512 MiB. Resource evidence does not validate profit.

Before commit, run the invented full eight-book pipeline with real statistics
under exactly this wrapper. Synthetic checks must demonstrate both success and
termination of an oversized resident allocation and a wall timeout. Independent
review must check the failed parent, unmodified financial sources, budget,
wrapper, exact preflight and new gate. Source and failed parent are committed and
remotely backed up before corrected financial execution.

On success, independently reconstruct all eight cash books/exposure statistics
from raw inputs without importing financial implementation, record the economic
decision and switch to the highest-value eligible question. On another failure,
preserve it and defer the dated question for an explicitly reviewed future budget
extension; do not silently rerun or reopen the cap. Zero validated strategies
follows from resource success alone.

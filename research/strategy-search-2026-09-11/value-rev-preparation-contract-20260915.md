# value_rev additive preparation contract — September 15, 2026

Status: conservative preparation decisions adopted under the resumed research
instruction, before P0, any ratio/panel evaluation or financial result. Source
admission, capture admission, stage admission and financial admission remain
false. This document creates no trial allowance or empirical identity. It does
not schedule a second snapshot. The original charter, gates, source, raw bytes,
spent samples and four-cell denominator remain unchanged.

## Authority and preserved originals

The September 4 charter SHA-256 is
`9a4fdb142af4ad259d3485bcb00fa4e704262b9246b14660b5e8d7710ab0252a`;
canonical named `gates.json:value_rev` SHA-256 is
`01cb96c80f16b4df5cee5787441bbe47ade1741896ae3043b49eb1d8240c127b`.
These additions make previously ambiguous preparation conventions explicit;
they are not represented as existing September 4 requirements. The independent
[decision review](reviews/value-rev-preparation-decision-review-20260915.md)
provides the old-clause mapping, rationale and remaining source requirements.
Any future empirical registration must bind this contract and its independent
review, exact source inventories, executable semantics and all cumulative history.
It cannot bypass any original numerical gate, admission or one-shot condition.

The two metrics, two breadths, 90 calendar days, dev/holdout windows, Monday
schedule, 500 draws per placebo family, worse-p rule, own-grid DSR n=4, fees,
full-capital charge and financial gates are preserved. No added market-cap
source, alternate window, surviving-metric subset or claim of untouched H1 is
admitted here. P0 stability alone cannot establish historical point-in-time safety.

## Decisions adopted now

| Original clause / ambiguity | Explicit conservative preparation convention |
| --- | --- |
| P0 second snapshot at least fourteen days after first | B1 is the authenticated all-member VPS verification at `2026-09-15T09:10:51.915449Z`. Every second-vintage request must start at or after `2026-09-29T09:10:51.915449Z`, with retained actual request/receipt intervals. B1 is an exact-byte existence bound, not September 4 retrieval or provider publication. Later evidence cannot silently advance this clock. |
| P0 common protocol-days, metric ambiguity | Unique protocol/metric/supported economic daily interval is the comparison key. Fees and revenue are compared separately and both must pass. No aggregation before P0 that can cancel constituent revisions. |
| More than 10% change, at most 5% changed | Exact arithmetic; positive baseline a is changed iff abs(b-a)>0.10*a. Changed/common must be <=0.05. Explicit 0→0 is unchanged; 0→positive and positive→0 are changed; zero-baseline percentage remains undefined. Zero common denominator is unavailable. |
| Missing/deleted/invalid observations | Retain requested, received, invalid, absent, first-only, second-only, common, comparable, changed, unchanged and recent-excluded identities. Negative/nonfinite/conflicting/malformed values are unavailable. An unresolved eligible first-only deletion or missing required member blocks a full-family pass. Never reduce to successful rows or substitute zero. |
| Days ending at least thirty days before first snapshot | Keep September 4 00:00 UTC as an exclusion anchor only. After interval semantics are supported, include intervals ending at or before August 5 00:00 UTC. Do not infer start labels or units. This anchor is not a claim about when exact first-vintage bytes were acquired. |
| Several protocols per token, trailing 90-day denominator | Freeze separately admitted required constituents from vintage one per token/metric. Require every constituent on every one of 90 calendar days. Missing pre-inception data stay unknown absent supported zero-activity evidence. Require finite nonnegative quantities, strictly positive summed denominator and supported positive finite market cap. Mapping and overlapping economic contribution still need admission. |
| P1 median weekly signal-valid names >=20 | Require both metric-specific medians >=20 across every scheduled dev Monday, including zero-valid weeks. Keep all four cells. This population is value-valid; missing control coverage cannot silently shrink it. |
| Ordered P0→P1→P2→grid | Each stage requires a durable exact source/vintage/policy/code/calendar binding, immutable predecessor digest, exclusive intent and complete/failed/unavailable terminal. A failed/unavailable prerequisite blocks successors. Pure helper declarations do not supply this proof. |
| P2 two-day lag with logged pre-grid widening | Once the exact delay claim and supporting clocks are separately admitted, common whole-day lag is max(2, ceiling of greatest admitted required-series delay in days). Missing/unbounded delay stays unknown. Apply the common lag to value and controls and revalidate P1. No breadth/return optimization of lag. Latest-point age is not historical publication delay. |
| C1/C2 through identical pipeline | For each metric/date freeze the value-valid cohort, then require full C1/C2 coverage on that cohort. Missing/late control input makes the comparison unavailable; no favorable intersection. Match breadth, cohort, decision/action clocks, feature lag, execution/funding inputs, fees, capital charge and endpoints. Fees and revenue cohorts may differ; reuse requires identical full contracts. |
| B count-matched rank reassignment | Permute only within the frozen eligible set. Preserve dates, leg counts and all 500 intended identities; retain unavailable draws without redraw or successful-only denominator. A's separate circular-shift definition is not replaced by B. |

The retained first-vintage manifest SHA-256 is
`89a9eb8ccbe073c1530047faa3bd64ca22085a79f933d782dcd2098a870d5151`;
archive SHA-256 is
`8dc9129026813137a7f8f3f1527ebeb94ef1f97f3d12854b17c8f5dfa80dd9a1`.
The [VPS proof](reviews/value-rev-verified-vps-backup-20260915.json) binds all
704 raw members plus manifest. The witness is an authenticated operational
record, not third-party cryptographic timestamp attestation.

## Explicit remaining gates

Provider chart units, economic interval boundaries, provisional/final meaning,
valuation currency, revisions, mapping/parent-child overlap, contemporaneous
market-cap/price availability and event-complete funding remain unsupported.
A chosen convention cannot establish those facts. Historical first-publication
availability remains separate from two-vintage revision stability and current
latest-point age. Actual P2 must explicitly select its source vintage, clock and
claim; this document does not substitute B1 for a historical capture time or
substitute vintage two for the original factor inputs.

C2 score/return semantics and A/rank/endpoint conventions are undergoing narrow
independent preparation reviews. Their proposals are not adopted by this table.
No empirical runner or speculative platform is being built while admission
facts remain unresolved. A future opaque raw capture would still need its own
committed fixed request recipe, exclusive destination, one-attempt/resource
limits, complete unavailable denominator and review after the frozen time bound.

The next source step requires new authoritative interval/availability evidence.
The two retained serializer 404s are not grounds for another identical request.
The reviewed pure readiness/control helpers are useful preparation artifacts;
their synthetic passes do not repair or authorize the legacy empirical mains.

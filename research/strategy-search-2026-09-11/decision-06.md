# Decision 06 — second dated harness failure and deferral

The registered resource correction ran once from committed, pushed and remotely
verified source 6ee6317f8cc04f94fa48932dccbe01572a05fd8c. It failed before the
first read_input returned, while the lifecycle checked source through a short
Git subprocess. The monitor reported a live process without a VmRSS field and
terminated the financial child. Elapsed time was 4.68 seconds; sampled aggregate
RSS peaked at 215,728,128 bytes, below the 512 MiB limit. This is an observed
monitor failure, not memory exhaustion or economic rejection.

The failed claim retains eight intended cases, zero completed verdicts and no
outputs. The monitor sidecar and independent second-failure review remain.
ResearchRun.start already hashed raw bytes; the subsequent financial evaluator
was not reached. No freshness claim follows. Both failed identities are immutable.

The suspected process-exit race is supported by the source and a subsequent
synthetic lifecycle test, which additionally exposed ESRCH on a departed process.
The earlier preflight had real statistical calculations but omitted ResearchRun's
short Git helpers. That verification gap is explicitly acknowledged. The new
unfrozen v2 monitor retries a missing memory field once, accepts confirmed process
departure and still rejects persistent live unknown memory. Deterministic tests
and a disposable lifecycle plus 100 short helpers now pass. These are engineering
checks and cannot supply the missing dated financial result.

## Disposition and next action

Historical one + archive one + failed book one + failed correction one exhaust
this family's administrative cap of four. The dated episode is deferred without
an economic verdict. The current admission API rejects changed family budgets;
an additional attempt needs an explicit reviewed amendment mechanism and an
information-value decision. A renamed gate or family cannot evade this boundary.
An eventual unchanged-data attempt may be informative after that route exists,
but no extra dated execution is currently admitted.

Continue independent useful research: finish the options metadata collector's
pre-review and exact resource/output-bound preflight, freeze its own four-request
source registration, then capture and independently review actual instrument rules
and archive catalogue types. Next is the fixed same-venue two-cycle triangle
necessary-feasibility question. Interim coverage review explicitly finds affordable
gaps across several other mechanisms; this deferral is not research exhaustion.
Program active and incomplete, with zero validated strategies.

# Options capture and VPS engineering checkpoint

September 11, 2026. **Incomplete setup; no collector deployed or running.**
This is ordinary engineering, not a twentieth investigation, budget extension,
paper start or profitable-strategy finding. The nineteen empirical identities
remain terminal: sixteen complete and three failed. Zero strategies are validated.

The latest user instruction is to finish the current options/VPS work, then pause
for a while. No other research family or automatic wakeup is part of the remaining
scope. A preference question is pending: whether an eventual hourly collector
should continue during that pause or all research processes should remain stopped.
No new unattended collection should start while that question is unresolved.
This checkpoint does not label the setup finished or the research space exhausted.

## Components prepared

- The previously backed-up options ledger/batch handles eight fixed capital/cost
  alternatives on invented hourly records. Its independent full-path accounting
  proof, two retained arithmetic failures and limits remain in
  `engineering-options-policy.md`; none establishes an economic edge.
- `options_policy_selection.py` selects shared expiry/strike identities before
  checking the selected pair's rules. Minimum-anchored lot grids are intersected
  exactly, with no easier-pair fallback. Eleven tests and an independent 2,592-case
  reconstruction pass. Raw metadata/index availability and full execution filters
  still require the outer source adapter.
- `research_options_capture/journal.py` retains immutable intents, bounded prefixes,
  receipts, missed/interrupted states, recovery and a source seal. Eighteen tests
  and independent adversarial review pass. The seal is not an outer terminal.
  Lock/source checks and retained byte/member caps remain explicit.
- `research_options_capture/transport.py` supplies public, allowlisted HTTPS
  attempts through short-lived children. It preserves published raw prefixes before
  interpreting later frames, bounds framing/body sizes, rejects truncated HTTP
  bodies and malformed protocol, verifies worker/controller clocks, signals access
  restrictions, and refuses sealed, terminal or unlocked journals before spawning.
  It supplies no assignment, source calendar, strategy or network authorization.
- `research_options_capture/control.py` is a local controller prototype for one
  explicitly granted future episode. It preserves the original options family
  object and nineteen-history inventory, distinguishes source seal from terminal
  completion, permits one analysis intent only after the frozen observation end,
  and retains nonterminal stop-pending state until a quiescence condition exists.
  Its internal verifier is a self-check. Source/stop declarations need independent
  evidence of actual worker quiescence; the prototype is not a full independent
  admission or rollout implementation.

## Measured scope and retained failures

The initial journal construction attempt failed its 120-second guard after only
part of the full synthetic inventory. Its report is retained. A separately scoped
near-complete fixture then tested actual entry, late writes, validation and sealing
at 17,000 slots/34,003 members/200,773,105 bytes: 37.646 seconds and 157.4 MB sampled
aggregate RSS. The direct fixture setup was not a production-fsync construction
proof. Full validation must precede the five-second acquisition window.

The actual nineteen-history structural/hash check passed: 7.977 seconds,
51,208,192 sampled aggregate RSS bytes, sixteen complete and three failed receipts.
It reads retained outputs only for hashes, not financial-value recomputation. The
guard/result files and checker preserve the complete historical denominator. This
is not a frozen-source end-to-end admission rehearsal.

Synthetic transport/resource proof used fresh small journals and invented child
frames, without network:

| Group | Bodies | Acquisition | Retained journal bytes |
|---|---:|---:|---:|
| Routine | 16 × 8192 bytes | 0.7555 s | 330,982 |
| Maximum initialization fixture | 3 × 5 MiB | 1.4391 s | 36,705,306 |

The combined guard took 2.8494 seconds and measured 305,315,840 bytes aggregate
RSS. Fake and production children both have Linux parent-death protection; the
guard's generic no-detached-child qualification is retained, not rewritten as a
complete production cleanup proof. The resource fixture predates the final active
journal precondition, which received targeted rejection tests. It does not prove
real network timing, a near-complete journal plus transport, metadata projection,
full calendar retention, history plus capture or VPS behavior.

The first transport test candidate had three failures: the worker wall-clock jump
was not independently checked, and two timed cases missed deadlines during an
unexplained long parent delay. Receipt timestamps preserve approximately 106 seconds
between child and controller in that first sixteen-body case. The clock defect was
fixed; a subsequent run passed without relaxing deadlines. Timing variability also
affected controller-suite duration. These are uncommitted engineering iterations,
not empirical retries or evidence of a guaranteed five-second service envelope.
Later review also corrected incomplete HTTP handling, durable-prefix publication,
post-completion protocol rejection, close-error tail retention, active journal
checks and the premature-analysis control defect before deployment.

Published prefixes are durable; bytes still in a socket, child, pipe or unpublished
controller operation can be lost on abrupt death. Their existing intent becomes
unavailable and is not retried. Prefixes coexist with encoded final receipts, so
the earlier proposed 1 GiB whole-episode bound cannot be reused without accounting
for this duplication and the daily metadata/funding schedule.

## VPS facts and remaining work

The user confirms pck-preds-1 /46.225.169.184. Read-only SSH confirmed root access,
Python3.12.3, tmux/screen, two CPUs and39,759,348KiB available. Python3.13 was not
on PATH. No credentials were inspected and no remote file, package, service or
research process was changed. An isolated pinned3.13.13 runtime remains to be
prepared and verified; the shared application environment must not be upgraded.

The deployable collector still needs the exact assignment and calendar, initial
selection/source-clock integration, daily rule/funding coverage, returned raw-member
verification, independently reviewed source admission, complete output/retention
bounds and a representative combined resource proof. Then freeze the absolute
future window, full charter/policy and explicit effective-five book grant, commit
and verify backup, create the sole authoritative local claim, and prepare the
externally anchored worker package/manual launch. No grant, claim, assignment,
package installation or capture date exists at this checkpoint.

Manual rollout must use a dedicated source/data/lock location. It must demonstrate
stop behavior, lease expiry, restart without retries, absence of an existing worker,
and verified raw return into a new local staging path. Historical monitor/funding
launch commands are not an options deployment. No systemd edit over SSH, production
mutation, real order, paid source, provider contact or implicit scheduler follows
from this preparation. Preserve all frozen runtimes, raw stores and prior attempts.

Read `STATE.md` for final verification and remote-backup identifiers. Resume only
within the user's bounded options/VPS scope and apply the pending pause preference;
after the setup boundary, wait for explicit user resumption of research.

## Final engineering verification

The pinned runtime/lock check passed. The named offline target passed1,426tests
and20subtests in624.14seconds. It started before the last controller output-member
guard patch; that final patch separately passed ten unsafe-output regressions and
three normal-completion/observation-boundary checks. This distinction is retained
instead of claiming a full suite ran from one immutable source revision. The
verification JSON binds final component/test hashes and the saved offline log.
Both independent controller/transport reviews accept components only, with the
remaining rollout requirements explicit. The four historical runtimes and actual
research-run files have no diff against the preceding backup.

# Decision 08 — one options archive schema admitted

The fixed earliest-listed BTC EOHSummary ZIP and checksum completed once as
options-eoh-schema-20260911 from source
c6f23fc7657390e6131539e0ac2eff23a9cd3598, committed, pushed and remotely verified
before acquisition. Both declared cells are complete, zero unavailable. Four
immutable outputs total 1,083,353 bytes and preserve 402,433 raw bytes. The request
span is September 11, 2026 09:05:38.686084–09:05:41.287659 UTC. The input definition's
earlier observation interval does not backdate these captures or publication.
The sampled resource report records 4.826 seconds and 66,281,472 bytes peak tree
RSS, within the frozen bounds. Receipt verification passes; independent raw
reconstruction is recorded in reviews/options-eoh-review.md and its JSON report.

The paired SHA256 check passes. The single UTF-8 CSV contains 5,735 records,
including its first candidate-header record, all 26 fields wide. There are no
blank or ragged records. After the first record, all 5,734 entries in column 16
are empty strings; its literal candidate label is best_buy_iv. Other columns
have no empty strings or absent fields. These are lexical observations only;
nonempty cells are not automatically valid numerical data.

Candidate names include date, hour, symbol, underlying, strike, best_bid_price,
best_ask_price, best_bid_qty, best_ask_qty and option risk/volume names. Their
units, clock conventions, quote age, side semantics and executability remain
unverified. No numerical cells, spread, IV, premium, cash return, exposure or
strategy profit were interpreted in this probe. The 2023 object remains exposed
historical discovery, not fresh validation. The old RVIV failure is unchanged.

## Learning and next action

This object provides a concrete structural starting point for a historical-chain
admission question; the complete absence of one candidate IV column is retained.
It does not yet support an option book. The remaining third new options question
should target historical contract/clock/field semantics and necessary quote-size
quality under a fixed design only if authoritative semantics can be established.
Do not spend it on an automatic broad archive download or return calculation.
Options has used two of three new investigations, not two independent trials.

Proceed to the already frozen six-request spot-triangle source capture. Its
follow-up arithmetic has a separate financial charter and must be registered,
reviewed, committed and remotely backed up before real-input evaluation. Continue
public venue-comparability research in parallel. Funding-carry and dated budgets
remain unchanged. The program is active/incomplete; zero validated strategies.

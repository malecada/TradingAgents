# Decision 15 — saved news sources not admitted

The exact news-provenance-20260911 source inventory completed once from
7e5e8b1affb1feb4e729290b943c313a56493a46 after commit/push/remote equality.
All seven source cells and thirteen subordinate schema slots are unavailable;
nine immutable sanitized outputs total19,350bytes. The structural verifier
passes. Independent bounded source/privacy reconstruction also passes; the
checker and reports are retained in reviews/news-provenance-review.{json,md}. The guard records
7.846seconds and46,518,272bytes sampled aggregate RSS, no limit reason.

All six literal Alpaca/GDELT monthly roots are missing at their recorded paths
across the consolidated, original and predlab worktrees. This confirms the
already exposed directory-discovery finding, not absence from every possible
location or destruction of the historical stores. No alternative file was
selected. All twelve unfilled footer slots remain explicit.

The nested external full_articles.csv exists with576,048bytes. Only its first
physical13-byte line was inspected; its digest is
4f9151e43bb1edad6ff8a4115df39b31136a347d834be98dbdbf280f80dd2d99.
Independent decoding of only that region found one CSV field, zero recognized
whitelist fields and zero plausible identifier/time fields. The frozen schema
rule therefore leaves header semantics unavailable. Before/after descriptor/path identity,
size, modification and change metadata agree; this is best-effort consistency,
not a guaranteed atomic snapshot. No bytes after the first physical line were
read; its unrecognized text was not published; no whole-file hash was taken.
The first line itself has unverified semantics. The filename does not establish
identity with the previously described crypto news stores.

## What this resolves

The currently located sources do not supply an admitted observation/retrieval/
version-aware input. Existing narrative claims and modern collector code cannot
retroactively authenticate the historical bytes. A source column or publisher
date would not by itself establish first availability or version preservation.
The result is source unavailability, not a failed financial signal, a new
negative news backtest, or evidence against every attention mechanism.

Do not relabel this CSV as the old corpus, add a presumed lag, read article text
to invent timestamps, re-extract C1/C3 labels, or widen paths until a convenient
sample appears. A versioned metadata manifest identifying the actual stored
sources, their original observation clocks and exact source lineage is the
named reopening dependency. A newly identified exact path can support a future
reviewed information-value proposal; it is not an automatic fourth row8 claim.
No corpus backup was established by these sanitized research outputs.

## Continuation

This consumes the single news-inventory claim and the third initial MAProw8
question after the two WBETH investigations. C1/C3/NLST/SMW/DEX and unknown
broader multiplicity remain inherited; no financial history or family budget
was reset. Independent reconstruction and findings are complete; verify branch
backup before the next empirical question.

Proceed with the distinct final triangle source/persistence investigation under
synthetic preparation. It can answer whether cost- and size-qualified local
quote states recur in one fixed interval, which the one-off snapshot cannot.
Freeze transport, byte limits, all5,400gridpoints and8case series before any
stream. All other map dependencies remain explicit, including value_rev's
September18-or-later second vintage and unresolved options/venue source semantics.
No stopping criterion has been established while this affordable question
remains. Zero strategies validated; research remains active and incomplete.

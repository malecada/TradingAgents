# Dedicated options research launch and return

September15,2026: source/grant/claim and exact VPS release are admitted and
verified. The approved v2 launcher started08:50:15UTC and is waiting until08:58.
No market collection was present at08:50:24. The durable launch evidence is
reviews/options-actual-launch-20260915.json; later operational records supersede
this snapshot. This procedure is unchanged; do not rerun the initial launcher.

Confirmed host pck-preds-1 /46.225.169.184. Dedicated root:
`/opt/thesis-research/options-episode-20260911`. Its isolated runtime, exact source package, preserved synthetic return proof and
finite launcher are installed. The shared Python, systemd, cron, production code and journals
were not changed. Runtime evidence is in `reviews/options-vps-runtime-20260915.json`.

Pinned executable:
`/opt/thesis-research/options-episode-20260911/runtime/cpython-3.13.13-linux-x86_64-gnu/bin/python3.13`.
The copied archive and complete5367member tree match the local pinned runtime.
Use `-I -B`; no package installation or shared environment is needed.

## Before launch

1. Complete independent source/financial/resource review and named offline checks.
   Freeze exact future UTC hour, observation/lease boundaries, eight cases, code
   and policy. Commit target-specific effective5 book grant, with nineteen prior
   identities and the unchanged options family. Push and independently verify
   remote commit equality before claim/source access.
2. Perform authoritative local admission and exclusive claim creation. Independently
   verify the active prospective claim; no VPS run root can replace this full
   local history check. Preserve the admission and source hashes.
3. Build one release with `scripts/options_release_package.py`, binding the remote
   data path `/opt/thesis-research/options-episode-20260911/data`, host identity,
   committed source and exact copied claim. Release files include only eight
   pinned source modules plus assignment/claim; a standalone root bootstrap is
   among the pinned sources. The package builder refuses an existing destination.
4. Transfer to a new versioned release directory, verify its complete inventory
   and external assignment/bootstrap hashes. Retain local and remote package
   bytes. Require at least8GiB free,4KiB or smaller allocation units and1,200,258
   free inodes; the logical source cap remains2GiB. These are separate dimensions.
5. Check the externally supplied bootstrap hash with a shell utility before
   executing Python. An adjacent self-hashed manifest alone is insufficient.
   Invoke the verified bootstrap with the independently supplied assignment hash,
   exact package path and exact data root using isolated Python3.13.13. The worker
   independently checks host, path, copied claim, source and fixed lease. Every
   child re-enters through the verified isolated bootstrap. No extra import file
   or replacement symbol is permitted.

A named tmux session may contain this single finite foreground command; no new
systemd unit, cron entry, recurring Codex task or production launcher is used.
If the command is scheduled before its lease, one explicitly recorded finite
wait may precede it, followed by the same frozen execution. Waiting does not
change the absolute lease or permit an early market request. Record exact
session/PIDs, package/assignment hashes and first process/source status after
launch. Do not label a waiting command as observed market collection.

## Stop, interruption and observation policy

No interim financial evaluation or significance look is authorized. Read-only
operational status may inspect process identity, locks, timestamps, request counts,
retention or error logs. It must not become a Greek/cash/performance look. The
worker makes no orders and no account calls.

An explicit STOP file in the fixed data directory requests source termination
at the next guarded boundary. Retain it; do not delete it to restart. The
supervisor's bounded no-progress/clock/RSS/absolute-lease guards independently
terminate children. Access-denial statuses stop later groups. Partial raw prefixes
and all intents remain evidence. Controlled restart never retries attempted or
uncertain slots, does not change data root, and cannot reselect entry. At most64
manual invocations; no automatic restart.

Before outer closure or analysis, obtain an authenticated stopped-process proof,
acknowledged source seal plus process exit, or independently established lease
expiry. A source seal or controller boolean alone is not proof of quiescence.
Unreachable host means stop-pending until the frozen condition is established.
A crash during partial journal sealing refuses automatic recovery and requires
forensic review; no new empirical identity silently replaces it.

## Return and later analysis

Preserve the original remote data tree. Copy every registered member into a new
local staging path without following symlinks or overwriting existing evidence.
Do not extract an unchecked archive into the workspace. Verify paths, regular
file types, counts, byte/production-prefix bounds and every content hash before
admission. `returned.prepare` reconstructs the selected calendar, journal/global
seals, raw initial selection, source clocks, benchmark paths, per-asset rule
vintages and funding inputs. It computes no financial results and explicitly
leaves quiescence to the external review.

Bind only the frozen normalized inputs and source inventory after independent
source/quiescence review. Wait until the frozen observation window has closed,
even if the worker stops earlier. A failed early source episode may failure-close
once quiescence is proved; it does not authorize an economic replacement run.
Record exclusive local analysis intent, evaluate exactly eight frozen books once,
publish bounded outputs and complete/unavailable cells, then independently verify
the terminal. A crash after intent retains partial outputs and forbids replay.

After diagnosis, retain decisions/state, commit/push verified branch backup and
record which full raw copies are actually recoverable. A raw manifest or configured
remote alone is not a complete raw backup. The user resumed broader research on
September15; continuing another justified question still requires its own frozen
gate and remaining cumulative budget.

## Actual launch identity and status route

Source:ebc21e931541e7e03e139e48f7d2069a8426742f.
Claim:9755f008355cbffdef9c0b561e3806ed39a1c2d874fb7a0f285def2ebab9dd78.
Assignment:8349daecd93b39ed2e41e48194b58790706edaf209cf2a3b24ba62b0b430eccf.
Release:`/opt/thesis-research/options-episode-20260911/release-ebc21e9`.
Approved launcher:`launch-options-20260915-v2.sh`, hash
1b6350518d078806b946f0cb3371f26560b75239953d0d5355aae0b0f4dab71d.
Original v1 remains evidence and is not approved for execution.

Read-only SSH on confirmed host can use `tmux -L thesis-options-20260915 list-panes
-t options-episode-20260911 -F '#{pane_pid} #{pane_current_command} #{pane_dead}'`
and process/log/receipt status counts within the dedicated root. Read only
operational fields; never derive Greeks, returns or financial significance during
capture. The one finite command does not create a Codex follow-up or automatic
restart. Loss of tmux/session is an error/exit requiring retained evidence review.
Use fresh preserved logs for any separately reviewed manual resume; never overwrite
or retry the initial launch. No data root relocation or entry reselection.

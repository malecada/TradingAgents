# Timing-corrected options episode — engineering policy September15,2026

Pre-observation engineering specification. No actual grant, claim or capture is
created by this document. It defines an isolated successor of the failed fifth options episode; the
original policy, source package and all20terminal predecessors stay immutable. The exact
absolute future entry, executable source hashes and +1 certificate remain to
be bound in a committed registration after independent resource admission.

## Scientific question and cumulative scope

Can the minimum unit-one BTC or ETH short-straddle with fixed hourly perpetual
hedging produce a complete conditional cash/cost/exposure path at 1,000 and
10,000 USDT? This addresses path feasibility left unknown by buyer-entry checks.
It does not test a forecast of positive expected returns. Eight correlated books
are BTC/ETH × 1,000/10,000 × base/stress. The same minimum option quantity applies
to both capital levels. No parameter search, replacement episode, interim PnL
look, seventh options allowance, paper account or real order is authorized.

Original options family budget4/prior1 and the four consumed program claims
remain unchanged. The prior effective5 allowance includes the failed
options-episode-20260911 source episode with eight unavailable cases and no
financial outputs. One separately reviewed target-specific effective6 grant is
required for options-timing-20260915. All20terminal predecessors and five frozen
runtime packages, including research_options_capture, remain immutable. No grant
is created by this engineering policy; exact future clocks and source/review/
resource hashes must be bound before any empirical source access.

## Exact source recipe

T is a future UTC hour. Bootstrap at T−60seconds comprises eapi exchangeInfo,
fapi exchangeInfo and fapi fundingInfo. Hourly h=0..1056 at T+h hours has eight
known roles (both venue clocks, BTC/ETH index, perpetual depth and premiumIndex)
and eight selected option depth/mark roles. The frozen selector chooses nearest
30day expiry within7–45days, earlier tie, nearest shared strike, lower tie, then
lexical identity. Exact unit1/USDT/lot-grid identity precedes availability. No
premium, spread, delta, displayed size or economic score chooses the contract.

For each nominal hour N=T+h hours, every known and selected journal slot has
earliest acquisition scheduled_ms=N+2000 and absolute deadline_ms=N+5000.
Nominal observation time remains N, recovered from the fixed entry and validated
hourly role identity. Economic action, freshness and funding ownership remain
N+5000. Acquisition release is distinct from actual receipt availability.
Known acquisition starts no earlier than N+2000. Selected acquisition starts
immediately after known acquisition and, at entry, deterministic selection and
selected-journal initialization. Those operations share the remaining three
seconds; there is no second delay, N+7000 deadline, retry or replacement quote.
This exact phase applies to every hourly group, both benchmarks through the
later selected exit, and inclusive own-exit observations. Both
asset selections must succeed for collection to continue; otherwise preserve
all17,144 conceptual slots, available selection facts and eight unavailable
financial cases. This all-or-none entry is an operational prerequisite, not
an economic rejection of the other asset. The selected journal initialization
cost is part of the entry deadline, not excluded overhead.

Each book exits at selected expiry minus24hours. Selected option/perpetual
asset roles after their own exit remain explicitly unused. Both market-index
roles continue through the later selected exit for both beta benchmarks.
Daily d=0..44 at T+d days+60seconds captures the three metadata roles and both
funding histories over the fixed preceding48hours, limit1000. All daily roles
remain in the source calendar. At T+45days each asset has two fixed funding
partitions [T,T+22days−1ms] and [T+22days,T+44days+5000ms], limit1000. This final
request begins24hours minus5seconds after the maximum modeled exit action;
it is a research deadline, not a publication SLA. No extra page or retry.

Five journals contain3/8456/8456/225/4 slots, total17,144. Each request has a
maximum5second controller window for bootstrap/daily/final and a shared3second
hourly window for known/selection/selected. Routine bodies cap8192bytes; each exchangeInfo
5MiB; fundingInfo64KiB; funding history512KiB. Every attempted slot has a durable
intent before networking, exact raw/prefix hashes and own/controller clocks.
403/418/429/451 halts later groups. Redirects, proxies, authentication, account
requests and transport retries are excluded. Lost unpublished socket/RAM bytes
remain unknown; a retained intent is never permission to replay the request.

## Accounting, costs and timing

The existing independently tested engine retains exact signed quantities and
cashflows; logs are a convention diagnostic only. It allocates40% option wallet,
50% perpetual wallet and10% idle. Two short option legs use the fixed minimum
common lot, with unit1. Target hedge is q×(call model delta+put model delta),
nearest admitted perpetual lot with exact half ties toward zero. The action is
nominal hour+5000ms; unknown decisions create no substitute action. Exit failure
leaves cash attribution unavailable. Last-observed rules age at most24hours plus
5minutes; any observed selected identity or normalized rule change latches
future actions/valuation unavailable, even if a later snapshot reverts. Missing
captures can recover only through a later unchanged valid vintage.
An unparseable JSON body or HTTP/clock-unadmitted response is a missing refresh,
not evidence of an actual rule change. Decoded metadata missing or contradicting
the selected identity/rules latches the affected asset. One asset's rule defect
does not invalidate the other asset's otherwise admitted daily rules.

Base option fee rate0.00024, perpetual fee0.0005, effective perpetual slippage
0.0002, no option tick worsening. Stress option fee0.00030, perpetual fee0.001,
slippage0.0004, one selected option tick worsening at entry/exit. Option fees are
min(rate×index,premium/10)×quantity per leg. These are frozen account-independent
assumptions, not verified personal fees. Effective perpetual slippage changes
cash cost; its resulting price is not asserted to be a tick-valid submitted
order. Literal quotes obey the conservative admitted price grids. Sufficient
perpetual lot/max-quantity bounds are checked; full account/order constraints,
actual fills, intrahour Greeks and margin remain unverified.

Funding uses finalized Regular rate/mark observations. Missing rateType is only
conditionally admitted with captured COIN/USDT perpetual identity and remains
labelled unspecified_crypto. Special/unknown events prevent complete cash
attribution. Each held hour requires an admitted prospective nextFundingTime;
no implicit8hour calendar or omitted fundingInfo default. Fixed histories retain
unannounced events and conflicting duplicates. A1000row response is unexhausted.
Required missing history/calendar, conflicting records or uncertain ownership
makes engine_inputs unavailable, never empty-and-complete by substitution.

Funding event local-time bounds use contemporaneous venue calibrations within
one hour expanded by a frozen1second clock-drift allowance. Compatible intervals
use their conservative hull; contradictions are unavailable. Bounds crossing an
entry/hourly/exit action leave hedge ownership unknown. A deterministic mapped
timestamp is only an ownership-equivalent representative, with original venue
time and uncertainty retained. This bounded-drift convention is conditional;
calibration does not prove unobserved clock stability. Funding is charged to the
position actually held before the action, including zero inventory before entry.

## Outputs, uncertainty and decision rules

Eight fixed cases retain complete/unavailable status, reason, explicit cash
components, full-capital return,0/3/5% benchmark, wallets, turnover, hourly NAV,
missed decisions, drawdown and the arithmetic/log convention diagnostic.
Unknown funding coverage produces no zero-filled cash book. Fresh complete
market benchmarks and positive consecutive NAVs support two marginal hourly
OLS betas. At least72 consecutive returns are required; Bartlett Newey-West
lag24, finite-sample n/(n−2) correction and normal multiplier2.2414027276 yield
nominal simultaneous95% intervals across BTC/ETH. These descriptive asymptotics
do not supply independent-episode or expected-profit confidence. Eight cases
and prior family searches are correlated; no significance-driven selection.

Program provisional screens remain: positive cash and annualized simple net
return≥3% of all committed capital; beta estimates≤0.10 in absolute value with
both intervals inside[−0.20,0.20]; residual delta≤1% NAV; drawdown≤10%; no admitted
cash/margin deficit in down50%/up100% and basis/liquidity stress. Annualization
is a scale comparison, not a forecast. Model residual/gross exposures use NAV
and separately labelled initial capital; unknown admitted inputs make maxima
unknown. Model delta does not establish realized true delta. No complete option
repricing/liquidation/margin stress model is admitted, so those stress and
tradability screens remain unavailable and block advancement. Expected-return
confidence/power and tail probabilities remain unavailable from one episode.
A profitable development episode cannot validate a strategy or launch paper use.

## Timing correction rationale and negative cases

The failed parent supplied no selection or financial result. Its index events
were approximately one second before nominal entry; measured options-clock
uncertainty extended their local earliest times by153ms. At the fixed N+5 action,
worst ages exceeded five seconds. The single N+2 acquisition release addresses
that observed acquisition/action mismatch while preserving the original age bound
and uncertainty. This is a fixed engineering hypothesis, not an update-frequency
SLA, observed profitability, successful phase search or guarantee of timely quotes.

Before a grant, synthetic proof must retain realistic one-second cached indices,
clock uncertainty, adverse unchanged/stale updates, known/selection costs leaving
insufficient selected time, late replies, missed phases, source drift and prefix
interruptions. Funding announcements must be proved future at actual availability;
a due event during acquisition and any interval crossing the unchanged action
remain unknown or assigned only by the existing held-inventory convention.
No narrowing of measured clock bounds or source age is allowed to repair a case.

## Operations and resources awaiting final proof

The local checkout owns the authoritative claim, full history, grant, source
admission, exclusive later analysis intent and terminal. VPS pck-preds-1 is a
subordinate raw-only worker in one new dedicated path, anchored to exact host,
canonical data root `/opt/thesis-research/options-timing-20260915/data`, copied
claim and package hashes. The target is options-timing-20260915 and the isolated
source namespace is research_options_timing. Schema3 binds the single hourly
acquisition delay2000ms. Parent target/root/package are never reused. Verified Python3.13.13
uses an isolated distribution, never the shared Python3.12 runtime. An external
shell hash check precedes the isolated bootstrap; the bootstrap verifies the
exact source inventory before importing any package, including on child launch.
A persistent supervisor lock and worker/journal locks prohibit duplicate writers.

Proposed logical journal caps32/224/224/1472/16MiB, with terminal reserves
1/16/16/4/1MiB, leave64MiB auxiliary allowance and16MiB headroom under2GiB.
Physical allocation and inode requirements are separately admitted because
small files consume allocation blocks. Initial admission requires8GiB free,
allocation units at most4096bytes and at least1,200,258 free inodes. The conservative
regular-file bound excludes filesystem metadata and unrelated writers; actual
write failures still terminate with retained evidence. The representative resource proof must
cover legal prefix fragmentation, atomic staging, seals and initialization.
Sampled aggregate RSS512MiB, at most2CPUs,120second no-progress watchdog,
rolling100ms wall/monotonic discontinuity check and fixed absolute/monotonic
lease end bound the worker. A sampled RSS bound is not a continuous peak proof.
Lease T−120seconds to T+45days+60seconds; idle waits do not extend it. Up to64
manual invocations, no automatic restart; numbered bounded exit records preserve
errors and kill reasons. A hard crash can leave intents and no exit report.
Partially sealed journals refuse automatic recovery and require forensic review.

Returned data use a new staging path, strict nofollow inventory/hash admission,
complete raw members and independent source review. Worker seal alone does not
prove quiescence: authenticated process-exit/stop evidence or verified lease
expiry must be independently retained before local terminal/analysis. No original
remote or local evidence is overwritten. Analysis outputs total≤64MiB, each≤16MiB,
control≤4MiB, terminal reserve64KiB and staging reserve16MiB. A failure after the
exclusive financial intent is preserved and never silently recomputed.

No systemd/cron change, production mutation, account secret, recurring Codex
wakeup, paid resource or provider contact follows from this protocol. Manual
finite tmux launch is subject to the committed source/grant and reviewed release.

## Historical and allowance admission

The prior history is exactly20terminal identities(16complete/4failed). Ordinary
legacy receipt validation does not fully check the twentieth options control,
quiescence or unavailable-cell fields. A committed closed-parent certificate and
independent historical protocol reconstruction must bind its original effective5
grant, nineteen-member historical boundary, full claim/control/output/terminal
inventory, chronology and external quiescence proof. The full actual ledger must
remain visible; no filtered filesystem, monkeypatched target or fake parent is
allowed. The new sixth allowance must explicitly recognize its one descendant.

Grant schema2 preserves original budget4/prior1 and records prior_effective_budget5,
increment1,effective_budget6,parent_experiment,consumed_episode_grant,closed_parent,
all20prior_claims and four options_prior_ids. Independent approval, complete
source/resource preflight and verified remote backup precede a new exclusive
claim. No certificate may substitute for representative20-history validation and
full retained source/output/terminal resource proof. A later failure consumes the
allowance; it never authorizes an automatic seventh attempt.

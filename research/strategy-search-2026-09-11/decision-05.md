# Decision 05 — preserved dated-book resource failure

The single registered `dated-book-20260911` attempt failed with MemoryError at
its lazy statistics import. Source d287a1420d70e298bae9b0f7e69fdc79c665720e was
committed, pushed and remotely verified before execution. Claim and failed
receipt retain all eight intended cases and zero output hashes; no case received
a completed economic verdict. The independent receipt verifier passes the failed
record, and the independent recovery review reconstructs that status.

The shell invocation imposed 512 MiB virtual address space. The old charter did
not explicitly distinguish virtual from resident memory. Statistical-library
mapping/import requirements exhausted that cap; this does not establish excessive
physical memory or a negative economic result. In-memory books were calculated
before failure, but no financial values were printed, inspected or saved. The
whole period remains exposed development. The failure command record distinguishes
the retained tool excerpt from independently checked artifacts.

A narrowly scoped registered correction is justified because it resolves a
measurement defect without changing instruments, dates, costs, quantities, gates
or financial/statistical code. It consumes the third and final new dated-family
investigation. Resource enforcement will explicitly monitor 512 MiB sampled
aggregate RSS and 120 seconds, with two CPUs/BLAS threads and no virtual-address
cap. The initial invented pipeline passed imports but used a singular benchmark; it
is retained as limited evidence. Independent review required a noncollinear
benchmark, which now exercises all eight OLS/HAC fits under the exact wrapper:
3.90 seconds, 215,289,856 bytes sampled RSS and 212,768 KiB individual-child peak.
Separate invented memory and timeout failures were terminated. The launch-error
reporting defect found in pre-review is fixed and a missing-executable failure
now preserves a structured report. No financial sources changed.

Preserve the failed attempt; independently review and commit the correction
charter/gate/source and verify remote backup before one corrected run. Then
independently reconstruct the financial result and follow its economics. Another
failure cannot trigger an unregistered retry or budget reset. Options metadata
implementation and neutral-alternative source research continue independently.
Program active and incomplete; zero validated strategies.

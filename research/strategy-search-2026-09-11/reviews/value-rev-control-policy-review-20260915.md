# Independent consistency review — value_rev control preparation policy

September 15, 2026. The [control preparation policy](../value-rev-control-policy-20260915.md) is consistent with the revised [null/endpoint review](value-rev-null-endpoint-preparation-review-20260915.md) and independent [C2 review](value-rev-c2-preparation-review-20260915.md), after the narrow orientation spelling correction recorded below. It grants no source capture, stage, financial run or random draw. This was a bounded document consistency review; no source requests, financial outcomes, engine executions or repeated broad tests were used. Only this review was written.

Initially reviewed policy SHA-256: `de15997028038ec11c88b6143e9d3a3b9e9f14f638c1267a563f454103dc7a0e`.

The C2 score/orientation pair is coherent: `-R30` with `high_is_long` buys recent losers and shorts recent winners. The common economic shortness conversion negates that score, preserving the inherited high-short ranking direction. Compounding is explicitly identified as a substantive change to the inherited sum, with finite positive-mark intervals and separate availability requirements. It does not authorize comparing alternative controls against real outcomes.

The ranking policy preserves nearest ties-to-even counts with minimum five names and short-first two-sort lexical priority. It does not quietly introduce the previously considered floor counts or reverse identifier order on the short side. All-equal and incomplete required cohorts remain unavailable. Endpoints retain inclusive daily accounting, a separate anchor, flat start, the first capital charge, immediate supported execution boundaries and costed terminal liquidation. Unsupported source clocks, held funding and prices remain blockers. The terminal boundary does not authorize holdout economic intervals or signals.

B adopts only finite-cohort permutation and retention invariants, with all 500 identities and no redraw. Its RNG, statistic and p convention remain unadmitted. A is explicitly deferred for both execution and inference; no conditional screen is mislabeled a calibrated scientific gate, and no alternative null is authorized as a fallback. These are preparation definitions, not a complete engine contract or a finding of empirical feasibility.

**Narrow interface finding:** the ranking paragraph initially names `high_is_short`, while the pure declaration helper accepts exactly `low_is_long` and `high_is_long`. The economic intent is clear, but an implementation should not have to infer an undocumented alias. Replace `high_is_short` in this policy with the helper's existing `low_is_long`, or explicitly document the equivalence without silently expanding the helper API. This finding was sent to the coordinator. No other material inconsistency was found. Once the spelling is aligned, the policy is suitable for its stated preparation-only role; no financial admission follows.

Final verification: the coordinator changed the label to exact `low_is_long` and explicitly explained the economic high-is-short synonym without expanding the helper API. The corrected paragraph was reread. **Pass for preparation-only scope; no unresolved material consistency finding.** Final policy SHA-256: `c7eb6723009b787066a095e98e0e3f55c994f21e71916cb815096650a345b80a`. No source, inference or financial admission is implied.

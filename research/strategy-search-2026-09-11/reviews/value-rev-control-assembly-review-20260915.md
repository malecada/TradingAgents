# Independent review — value_rev control declarations

September 15, 2026. **Pass within the pure, readiness-only declaration scope.** No material defect was found. No source, policy interpretation, empirical stage or financial experiment is admitted by this review.

Reviewed source: `tradingagents/research_value_controls.py`, SHA-256 `3aaf1171d94563ba2ccc75129128e8f4e4e273d3d36ad0ad4e55fedfad97d632`. Reviewed tests: `tests/research/test_value_rev_controls.py`, SHA-256 `8579940d8f10aa65060ecbd741573a44c0e4ffcb594f60fe9c15d80590d7f172`. Both hashes match the implementation report. The review used the [engine parity audit](value-rev-engine-parity-audit-20260915.md), [pre-outcome interpretation](value-rev-preoutcome-interpretation-20260915.md), [readiness review](value-rev-readiness-review-20260915.md), and [implementation description](value-rev-controls-implementation-20260915.md).

All 14 supplied tests passed independently in the checkout-local Python 3.13.13 (`python -B -m unittest discover -s tests/research -p test_value_rev_controls.py -v`, 0.015 seconds). A separate in-memory test builder, without importing the supplied test fixture or expected answers, evaluated **221 invented bundles**, all passing. This was targeted offline verification, not a replacement for the coordinator's named full offline profile. No raw market body, panel, return, funding history or financial engine was read or executed; no network request or empirical mutation occurred.

The independent matrix applied 17 faults separately to every combination of four cells and three roles: missing score, late score, NaN, infinity, null, Boolean score, changed cohort, wrong breadth, changed decision clock, changed lag, changed input hash, changed policy hash, changed action time, missing orientation, invalid source hash, extra score identity and missing role. Additional bundles exercised each partial-grid size from zero through three cells, five malformed common clocks, five malformed common execution references, an extra cell and explicit opposite orientation. Every invocation checked input preservation, exactly four output cells, exactly three retained roles per cell, the full declared required-symbol set, and false source/empirical admission flags at bundle, cell and role levels. Local role faults left unaffected cells visible and aligned; no missing or invalid score was converted to zero or removed from the required cohort.

The implementation restores only explicit declared parity. Each cell's strategy and two controls must share its exact caller-declared cohort and breadth, and every cell must share the common decision clock and execution reference. Supplied lag and action time are checked for equality and basic chronology; no two-day default or widening policy is chosen. Signal orientation is mandatory and remains descriptive. All four fixed metric/breadth identities remain present when cells or roles are unavailable. Extra identities reject the bundle rather than enlarging or replacing the registered denominator.

`declared_aligned` is deliberately narrower than pipeline parity. The helper does not choose the metric cohort, enforce a cross-breadth cohort-selection policy, prove the truth of a score-source hash, validate an execution-policy document, construct a rank or weight, infer the reversal sign, calculate returns, or create an immutable trial intent. Different explicitly declared cohorts across cells therefore do not establish that the eventual scientific cohort policy is valid. The assembly digest binds the emitted description; it is neither an authenticated source witness nor evidence that the declared underlying inputs exist.

The audit's missing-control policy, C2 return aggregation/orientation, placebo masks, common lag, funding-event accounting and scored endpoint conventions remain unresolved and unadopted. Source day semantics, vintage timing/provenance, ordered stage admission and actual funding completeness remain external prerequisites. No test above resolves those conventions or demonstrates profitability. No implementation correction is requested within this helper's scope; the next engine or source step still requires its separately reviewed explicit contract.

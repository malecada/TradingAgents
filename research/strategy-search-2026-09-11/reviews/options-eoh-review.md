# EOHSummary completed probe: independent lexical review

**PASS for byte integrity and the registered literal schema/count claims.**
There is no admission of field meanings, quote execution, historical chains or
economic viability. No network request, collector import, numeric CSV conversion
or financial calculation was performed by this review.

The independent [checker](check_options_eoh_schema.py) reconstructs retained
outputs directly from Base64 bodies using standard-library checksum, ZIP and CSV
operations; its [machine-readable evidence](options-eoh-review.json) contains all
reconstructed fields and comparisons. The official structural receipt verifier
also passes: two complete cells, zero unavailable cells, four outputs.

## Provenance and integrity

The claim and completion match committed source
`c6f23fc7657390e6131539e0ac2eff23a9cd3598` and registration SHA256
`a1f63a88374a3514728a966c45744691fadfb79e51cf972bc6c4a5beb728fe5b`.
Committed source/runtime/charter/input hashes match the saved contract. Every
ancestor object remains unchanged. Both individual receipts equal their copies
in the aggregate capture, including exact URLs, identities, headers, error state,
actual clocks and body hashes. The four output bytes match their completion
hashes and total **1,083,353 bytes**, within 64 MiB.

| Retained object | Bytes | SHA256 |
|---|---:|---|
| ZIP | 402,333 | `1e2d2ad7f767a6d554c54fc0d78b873ac0006094e9dbcd31324c9b9083e86655` |
| CHECKSUM body | 100 | `0d478ba5c7bbabfd96c5fdc7bbc4e720287a2cc9c0466a4c2351edf11961d667` |
| Sole CSV member | 1,562,027 | `66512a602b2a37ae3e2dddac43d0b46b8411f2ff4024795ce54ce4a8f5f77372` |

The checksum record matches the complete ZIP bytes and exact basename. The ZIP
contains only `BTCUSDT-EOHSummary-2023-05-18.csv`, without encryption; full reading
passes CRC and uncompressed-length checks. Its member expansion ratio is about
3.88451, within the declared 100 limit. UTF-8 decoding succeeds without a BOM.

## Independent lexical reconstruction

All reported structural fields match raw CSV reconstruction exactly:

- First record: 26 literal candidate-header tokens.
- Total records including the first: 5,735; subsequent records: 5,734.
- Every record has width 26; zero blank records and zero ragged records.
- No absent columns in subsequent records.
- Column index 16, whose literal candidate token is `best_buy_iv`, contains
  the empty string in every one of the 5,734 subsequent records.
- Every other column has zero literal empty strings. This does not establish
  valid numeric values or exclude other unknown null encodings.

The candidate first record includes tokens `best_bid_price`, `best_ask_price`,
`best_bid_qty`, `best_ask_qty`, `date` and `hour`. Their literal presence is the
registered finding; it does not validate their units, interpretation, quote age,
simultaneity, timestamps or executable size. None of their subsequent values was
converted to numeric data or used in a financial calculation. The first record
remains an unverified candidate header under the frozen contract.

The independently encoded lexical result is 3,839 bytes, within 2 MiB. There is
no hidden normalization truncation or lost row-width denominator in this object.

## Capture clocks and resource limits

The ZIP request began at 09:05:38.686084 UTC and its local receipt completed at
09:05:40.483384. The checksum request then ran from 09:05:40.859502 to
09:05:41.287659. Both lie within the recorded run interval, preserve sequential
ordering and agree with their monotonic durations within 35 microseconds.
These are local request/retrieval observations, not historic availability or
matching-engine event clocks.

The checksum HTTP Date header is 0.712341 seconds later than local retrieval;
the ZIP header is 0.483384 seconds earlier. These literal differences are retained
as cross-clock limitations, without inventing synchronization or a new rejection
criterion. The contract admits local capture ordering and does not establish
independently attested server-clock accuracy. No CSV date/hour semantics are
inferred from either HTTP header.

The separate resource record reports successful exit with no limit reason,
4.82556 seconds elapsed, 66,281,472-byte peak sampled process-tree RSS and
40,360 KiB child `ru_maxrss`. It is consistent with the declared 512 MiB/120-second
sampled policy. Its transient-overshoot and shared-memory qualifications remain.

## Interpretation and next dependency

This is the second new options investigation, following the metadata capture.
One inherited administrative RVIV bundle plus those two claims consumes three
of four family units, leaving one at this run's completion. Twelve historical
RVIV ledger rows and its original failed gate remain distinct from that budget;
unknown statistical multiplicity is not reset. The 2023 episode remains within
the conservatively exposed RVIV-era history; newly inspected columns do not create
a fresh financial sample.

The useful new fact is that the selected object is intact and has stable lexical
columns, including names that may motivate a **separate** semantic investigation.
The next unresolved dependency is authoritative meaning for this old archive's
quote/quantity/time fields, its instrument generation and lifecycle, plus an
execution-compatible hedge source. A single intact day cannot establish those
properties. If that mapping cannot be supported, defer the historical executable-
chain claim rather than invent meanings or start a financial test.

Untested: official header/time semantics, all numeric cell values and financial
outcomes, complete historical universe/expiry coverage, missing-day repair,
fees/lots/access, simultaneous fills and external recoverable backup. No remaining
defect was found in the bounded completed integrity/lexical claims.

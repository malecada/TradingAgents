# Independent public transport prototype review

Disposition: accepted as a bounded engineering component, **not admitted for rollout or an empirical collection**. This review covers `tradingagents/research_options_capture/transport.py` SHA256 `a0dc80963ede00d17e748dabdc36ed0f112cbc53188ef79119e7de550d95d79d` and tests SHA256 `a6417c4a19a8696236b8acd4fdd8b42f5405607144e09062de48c4155b50dbad`. No network, historical economic calculation, claim creation or deployment was performed by this review.

The source was inspected independently of the author's disposition. The public route and parameter allowlist excludes credentials, orders, redirects and retries (lines 25–43). The child uses verified HTTPS and a parent-death guard. The controller validates the entire scheduled group and active journal boundary before spawning (lines 111–128), persists each accepted body frame through `journal.partial` before subsequent protocol processing (lines 165–169), and attempts retention for every intended slot during cleanup. Missing, partial, late and access-restricted attempts remain explicit; access restriction is a signal which the unfinished outer scheduler must obey.

Material findings resolved during review were: volatile-only body retention; premature Content-Length EOF and missing IncompleteRead tails; unbounded frame count relative to journal prefixes; mixed-group side effects before complete precondition checking; worker-clock inconsistency; trailing or post-completion protocol material; cleanup that could skip later receipt attempts; close errors that could lose a final partial frame; and network launch after journal sealing, context exit or outer termination. Current lines 93–101 flush the tail before protected connection closure. Lines 114–115 check the active journal before spawning. A reviewer-run focused check of the final close-failure and sealed/terminal/unlocked zero-Popen regressions passed all four cases. Earlier author's full transport checks and their timing failures remain distinct evidence.

Only prefixes atomically published by the controller are guaranteed retained. Bytes still in child memory, the pipe, a controller read buffer or the read-to-publication crash interval can be lost on abrupt death; their durable intent remains uncertain and cannot be retried. The cap must count duplicated partial and final bodies and temporary publication bytes. The coalescing rule limits production body frames to 64; it does not promise lossless persistence of every network byte.

`options-transport-resource-result.json` and its guard record establish an invented-frame proof for the earlier source `fbf69e91bf83bba721650704b81994c8e4383a04d15e1d99afe4f7e346daf970`: 16 × 8,192-byte routine sources and three 5-MiB metadata sources, aggregate sampled RSS 305,315,840 bytes and 2.849 seconds under the 512-MiB/120-second guard. The active-boundary patch postdates that measurement. The proof is neither a complete-calendar nor a real-network latency or VPS runtime proof. Previously observed long host pauses are not explained by a later quick pass; hard deployment timing remains unadmitted.

Unfinished rollout prerequisites are the actual target grant and honest prospective window; resolved request assignment and deterministic selection binding; full scheduler/lease ownership and stop acknowledgement; semantic admission of the retained batches; whole-episode storage/staging limits; the exact pinned portable worker package; and integrated resource, interruption and return-to-local proofs. None follows from this component acceptance. No collector is running. The current setup must remain checkpointed as incomplete under the user's pause instruction.

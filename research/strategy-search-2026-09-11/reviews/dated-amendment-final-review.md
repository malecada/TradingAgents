# Final independent single-attempt amendment review

Decision: **approve-single-amendment**, increment exactly **1**, for
`dated-book-amended-20260911` only, parent
`dated-book-resource-correction-20260911`.

This approval binds target contract SHA-256
`1322592d2ac2beaccd7156d8a400dee286ed7a55f2f3ab4fe01cb4c33c2512f6`
(canonical target experiment excluding its certificate reference) and economic
manifest SHA-256
`2f488c32254a28bc86a13f0171293b51f401573378ff039a70a1bc8af47c8ed0`.
No different source, input, window, cell, output contract or target is approved.

## Independent evidence checks

The target hash, manifest hash, all 15 registered source hashes, 12 runtime module
hashes, four input byte hashes and all existing certificate reference hashes were
recomputed without importing the financial engine or successor admission helper.
The complete source map and runtime map equal the retained invented CLI preflight
maps. All six original runtime modules also match their bytes at the failed
parent's committed source. No original runtime file was changed.

The new gate preserves every experiment, dataset definition and family object
from `gates-wbeth-inputs.json` and adds only the named amended experiment. All
dataset definitions and family objects present in the failed parent's older
registration remain identical; other families added in intervening registered
research are retained. The original dated family remains base budget 4,
prior_attempts 1 and the same mechanism/history reference. The certificate gives
only this named target effective budget 5, leaving all original objects unchanged.

The relevant claim inventory was independently collected from every retained run
directory and exactly matches the three certificate entries:

| Prior attempt | Retained terminal | Structural result |
|---|---|---|
| `dated-archive-20260911` | complete | 8 cells, 10 outputs |
| `dated-book-20260911` | failed | 0 published outputs |
| `dated-book-resource-correction-20260911` | failed | 0 published outputs |

The original verifier independently checked these receipts. Each claim and
terminal byte hash matches its certificate binding, and each historical experiment
object matches the new gate. Three retained claims plus one historical attempt
exactly exhaust the unchanged original budget. No amended target claim exists at
review. The failures continue to count; their eight intended cases each remain
in their claims even though no completion cells were published. The first failure
computed books in memory before the statistical import failed. The second failed
during input-source admission before evaluation. Neither is erased or treated as
proof that no prior calculation occurred.

Inputs, window objects, eight case identities, stage, exploratory reuse, selection
and every other non-harness experiment field equal the failed parent exactly.
The seven economic/dependency files are byte-identical. The only replaced harness
components are the launcher and resource guard; six explicitly pinned successor
runtime modules are added. The launcher diff preserves input order, the same
evaluate call, three outputs and self-RSS check. The unchanged outputs are
`books.json`, `summary.json` and `resource.json`.

## Repair and resource admission

The implementation review found and resolved a dataset-identity/history reset
defect. Independent counterexamples now reject that reset in both admission and
structural verification. The final successor and actual invented dated-CLI suites
passed 35 focused tests in the reviewer's run. The exact `main()`/argument parser,
real statistical dependencies, Git checks, certificate lifecycle and verifier ran
on invented full-shape input with all eight cases and eight HAC calculations
complete and all three outputs retained. No real financial sample was evaluated
by the review or preflight.

The durable repair evidence is:

- Guard report SHA-256
  `5145f45423b8342a6f0d9ae1f1cb6894f34812eb420b079da960a9b7e13488c1`.
- Result report SHA-256
  `996b39b582e1f2a49210efa0a2ef0f2fdc0fd1264935cdb585ec3c453ed8048f`.
- Repair wrapper SHA-256
  `1fa79b0f6607d3789ad3baae388a357a0837fad53be00001a64bcf21b59efc52`.

The wrapper binds the reviewed economic manifest. The retained reports show exit
zero, no limit reason, 9.93514 seconds, 434,868,224 bytes peak sampled aggregate RSS,
two CPUs and the frozen 512 MiB/120-second limits. RSS is nominally sampled every
20 ms with a single process-exit retry; transient overshoot and shared-page double
counting remain possible. Individual `ru_maxrss` is not aggregate RSS. This is a
foreground, no-network execution; detached or secondary-thread subprocesses are
outside the reviewed resource contract.

## Scope of approval and required freeze

The narrowly bounded information gain remains justified: preserved verified
inputs and an unchanged fixed economic question lack a completed financial
answer after two documented harness failures. The extension is administrative,
not a new independent statistical trial, fresh sample, outcome-selected
configuration or strategy validation. No chained amendment or second extra
attempt is approved. Failure consumes the only increment and retains every
claim, terminal and partial output.

The coordinator may now rebind only the approval reference in the certificate and
the certificate reference in the gate, leaving the target contract hash and
manifest unchanged. Before execution, commit all source, charter, evidence,
approval, certificate and gate files; verify the final reference hashes and
remote commit equality; use the unchanged reviewed v2 guard and named successor
CLI. This review does not waive those lifecycle requirements or permit a manual
admission bypass.

One exposed 56-day pre-terminal episode cannot establish expected profitability
or power. Fees/lots remain assumptions; actual marks, fills, margin and liquidation
risk remain unavailable, and the trade-high reserve scenario is a nonconservative
proxy. Any historical cash and exposure results require independent reconstruction
after completion. No candidate adoption, capital deployment, trading, paper start
or confirmation claim is approved. No financial outcome or expected-return claim
was tested in this final source/admission review.

# Existing VPS: bounded research deployment prerequisites

September11,2026. The user confirms that the existing VPS is always on and can
host research. This resolves the earlier host-availability question. Research
and local/free foreground collection are already authorized; the remaining
identity/access and deployment facts are not a request for credentials or
repeated research permission. No remote connection or mutation occurred in this
review. Only nonsensitive consolidated-checkout instructions/docs were read.

## Exact retained references

- `AGENTS.md:38`: do not issue VPS systemd edits over SSH; a separately
  authorized rollout follows the reviewed manual route.
- `docs/operations/MANUAL_ROLLOUT.md`: reviewed operator-executed source staging,
  monitor-only switch and rollback. New immutable source directories/checksums
  are required; preserve production checkouts, shared environment and data.
  Its monitor service changes are not part of a research collector deployment.
- `docs/funding-capture/MANUAL_COLLECTION.md`: directly relevant precedent for
  a public, credential-free collector using an immutable source release,
  dedicated writable output root, persistent non-replaced lock and explicit
  interpreter. No environment file is sourced. Its existing funding collector,
  universe/window and production-adjacent paths must not be repurposed silently
  for the new options policy.
- `docs/operations/OPERATIONAL_INTEGRATION_2026-09-10.md:7`: prior read-only
  inspection at2026-09-10 10:23:56UTC, with selected hashes/service/data metadata.
  The document explicitly did not establish current scheduler commands,
  interpreter/dependency versions or account state. It is historical evidence,
  not permission to import an application or inspect environment files now.
- `docs/superpowers/plans/2026-05-30-v5-8coin-live-DEPLOY-HANDOFF.md:4`:
  historical host label **pck-preds-1**, address **46.225.169.184**, described
  then as Binance testnet deployment. The old instructions say deployment steps
  run as root; that does not establish today's SSH login, configured alias,
  available authentication or intended research host. Its trading performance
  claims and restart instructions are obsolete scope, not actionable research
  instructions.

No `.ssh/config`, key, token, credential directory, `.env` or environment-file
contents were inspected. No broad runtime/import/deploy script was invoked.

## Standalone research placement is feasible in principle

The new collector can be independent of the monitored/trading application:
immutable reviewed source package, its own pinned minimal interpreter/runtime,
dedicated research output and lock directories, explicit clock/resource bounds,
and a manually launched research-only command. This need not overwrite either
`/opt/tradingagents/repo` or `/opt/tradingagents/predlab`, mutate shared virtual
environments, use their journals or touch existing services/timers. No monitor
switch, application import, paper writer, exchange client with account access,
production scheduler restart or systemd change is intrinsically necessary.

Use a new path selected for the existing account's writable area, for example
an as-yet-uncreated `research/options-<registered-id>` beneath that account's
designated research root. This is a proposed layout, not an observed remote
directory or a created deployment. Avoid guessing that production-owned
directories are writable or that a new OS user/sudo is needed. Keep data,
source, locks and logs scoped to the new research root. Existing folders must
not be overlaid; source/data hashes and actual raw backup delivery are separate.

Manual launch does not by itself guarantee continuation after terminal/session
loss or reboot. Determine the already available persistent execution method
without reading private configuration. If the user supplies an established
terminal multiplexer/operator launcher, the dedicated research process can use
that after the exact command is reviewed. Do not silently install a new daemon,
cron task or systemd unit. Any needed service changes must follow the operator
manual route; this review supplies no such changes or command execution.

## Narrow facts to resolve, while useful preparation continues

1. Confirm whether the historical pck-preds-1/address identifies the intended
   current VPS; obtain only the saved public hostname/alias and login reference
   or normal existing access method, never keys/passwords.
2. Establish an existing writable research root, compatible pinned interpreter
   and free resource allowance using a separately scoped read-only inspection
   when access is available. Do not infer Python3.13.13 or install into the
   shared trading environment merely because local tests pass.
3. Identify the existing allowed persistent/manual launch method, its ownership,
   reboot/session behavior and stop procedure. User-confirmed uptime removes
   the laptop-sleep concern; process persistence still needs concrete evidence.
4. After offline independent acceptance, finish the bounded research collector's
   restart/missing-slot/lock/output limits and exact source package/runbook.
   Stage only the reviewed package under its separate research root. Initial
   empirical instrument selection/capture still requires its own committed
   cumulative gate and exact source definition before outcomes.

No current SSH username/alias or authentication method was established by this
bounded documentation search. The historical host reference is sufficient to
avoid claiming no host evidence exists, but not sufficient to connect by guess.
The parent is already resolving these nonsensitive facts with the user. Their
arrival should resume the concrete next step, not leave a stale blocked status.
No routine collection approval is missing, and the current research phase is
not closed by this prerequisite note.

## Subsequent read-only verification and pause request

The user confirmed pck-preds-1 /46.225.169.184. A bounded read-only SSH connection
as root succeeded with existing authentication and strict host-key checking.
It reported Python3.12.3 at/usr/bin/python3, no python3.13 on PATH, installed
tmux/screen,2CPUs and39,759,348KiB available on the root filesystem. No credentials
were inspected, no package was installed and no remote file or process was created.
This supersedes the unresolved host/login statements above, not the deployment
requirements. Availability of tmux is not proof of a running collector or reboot
persistence.

The user requests finishing the current options/VPS work and then pausing research.
The collector remains incomplete and unlaunched; no final source package, effective5
grant, absolute future sample window or assignment has been frozen. The choice of
whether hourly collection may continue during the pause is pending. No systemd
change is needed or authorized through this note, and the historical monitor or
funding rollout commands must not be substituted for an options launch command.

# Options entry component: staged independent review

September 11, 2026. **PASS for the final frozen contract in the addendum below.**
Only the charter and already saved primary-source/schema notes were read.
No request, option quote/index/Greek, saved strike/expiry selection or financial
outcome was inspected. The concurrently running triangle capture was not read.
No test or heavy workload was launched during that capture.

No document-level blocker was found in `options-entry-charter.md`. The concrete
seven-request, buyer-entry-only charter supersedes the earlier illustrative
nine-request, hedge/seller and larger-resource proposals. Missing historical
EOH semantics remain unrelated to current source admission. Known RVIV and
both new options source attempts remain exposed; this is the third/final new
options investigation rather than a budget reset.

The declared formula is consistent within its stated FAQ price/quantity
convention: premium A*q and transaction charge min(r*S*U,0.10*A)*q. Unit is not
applied a second time to premium. Both fixed fee scenarios remain illustrations
rather than the user's rate. Comparison with1,000/10,000 capital concerns the
same minimum buyer ticket only, never affordability of a hedged account or
sell-to-open margin. Fee/size/capital checks cannot establish expected profit,
liquidation protection, access, true delta or execution.

Selection uses only saved literal metadata, admitted current server time and
corresponding index. Fixed expiry distance/earlier-expiry and strike distance/
lower-strike/lexical ties cannot depend on premium or apparent capital fit.
Unique parent mapping for underlying/base/quote/settlement avoids the nonexistent
leaf-settlement field trap. Missing status may support only a publicly quoted
representative with status unknown; it cannot imply trading permission. Failed
selected instruments cannot be replaced. The stated seven source and eight
component slots preserve all dependent missing cases; missing mark must not
erase a separately admitted buyer entry component.

Before any final approval, source and synthetic tests must establish:

- Exact bounded decimal arithmetic for lot/step consistency, fee cap and
  component<=capital/visible-size>=q boundaries; no float roundoff qualifications.
- Bounded representations before Decimal/Fraction operations, strict/deep JSON
  failure handling and preservation of all source/component unavailable cells.
- Raw receipt persistence before parsing, only selection prerequisites before
  capture completion, one public bounded request per slot with no fallback,
  and asynchronous/literal clock evidence without a synchronization claim.
- The fixed candidate identifier/count/tie evidence, unique parent mapping,
  status/enum staleness and malformed/dependency failures.
- All ten immutable outputs, actual12MiB serialization limit,256KiB response
  bound and whole-request deadlines, with current-source worst-payload guarded
  lifecycle proof, full dependency pins and unchanged ancestry/budget.

The source, transport, tests, registration and actual results have not yet been
reviewed. This staged document does not authorize acquisition or financial
calculation. No additional effort is indicated until the concrete source and
frozen gate are ready or a specific unresolved accounting question appears.

## Final source, accounting and gate review

**PASS; no material blocker identified.** The final scope is the one named
third/final new options entry-component attempt, conditional on source commit,
verified remote backup and normal lifecycle admission. No option observation
was inspected or acquired during this review; existing inputs were byte-hashed
only. No real selection, fee component or prospective performance was executed.

Exact reviewed identities:

- Gate: `54d47d9c295a9b91a65b60af8ec16df07ef53554daa277f1f55066c15d5db3ad`.
- Entry source: `89b361580dead7236b1744af05a2729deda5111343e25d9da7974b775fa15487`.
- Bounded transport: `a157814cd1131ca89a1533cd002905664aff7e7ccc9fda0e10e5489cdfbad427`.
- Tests: `43457394cfcc5404d171627b9c1fb05ec5359109b06eafb951a80ae9e5b05a6f`.
- Charter: `3bbccbe373c1da5c8be426e98075c39a06d116349df8774ad1f21b2250953e7b`.
- Serialized specification: `325fb4800241dcd5f3c74cc36fef4fe42d0f02ed68e19093032735321167243b`.

All eight source pins, four input hashes, charter and six original runtime
hashes match. The specification equals the source default. Existing predecessor
triangle-stream gate experiments, family definitions and dataset histories
compare unchanged. The options family remains cap4/prior1, incorporating the
known old RVIV administrative bundle and two new source attempts; this entry
question uses the final new allowance. The saved metadata window and local
specification observation window are distinct from actual future response
clocks and do not supply fresh financial confirmation. Exactly15cells and10
outputs are registered, including every source and component failure.

Selection and arithmetic were independently checked by
`check_options_entry_synthetic.py`, which uses Fraction arithmetic rather than
the implementation's Decimal expected values.28invented cases make140exact
assertions for premium, fee, total cash, capital fit and visible quantity. Cases
include both fee rates, both capitals, unit scaling, cap binding, exact capital
equality, a sub-float capital/size deficit, exponent extremes and64-character
coefficients. Expiry/strike tie rules, inclusive7/45day boundaries, non-TRADING
exclusion, absent-status qualification, exact lot-step exclusion and duplicate
parent rejection also pass. Under the declared64-character/exponent limits,
the512-digit local Decimal contexts have ample precision for these products,
fee comparisons and exact finite-decimal step checks.

Seven targeted tests passed for raw-before-parse order, quote parsing only
after all source slots close, missing mark versus buyer independence, malformed
including deeply nested JSON, and transport overflow/premature Content-Length
EOF preservation. The worker's full31-test result is supporting evidence.
The transport permits only the four declared public HTTPS paths, disables
proxy/redirect handling, preserves partial prefixes, enforces one20-second
whole-request alarm and suppresses remaining same-host calls after denial.
No authenticated call, retry, metadata refresh, premium-based reselection or
hedge/seller computation appears in the reviewed source.

The copied full/denial guard reports were matched byte-for-byte with the
original disposable ResearchRun preflights and their pinned source files.
Both retain15cells/10outputs. The full fixture uses2,000invented metadata rows
and seven256KiB responses, producing2,471,125output bytes, zero unavailable
cells and205,754,368peak sampled RSS bytes in1.9349seconds. The denial fixture
attempts only the first source and retains all15unavailable cells/10outputs,
362,681bytes,204,546,048RSS bytes in1.8134seconds. Both exit0 under the exact
240second/512MiB/two-CPU guard. Normalized selection and capture projections
have separate explicit bounds; a projection failure preserves unavailable
component cases rather than making a favorable selection. The initial reviewer
preflight-directory assertion counted the lifecycle .lock file as a run;
filtering directories resolved that inspection-only error without any rerun.

The mark schema is model evidence only; missing mark does not erase a valid
minimum buyer component. Non-integer unknown metadata enums may fail safe
during JSON projection; they are not coerced into access permission. Index
and depth clock labels are preserved without an exchange-synchronization or
quote-age claim. Personal fees, full hedged-account affordability, seller entry
margin, true delta, expected profit, power, beta, liquidation and graduation
remain explicitly unavailable. This review does not expand the third question
into a follow-up financial book or authorize orders or paper operation.

# Independent completed news provenance review

**PASS for the negative source-admission reconstruction.** All seven source
cells and all 13 subordinate schema slots remain unavailable. This is a bounded
source result, not a failed financial signal or rejection of all news mechanisms.

## Independently observed evidence

The reviewer checked only the same seven registered absolute paths, opening
each component relative to held descriptors with `O_NOFOLLOW`. No alternative
path, directory listing, recursive search, network or inspector implementation
was used. The six literal Alpaca/GDELT monthly roots remain missing. No Parquet
file or footer was opened; all 12 unfilled footer slots remain explicit.

The exact external CSV exists and its recorded 576,048-byte size, device, inode,
mtime and ctime agree with independent before/after descriptor and path checks.
These are best-effort filesystem comparisons, not atomic snapshot or historical
availability proof.

The only CSV content access was **13 unbuffered one-byte reads of the recorded
first physical line**, stopping at its line ending. No byte beyond that region
was read, and no full-file hash was calculated. The region SHA-256 matches
`4f9151e43bb1edad6ff8a4115df39b31136a347d834be98dbdbf280f80dd2d99`.
Its text and arbitrary name were not printed, copied or persisted.

The bounded region is valid UTF-8 and parses as one candidate CSV field. It
contains **zero recognized whitelist fields and zero plausible ID/time fields**
under the frozen normalization and CSV rule. The independent classification
therefore supports the original unavailable-header result. Only counts and
the unknown field-name digest are retained in the review JSON. Because header
semantics are unverified, the first line must not be described as an authenticated
header or as proof that the entire file lacks provenance. No following records
were inspected, and the file was not identified as the historical crypto corpus.

## Receipt, source and resource reconstruction

`check_news_provenance.py` verifies the seven individual sanitized receipts
against their inventory copies, all 13 schema-slot copies against admission,
all cell identities/statuses and the completion denominator. Source observation
times are ordered and lie between claim and completion. The original structural
verifier passes seven unavailable cells and nine outputs. All output hashes
match; total output size is 19,350 bytes, below 4 MiB, and each receipt is below
128 KiB. No raw header, schema values, article rows or full corpus copy appear
in these retained projections.

The source commit `7e5e8b1affb1feb4e729290b943c313a56493a46` predates the claim
and binds gate SHA-256
`e85a08c4ee2878c38aa111506f5d179d83161a430e7aa925b242177bb41f5bee`.
Registered source and six runtime hashes match both their committed bytes and
current files; the sole specification input hash matches its claim. Corpus
files are not substituted into that input-hashing step. The documentary design
window is separate from actual source observation clocks. Receipt verification
does not itself establish pre-execution remote push timing.

The resource report records exit zero, no limit reason, 7.84616 seconds and
46,518,272 bytes peak sampled aggregate RSS under 512 MiB/120 seconds. Sampling,
process-exit retry and individual-child versus aggregate-RSS qualifications
remain explicit. The technical family retains cap one and prior_attempts zero;
this claim consumes the single allowance and the third initial MAP row8 question
after the two WBETH questions. Historical news/LLM search and spent samples remain
inherited; no additional alpha budget follows.

## Decision and practical limit

Decision 15's missing-root and unavailable-schema conclusions agree with this
review. A precise privacy statement is that no bytes after the first physical
line were read, its text was not published and no whole-file hash was taken.
The unrecognized line itself has no authenticated header/content semantics.

Keep these exact source locations unavailable for historical observation- and
version-aware research. The reopening dependency is an identified versioned
metadata manifest or equivalent evidence of the actual stores, observation
clocks and lineage. Do not substitute this CSV, invent a publication lag or
search alternative locations under this spent registration. Missing exact paths
do not prove destruction or universal absence, and metadata-region hashes do
not establish an externally recoverable corpus backup.

The separately proposed final triangle persistence source investigation is a
distinct next question if its own fixed scope, resource limits, synthetic
validation and registration are admitted. Nothing in this review admits article
extraction, return modeling, expected-profit inference, power or graduation.
Historical availability, revision authenticity, entity linkage, corpus identity,
financial utility and external backup remain unverified.

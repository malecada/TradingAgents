# Crypto trading research — operative instructions

Read [docs/RESEARCH_START.md](docs/RESEARCH_START.md), then the linked current
state. Detailed historical results belong in the evidence index and reports,
not in this bootstrap. User instructions determine the active scope.

## Research boundaries

- Zero strategies are validated. Historical failed, conditional and unavailable
  outcomes remain distinct. The user launched the new research phase on
  September 11, 2026; follow current state and its committed program gates.
  Research authorization does not itself admit paper or production operation.
- The objective is positive net returns with little overall crypto-price
  exposure, initially approximately $1,000 and potentially $10,000. Binance is
  available according to the user; Bitrue product/account applicability is
  unverified. Public API access does not establish account eligibility.
- Historical BZRX/LUNA/BNX settlement recovery and its 22 blocked cases remain
  explicitly deferred. No paid data, provider contact or automatic revisit.
- Use the applicable tracked skills in `.agents/skills/`: research governance
  for empirical claims, provenance for market-data changes, research-cycle for
  registered run admission and continuation. They support the authorized task;
  ordinary engineering does not require a financial trial registration.
- Before outcomes, commit a charter and exact gate/configuration for any new
  financial experiment, correction or quote measurement. Check the family's
  prior trials, sample exposure and cumulative budget. Preserve old gate
  objects, artifacts and all attempted/unavailable cells. No completed run is
  repeated merely because context was lost.
- Use signed quantities/cashflows or a valid simple-return book for trading PnL.
  Never book log returns as arithmetic PnL. Include the registered convention
  diagnostic and independent accounting/data checks; synthetic checks do not
  establish an economic edge. Missing held prices, funding and settlements stay
  unknown. Match the engine to the instrument; see the engine index.
- Spent/inspected windows are not fresh confirmation. LLM signals must respect
  training cutoff and memorization risk. Negative gates are specific failed
  claims, not proof that all related mechanisms lack value.
- Do not read credentials, `.env` contents, keys, private token files or account
  secrets. No real orders, production changes or external messages are
  authorized by research preparation. Do not issue VPS systemd edits over SSH;
  any separately authorized rollout uses the reviewed manual instructions.

## Engineering and coordination

- Use this consolidated checkout, not preserved sibling experiment mains.
  Inspect branch/status first. Keep raw stores and dated evidence immutable.
- Use the pinned local research environment and the named offline verification
  target documented at the start page. Historical scripts can fetch, refit,
  write outcomes or place orders; a script name is not an execution permit.
- Search the specific directory with `rg`; exclude `.venv/`, `node_modules/`,
  `.git/` and secret locations. Read linked evidence only as needed.
- Give each mutation one owner. Independent review must retain access to raw
  evidence and must not simply reuse the implementation's expected answers.
- Perform authorized reversible preparation without repeated permission
  questions. A framework skill cannot reset frozen research criteria or expand
  external authority. Preserve source/data hashes and a precise checkpoint.
- Documentation describes the author's solo work in impersonal prose; do not
  write “we/our”. Retain upstream attribution. Push reviewed branch changes for
  backup when authorized; manuscript changes also require pushing the separate
  thesis repository. A configured remote alone is not proof of recoverability.
